# Playwright E2E test credentials
# Use the dedicated test account (ps4), not the primary user account.
TEST_USER_EMAIL=
TEST_USER_PASSWORD=

# Optional: override Playwright base URL.
# Defaults to http://localhost:8080 when unset.
# BASE_URL=http://localhost:8080
