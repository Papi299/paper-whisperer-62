# PFA-C03 — Staging Environment and Automated Security-Test Contract

> **Status: PFA-C03 is COMPLETE — Phase A and Phase B have both shipped and this document is the reconciled record.** C03A1-L and C03A2-L are merged and accepted, so **Phase A is complete** for the selected local-first path. **Phase B is complete:** the pre-existing security defects found while authoring C03B1 were remediated, merged, and deployed to Production (PR #177, merge `f408870…`, migration `20260802025704`; §9.6); C03B1's four comprehensive pgTAP suites + the `test:db:local` runner + the true-concurrency probe are merged and accepted (PR #179, merge `0f786dac…`; §9.7); and **C03B2 — the hosted, non-required `DB Tests / db-tests` workflow — is merged and accepted** (PR #181, merge `b57614182fdabff94bc26d128585c7754b7ae45c`; §9.8), so `.github/workflows/db-tests.yml` **now exists on `main`** and ran green on the merge commit itself. D3 resolved (2026-08-02, pgTAP); **D4 is RESOLVED (2026-08-16) → `DETERMINISTIC_STAND_IN`** (§12); **D5 is RESOLVED (2026-08-16) → `REQUIRE_DB_TESTS`** — the owner accepted the §16 audit recommendation and the required-status-check policy was **activated the same day**, so **`validate` and `db-tests` are both required merge gates** (each bound to GitHub Actions app `15368`, strict/up-to-date retained) while **`e2e-local` remains non-required** (activation record §17, canonical entry §12, durable decision in [decisions-and-triggers.md](decisions-and-triggers.md)). This document is the durable implementation and decision contract for **PFA-C03 — Safe staging environment and automated security tests**. **C03A0** (this contract) is complete; **C03A1-L** (local-stack seed/reset + env/guard foundation) is **merged to `main` and independently accepted** (PR #173, merge `299b19a…` — see §7.3, §10, §14); **C03A2-L** (non-required local Playwright CI workflow, `E2E (local)` / job `e2e-local`) is now **merged to `main` and accepted** (PR #175, merge `1d091e476461b50375083ac4c1a156e7c38a0b4b` — see §8.1, §10, §14) — its workflow file `.github/workflows/e2e-local.yml` now **exists on `main`**. **Phase A is complete for the selected local-first path. Phase B is complete:** C03B1 authoring uncovered pre-existing database-security defects; the bounded remediation was merged and deployed (PR #177, merge `f4088705a2d3b8869579b253b5d8f7b3162414cf`; migration `20260802025704` applied to Production, ledger 70) — see §9.6; C03B1's implementation was then merged and accepted (PR #179, merge `0f786dac7088b2afafc16cb76afae8eee5f206d6`; §9.7); and **C03B2's hosted `db-tests` workflow is merged and accepted** (PR #181, merge `b57614182fdabff94bc26d128585c7754b7ae45c`; §9.8). **D3 is resolved (2026-08-02, pgTAP); PFA-C03 is complete.** The **`Validate / validate`** gate is unchanged and still required; since the D5 activation on 2026-08-16 (§17) **`DB Tests / db-tests` is also required**, and `E2E (local) / e2e-local` remains **non-required**. No **cloud** Supabase project, GitHub secret/variable/Environment, or cloud fixture exists. On the database-test side, **all of it now lives on `main`**: the focused pgTAP regression file (`supabase/tests/database/000_preexisting_security_regressions.test.sql`, 139 assertions, merged with the §9.6 security remediation), the **full C03B1 four-suite pgTAP architecture** (`001`–`004`, 255 assertions), the framework-free 18-case verification, the **`test:db:local` local runner and its integrated lifecycle** (merged via PR #179 — see §9.7), **and the hosted `DB Tests / db-tests` workflow that executes them automatically** (merged via PR #181 — see §9.8). **Automated hosted DB-security CI therefore exists on `main`, and since the D5 activation on 2026-08-16 it is a *required* merge gate** (PFA-C03 itself shipped it as non-required; promotion was D5, resolved separately — §17). pgTAP was selected by D3; every suite creates the `pgtap` extension **transactionally** and rolls back, so **no persistent pgTAP installation is added through a migration**. The remaining named cloud artifacts remain **recommendations or future artifacts**.
>
> **Owner selection.** The owner selected **PFA-C03** on 2026-07-31. Its first bounded subtask, **PFA-C03A0** (contract + readiness audit), produced this contract; **PFA-C03A1-L** (local-stack seed/reset + env/guard foundation) was then **implemented, merged, and independently accepted** (PR #173, merge commit `299b19a08da0a61984bf2333d12406cbfd62aef7`, merged-main `Validate` run `30697869074` success, Vercel Production `dpl_9zhjYVmjLKTKqrt7PW6haZej9rLf` READY on `app.paperlume.app` — see §7.3, §10, §14); and **PFA-C03A2-L** (non-required local Playwright CI) has since been **merged and accepted** (PR #175, merge commit `1d091e476461b50375083ac4c1a156e7c38a0b4b`, merged-main `Validate` run `30711943868` and `E2E (local)` run `30711943858` both success, Vercel Production `dpl_4fZ4zGs2itrzKamigJF4gcPGGkVv` READY on `app.paperlume.app` — see §8.1, §10, §14). This document still ships no cloud project/workflow/framework of its own. Phase A is complete for the selected local-first path, Phase B is complete (§9.6–§9.8), and **PFA-C03 is complete** (§14).
>
> **Labels used throughout:** `[FACT]` current repository truth · `[REC]` recommendation · `[FUTURE]` future implementation · `[OWNER]` owner action · `[DECISION]` unresolved owner decision.
>
> **Model naming (read this first).** The 2026-07-24 audit called its *two-phase structure* "Model A" (Phase A = test backend + non-required Playwright CI; Phase B = DB-security tests). **This contract keeps that two-phase structure but reuses the names "Model A / Model B" for the *test-backend* decision**, which is the substantive architecture choice:
> - **Model A — local-first (recommended default):** an **ephemeral local Supabase stack** started inside CI; no cloud project, region, plan, cloud account, or cloud reset credential.
> - **Model B — persistent cloud staging:** a separate Supabase **cloud** project — chosen either as a deliberate **cloud-first** architecture (decision D2), or added **later for parity** when a wave demonstrates a need Model A cannot cover.
>
> Local-first is the **recommendation, not a mandate**; cloud-first is a genuine D2 option (§7.3, §12). Which Phase A phases are *required* depends on that D2 choice (§10).
>
> Where the audit's two-phase structure is meant, this document says **"Phase A / Phase B"**, never "Model A", to avoid collision.

---

## 1. Status and scope

- **Initiative:** PFA-C03 (product-feature-audit candidate; §12/§14 of [product-feature-audit.md](product-feature-audit.md)). Two phases: **Phase A** = an isolated test backend + a non-required Playwright CI job; **Phase B** = representative automated database-security/integrity tests. `[FACT]`
- **This task:** PFA-C03A0 — produce the contract and readiness audit only. Two documentation files change: this file and [start-here.md](start-here.md). No code, tests, workflows, packages, configuration, migrations, or generated types change. `[FACT]`
- **In scope of the contract (planning):** the local-first vs optional-cloud test-backend decision tree; deterministic fixtures/reset/cleanup; a two-layer fail-closed Production guard; a non-required Playwright CI job; a **separate** non-required Phase B database-security suite that reuses/evolves the existing SQL verification with an expected-failure negative-control; a phased PR roadmap; owner actions with public-config-vs-secret classification; an owner decision packet; a risk register; a definition of done. `[REC]`
- **Explicitly out of scope of this task:** provisioning anything, running Playwright, running any Supabase stack or CLI mutation, changing `.github/workflows/validate.yml`, adding a workflow, adding secrets, or merging. `[FACT]`
- **Guardrails that constrain the whole plan:** C27 (commercialization paused), C28 (owner/manager roles + owner AI exemption), C29 (Gemini Free Tier; Google Cloud billing disabled; provider-quota dashboard deferred) all remain **unchanged**. The required `Validate / validate` gate remains **unchanged and required**; any Phase A/B job is a **separate** workflow/check, never a new job silently added to `Validate`. Playwright-against-Production and real-Gemini Playwright remain **prohibited**. `[FACT]`

---

## 2. Verified current baseline

The C03A0 audit reconfirmed all items on branch `docs/pfa-c03-staging-contract` (cut from `origin/main` `5b78b08d`); the migration ledger (§2.4) and the database-test inventory (§2.5) below are **updated to current `main` (post-PR #184)** where they have since advanced beyond that audit point.

### 2.1 Required CI

- `.github/workflows/validate.yml`, workflow name `Validate`, job `validate`, on `pull_request → main`, `push → main`, and `workflow_dispatch`. Runs `npm ci` → tool-version report → `npm run lint` → `npm run typecheck` → `npm test` (Vitest) → `npm run build`. Node `22.x`, `ubuntu-24.04`, `timeout-minutes: 20`, `permissions: contents: read`, concurrency-cancel. Build env supplies **inert public placeholders** `VITE_SUPABASE_URL=https://example.supabase.co` and `VITE_SUPABASE_PUBLISHABLE_KEY=ci-placeholder-publishable-key`. **No** Playwright, browser install, Supabase command, migration replay, database test, or deployment. `[FACT]`
- This workflow was the **sole** required merge gate for `main` throughout PFA-C03, and must not be weakened, renamed, reordered, made to depend on staging, or extended with a Phase A/B job. It remains required and unchanged; since the D5 activation on 2026-08-16 it is joined by a **second** required gate, `db-tests` (§17). `[FACT]`

### 2.2 Playwright model

- `playwright.config.ts`: `testDir ./e2e`, `fullyParallel: false`, `workers: 1`, `retries: CI ? 2 : 0`, `timeout: 30_000`, `reporter: CI ? "github" : "list"`. Two projects: `setup` (matches `global-setup.ts`) and `chromium` (depends on `setup`, `storageState: e2e/.auth/user.json`). `webServer.command: "npm run dev"`, `port: 8080`, `reuseExistingServer: !CI`. `use.baseURL = process.env.BASE_URL || "http://localhost:8080"`. `[FACT]`
- `playwright.config.ts` manually parses `.env.test` (if present) and injects **only** the keys it finds there into `process.env`, without overwriting values already set (`if (!process.env[key])`). `[FACT]`
- `e2e/global-setup.ts`: reads `TEST_USER_EMAIL` / `TEST_USER_PASSWORD` (throws if missing), signs in through the real `/auth` UI, and asserts the dashboard renders a paper count (`/\d+\s+paper/i`) before saving `storageState` to `e2e/.auth/user.json`. **This hard-requires the test account to have ≥ 1 paper.** It runs in Node/Playwright and drives the browser; it does **not** and cannot read the browser's `import.meta.env` directly (see §5). `[FACT]`

### 2.3 Frontend env contract

- `src/lib/clientEnv.ts`: `requireClientEnv(name)` reads `import.meta.env[name]` and throws an actionable error if empty/blank. `src/integrations/supabase/client.ts` calls it for `VITE_SUPABASE_URL` and `VITE_SUPABASE_PUBLISHABLE_KEY` at module load and constructs the Supabase client. `[FACT]`
- `vite.config.ts` is minimal (React SWC, `@` alias, dev server on port 8080); it does **not** customize env-file loading, so Vite's default dotenv resolution applies. `[FACT]`
- Tracked env examples: `.env.example` (`VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`) and `.env.test.example` (`TEST_USER_EMAIL`, `TEST_USER_PASSWORD`, optional `BASE_URL`). `.gitignore` ignores `.env` and `.env.*` except those two examples, and ignores `e2e/.auth/user.json`. `[FACT]`

### 2.4 Supabase backend

- `supabase/config.toml`: `project_id = "lioxtgiputfniqbktcsz"` (Production ref); `verify_jwt = false` for all three functions (in-body `auth.getUser()` is authoritative). `[FACT]`
- Migrations: **71** tracked files, chain `20260203072053_… → 20260807141402_repair_merge_exact_duplicates_jsonb.sql` (Local = Remote; the C03A0 audit reconfirmed a **69**-migration chain ending `20260731162729`, the §9.6 security remediation then added `20260802025704`, applied to Production — ledger 70 — and migration **71**, `20260807141402`, is a **post-C03 corrective migration** from the separate `MERGE-EXACT-DUPLICATES-JSONB-REPAIR-001` repair (§9.6.1), not part of the original PFA-C03 implementation sequence); storage/quota-relevant migrations include `20260318010000_add_paper_attachments`, `20260318020000_add_attachments_storage_policies`, `20260327100000_private_attachments_bucket`, `20260521030000_harden_attachment_privacy_and_storage_quota`, plus the entitlement/quota schema `20260521010000` and AI-quota RPCs `20260521020000` / `20260724120000` / `20260725090000`. `[FACT]`
- Edge Functions (source in `supabase/functions/`): `fetch-paper-metadata`, `analyze-paper`, `get-gemini-provider-quota`, plus shared helpers. Secret names referenced: `GEMINI_API_KEY`, `GEMINI_MODEL` (optional override), `GOOGLE_CLOUD_PROJECT_ID`, `GOOGLE_MONITORING_CLIENT_EMAIL`, `GOOGLE_MONITORING_PRIVATE_KEY`. External hosts referenced: PubMed (`eutils.ncbi.nlm.nih.gov`, `pubmed.ncbi.nlm.nih.gov`), Crossref (`api.crossref.org`, `doi.org`), Google (`generativelanguage.googleapis.com`, `monitoring.googleapis.com`, `oauth2.googleapis.com`, `www.googleapis.com`), `esm.sh`. `[FACT]`

### 2.5 Existing database verification

- `supabase/tests/owner_access_and_quota_verification.sql` exists: local-only, framework-free, single transaction wrapped in `BEGIN … ROLLBACK`, run manually via `psql -v ON_ERROR_STOP=1` against a local stack. It simulates callers with `SET LOCAL ROLE authenticated` + a `request.jwt.claims` GUC and covers **18 cases**: role resolution (user/owner/manager) and `is_exempt` via `get_current_user_access()`; ordinary user cannot read/insert/update `internal_user_access`; anon cannot execute the access RPC; null-auth rejection; no arbitrary-user inspection (0-arg RPC); Free 15-lifetime cap; Pro 350-monthly cap; exempt owner allowed beyond cap with usage still incrementing; exempt refund decrements the same bucket; refund floor at 0; missing/inactive entitlement safety; `is_exempt` reporting; no email-based role column; grant hardening (FORCE RLS, zero policies, revoked direct grants for anon/authenticated, retained `service_role` CRUD, RPC EXECUTE boundary). `[FACT]`
- **This was the reusable Phase B seed, and it is preserved byte-identical** — the merged C03B1 suites run it **alongside** the pgTAP suites, never duplicating or ignoring it (§9.6–§9.7). `[FACT]`
- **The C03B1 database-security layers now exist on `main`** (PR #179, §9.7): the focused `supabase/tests/database/000_preexisting_security_regressions.test.sql` (139 assertions), the four comprehensive pgTAP suites `001_core_rls_isolation` / `002_relational_rls_isolation` / `003_rpc_caller_scope_and_grants` / `004_storage_and_quota` (255 assertions), the preserved framework-free 18-case `owner_access_and_quota_verification.sql`, and the **`npm run test:db:local`** local runner and its integrated lifecycle. A **sixth** suite, `005_merge_exact_duplicates_success.test.sql` (**77 assertions**), was added later by the separate `MERGE-EXACT-DUPLICATES-JSONB-REPAIR-001` repair (§9.6.1) — **not** by C03B1 — bringing the current aggregate on `main` to **471 pgTAP assertions** (`000` = 139, `001`–`004` = 255, `005` = 77) alongside the unchanged framework-free **18/18**. **Both local and hosted database-security execution now exist on `main`:** the C03B2 `DB Tests / db-tests` workflow is **merged and accepted** (PR #181, merge `b5761418…`; §9.8) and runs that same lifecycle automatically on same-repository pull requests and `main` pushes. PFA-C03 shipped it as **non-required**; it became a **required** check on 2026-08-16 when the owner resolved D5 to `REQUIRE_DB_TESTS` (§17). `[FACT]`

---

## 3. Changes since the 2026-07-24 product-feature audit

- **`supabase/tests/` now exists.** The audit stated "no `supabase/tests`". That is no longer accurate: `owner_access_and_quota_verification.sql` (18 cases) is committed. Phase B **reuses** it. `[FACT]`
- **Baseline commit advanced** `5a92229f` → `5b78b08d` (PRs #165–#169 merged). `[FACT]`
- **Three Edge Functions are deployed** (`fetch-paper-metadata` v10, `analyze-paper` v15, `get-gemini-provider-quota` v3), the third deployed-but-unused under C29. `[FACT]`
- **C29 exists** (2026-07-26): Gemini Free Tier; Google Cloud billing disabled; provider-quota dashboard deferred. No test backend replicates Google Monitoring or the provider-quota path (§6). `[FACT]`
- **Audit-time baseline — superseded:** At the time of the 2026-07-24 audit, Playwright drove the local dev server against the **Production** Supabase project and was excluded from CI. That state is **no longer current**: C03A1-L now **fails closed** on Production or remote targets, and C03A2-L runs the six-spec subset in the separate **non-required `E2E (local)`** workflow. **Updated since the audit:** the **full C03B1 pgTAP architecture** (four suites `001`–`004` + the focused `000` file) and the local **`npm run test:db:local`** runner now **exist on `main`** (PR #179, §9.7); the **hosted non-required `DB Tests / db-tests` workflow also now exists on `main`** (C03B2, PR #181, merge `b5761418…`, §9.8), while **no Edge Function automated-test workflow exists**; required `Validate` still does **not** run Playwright or any database-security test (it remains lint + typecheck + Vitest + build only); and `E2E (local)` remains a separate **non-required** Playwright workflow. `[FACT]`

---

## 4. Full E2E inventory

`e2e/` contains **13 spec files** plus `global-setup.ts` (auth setup) and `helpers.ts` (shared mutating helpers). All run Chromium-only, single-worker, serial, using the one authenticated `storageState`. Every spec that calls `waitForDashboard`/the `\d+ paper` assertion depends on the account having **≥ 1 paper**.

### 4.1 Classification matrix

| Spec | Primary workflow | Class | DB/Storage entities touched | Cleanup | Model-A (local) safe? | Initial CI subset? |
|---|---|---|---|---|---|---|
| `auth.spec.ts` | Auth page + logged-in dashboard | **Read-only** | none | n/a | Yes | **Yes** |
| `bulk-actions.spec.ts` | Select-all / selection toolbar | **Read-only** | none (select/deselect only) | n/a | Yes | **Yes** |
| `eager-load.spec.ts` | Lazy-load, select-all-IDs, export menu | **Read-only** | none (opens export menu; never exports/mutates) | n/a | Yes | **Yes** |
| `filters.spec.ts` | Search box, year filters, clear, columns | **Read-only** | none | n/a | Yes | **Yes** |
| `paper-import.spec.ts` | Add-Papers dialog states | **Read-only** | none (never submits an import) | n/a | Yes | **Yes** |
| `pools.spec.ts` | Sidebar pools/projects/tags dialogs, export menu | **Read-only** | none (opens dialogs only) | n/a | Yes | **Yes** |
| `filter-presets.spec.ts` | Saved-search CRUD | **Mutating** | `filter_presets` (insert/update/rename/delete) | `beforeEach` + `afterEach` delete all `E2E-*` presets | Yes (prefix-isolated) | Wave 2 |
| `file-import-order.spec.ts` | `.bib` file import + order | **Mutating** | `papers` (+ junctions) create/delete; **no external call** | pre-clean + post-clean by `E2E-Test*` prefix | Yes | Wave 2 |
| `mutations.spec.ts` | Create project/tag, assign to first paper, statistical-methods round-trip | **Mutating** | `projects`, `tags`, junctions, one manual `papers` row | in-test removal + `finally` paper delete | Yes (needs seed) | Wave 3 |
| `notes.spec.ts` | Notes add/edit/clear/filter/search | **Mutating** | `notes` field of 2 pre-existing papers | per-test restore-to-empty + `afterAll` defensive clear | Yes (needs seed) | Wave 3 |
| `search-attribution.spec.ts` | 6-field `Matched in:` attribution | **Mutating** | title/authors/journal/abstract/keywords/notes of **first** paper | `afterAll` restores captured originals | Yes (needs disposable paper) | Wave 3 |
| `attachments.spec.ts` | Attachment upload/open/refresh/delete | **Mutating + Storage** | `paper_attachments` rows + private-bucket objects; signed-URL HEAD fetch | pre-clean + in-group delete | Yes — local Supabase includes Storage | Wave 3 |
| `import-order.spec.ts` | Identifier import + order | **Mutating** | `papers` via a deterministic stand-in fulfilled at the `fetch-paper-metadata` boundary; real bulk-insert RPC | pre-clean + post-clean by `D4-E2E-*` title | Yes — no served Edge Function or egress needed (D4, §12) | Wave 4 / resolved; default lane |

### 4.2 Per-spec detail (auth, pre-existing data, external calls, order dependence, cleanup-failure consequence, remediation)

- **auth** — logged-out (fresh `storageState: undefined`) + shared logged-in state; ≥ 1 paper; no external calls; independent; no cleanup. `[FACT]`
- **bulk-actions / eager-load / filters / paper-import / pools** — shared logged-in state; ≥ 1 paper (eager-load adds assertions only when the library is large, and select-all asserts selected == total); read-only; no cleanup. For eager-load to exercise the large-library branch, a seed > 100 papers is desirable but not required. `[FACT]`
- **filter-presets** — ≥ 1 paper (dashboard loads); no external; serial; robust `E2E-`-prefix cleanup (`beforeEach`/`afterEach`, capped loop). Cleanup-failure: orphaned `E2E-*` presets, reaped next run. Some empty-state assertions assume no non-`E2E-` presets (defensively skipped) — a dedicated seed satisfies this. `[REC]`
- **file-import-order** — imports 3 papers from `e2e/fixtures/test-import-order.bib` (client-parsed, no external metadata); asserts reverse-insert order + two refreshes + `+3` delta; strictly serial (count-sensitive); leftover `E2E-Test*` reaped by pre-clean. **No concurrent library writer during it.** `[FACT]`
- **mutations** — creates `_e2e_proj_<ts>`/`_e2e_tag_<ts>`, assigns to the **first row** (`insert_order DESC`), then unassigns + deletes; second group creates/deletes a manual `_e2e_paper_<ts>`. Serial; depends on a stable first paper. Leftover `_e2e_*` on crash. Remediation: deterministic seed + unique names (already good). `[FACT]`
- **notes** — picks the first two of the first-30 rows with **no** notes indicator; unique per-run tokens; restores empty per test + `afterAll`. Needs **≥ 2 note-less papers in the first 30 rows**. No external; serial. Remediation: a seed with known note-less papers. `[FACT]`
- **search-attribution** — appends per-field tokens to **all six** searchable fields of the **first** paper; `afterAll` writes captured originals back. Serial. **Highest cleanup-failure blast radius:** an `afterAll` failure leaves a paper carrying `e2eattr*` across six fields. Remediation: run against a **disposable seeded paper** created/destroyed per run, not a real first paper. `[REC]`
- **attachments** — uploads `fixtures/test-attachment.png` to the private bucket for the **first** paper, opens the signed URL (in-page `HEAD` → 200), asserts persistence, deletes; group 2 confirms the SVG is rejected **client-side**. Needs the private bucket + owner-scoped policies + storage-quota triggers (**all created by migration replay; no cloud required — local Supabase provides Storage**). Leftover object/row reaped by pre-clean. `[FACT]`
- **import-order** — **superseded by the D4 resolution (§12).** It formerly imported fixed PMIDs `39140285/6/7` through **live PubMed/Crossref** and asserted exact remote title prefixes, which is why it needed a served `fetch-paper-metadata` plus egress and stayed out of the automated lane. It now imports three synthetic identifiers whose metadata is fulfilled deterministically at the `fetch-paper-metadata` HTTP boundary, asserts the request order, the single authenticated invocation, `+3`, exact reverse-insert order and two refreshes, and deletes its own rows again. No provider egress; runs in `DEFAULT_SPECS`. `[FACT]`

### 4.3 Shared modules

- **`e2e/global-setup.ts`** — the single auth gate; requires `TEST_USER_EMAIL` / `TEST_USER_PASSWORD` and a ≥ 1-paper account. `[FACT]`
- **`e2e/helpers.ts`** — mutating helpers (`importPapersByIds`, `deletePapersByTitleSubstrings`, `createProject`/`createTag`/`deleteProject`/`deleteTag`, `openEditPaperDialog`) + read helpers. Underpins cleanup. `[FACT]`
- **`e2e/fixtures/`** — `test-attachment.png`, `test-import-order.bib`, `test-invalid.svg`. `[FACT]`

**Cleanup caveat (mandated):** several specs are "safe" only if `afterAll`/`finally` cleanup succeeds. `search-attribution` (six real fields) and `mutations`/`attachments` (first-row dependence) are **not** safe against a crash, cancelled run, or overlapping run. The reset model in §7 must not treat cleanup helpers as the isolation boundary.

---

## 5. Environment-resolution analysis and the executable two-layer Production guard

### 5.1 How values resolved at audit time (pre-C03A1-L) `[FACT]`

Two **independent** environment surfaces exist during a Playwright run; conflating them is the root risk:

1. **The Playwright runner process (Node)** — `playwright.config.ts` reads `.env.test` and sets `TEST_USER_EMAIL`, `TEST_USER_PASSWORD`, and (if present) `BASE_URL` into `process.env` (non-overwriting). `global-setup.ts` consumes the two credentials; `baseURL` defaults to `http://localhost:8080`.
2. **The Vite dev server (browser bundle)** — `webServer.command: "npm run dev"` (= `vite`, development mode). The frontend's `VITE_SUPABASE_URL` / `VITE_SUPABASE_PUBLISHABLE_KEY` resolve through **Vite's own dotenv loading**, which reads `.env` / `.env.local` — **not** `.env.test`. On this machine the untracked `.env` defines both `VITE_` variables and references the Production ref `lioxtgiputfniqbktcsz` exactly once (confirmed by name/count only; values not printed).

**Distinctions that matter:** the *frontend URL under test* = `baseURL` (`localhost:8080`); the *Supabase project the frontend talked to* = whatever `import.meta.env.VITE_SUPABASE_URL` resolved to (**at audit time, Production**); the *authenticated account* = `.env.test` credentials (the Production-backed `ps4` account); the *Edge Functions reached* = the resolved project's.

### 5.2 The finding `[FACT]`

**At the time of the audit (pre-C03A1-L), Playwright drove a Production-backed frontend as a real Production account.** `.env.test` supplied only the runner's credentials and base URL; it did **not** repoint the frontend's Supabase project, and at that time there was **no** mechanism that redirected the tested frontend to a non-Production backend. That finding motivated the two-layer guard below; it is **no longer current** (C03A1-L's local lifecycle now supplies an ephemeral local backend behind a fail-closed Production/remote guard).

### 5.3 Why one guard is not enough `[FACT]`/`[REC]`

- Node/CI `process.env` proves the **intended** configuration, but Node **cannot read the browser's `import.meta.env`** — those values are inlined into the Vite bundle at dev-server start and live only in the browser runtime. A `process.env` check therefore cannot prove which backend the loaded frontend **actually** talks to.
- Conversely, the browser can observe the backend it actually uses, but a browser-only check runs too late to stop a misconfigured server from starting.

Both layers are required: Layer 1 prevents a wrong server from ever starting; Layer 2 proves the running frontend actually points where intended, **before** any credential is entered or any mutation occurs.

### 5.4 Layer 1 — pre-server Node/CI guard `[FACT — merged in C03A1-L]`

A CI/Node preflight step, before `vite` starts, that:

1. **Requires the complete backend target configuration** (`VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`, `TEST_USER_EMAIL`, `TEST_USER_PASSWORD`) and **materializes the `VITE_` values deterministically into the file Vite reads** (job-scoped `.env.local`/`.env`) rather than assuming `process.env` inlining.
2. **Rejects blank/missing values** — fail the job; never fall through to defaults, `.env.local` on a developer machine, or Production.
3. **Parses the configured Supabase URL** and:
   - **rejects the Production ref `lioxtgiputfniqbktcsz`** outright;
   - **Model A:** requires a **local** Supabase host (e.g. `127.0.0.1`/`localhost` on the local API port) and **rejects any remote Supabase host**;
   - **Model B:** requires the **exact approved staging ref/host** and rejects anything else (including Production).
4. **Never prints key/password values** (mask; assert on host/ref substrings only).

### 5.5 Layer 2 — browser-observed runtime guard `[FACT — merged in C03A1-L]`

Inside Playwright, **before** entering credentials or mutating anything (i.e. at the very start of `global-setup.ts`, after the first navigation), verify the backend the loaded frontend actually uses, via **one** implementation-ready mechanism:

- **(Recommended) Network observation:** load the app, capture the host of the frontend's **first Supabase request** (e.g. a `**/rest/v1/**` or `**/auth/v1/**` call via `page.waitForRequest`/routing), and assert that host equals the approved expected host/ref and is **not** the Production ref; or
- **Sanitized runtime indicator:** expose a **test-only**, key-free project-ref/host indicator (e.g. a `data-…` attribute or a `window.__E2E_SUPABASE_HOST__` set from `import.meta.env.VITE_SUPABASE_URL`'s host only — **never** the publishable key or any secret) and read it through the browser.

If the observed host is Production or unexpected, throw before authentication so no credential is submitted and no data is touched. This is the guard that closes the gap `process.env` cannot: it confirms the **actually-loaded** bundle's backend.

### 5.6 Guard summary

| | Layer 1 (Node/CI, pre-server) | Layer 2 (browser, pre-auth) |
|---|---|---|
| Runs | before `vite` starts | after app load, before credentials |
| Proves | intended configuration is complete + non-Production | the loaded frontend actually talks to the approved backend |
| Reads | `process.env` / materialized env file | live Supabase request host or sanitized runtime indicator |
| Cannot | see the actually-loaded bundle | prevent a wrong server from starting |

---

## 6. Recommended test-backend resource contract

`[REC]` unless marked `[OWNER]`/`[DECISION]`. **Model A is the recommended default; Model B is the backend for a cloud-first D2 selection or for later cloud parity.**

### 6.1 Model A — local-first (recommended default)

- CI starts an **ephemeral local Supabase stack** (`supabase start`) in the job; the full **tracked migration chain (currently 71 migrations) is replayed** locally; deterministic Auth users + seed data are created locally; Vite and Playwright point **only** at the local stack; the stack is destroyed after the run. `[REC]`
- **No cloud Supabase project, region, plan, cloud account, or cloud reset credential is required. No sensitive cloud secret is needed for the local path.** `[REC]`
- Model A serves: the initial read-only Playwright subset; the deterministic mutating waves (`filter-presets`, `file-import-order`, `mutations`, `notes`, `search-attribution`); **attachments** (local Supabase includes **Storage** and **Auth**); and all of **Phase B**. `[REC]`
- **Do not claim Storage or ordinary Auth alone requires cloud** — local Supabase provides both. Concrete capabilities that genuinely need cloud (Model B) are narrow: **real outbound email delivery** (password-reset/confirmation via Resend/SMTP), **production-parity edge/CDN or signed-URL behavior** if a spec depends on it, and **cloud-only quota/limit behavior**. **No current spec requires any of them.** `import-order` was the one candidate, and since D4 was resolved (§12) it takes its metadata from a deterministic stand-in instead of provider egress, so it needs neither a served metadata Edge Function nor network access to a provider. `[REC]`

### 6.2 Model B — persistent cloud staging (cloud-first, or optional parity)

Chosen either as a deliberate **cloud-first** architecture (D2 option B), or added **later for parity** when a wave has a **demonstrated** requirement Model A cannot cover:

- A **separate** Supabase **cloud** project (distinct ref/URL/keys), name `paperlume-staging`; never a prefix inside Production; never re-link the repo away from Production (`supabase/config.toml`'s `project_id` stays Production; staging commands pass `--project-ref` or use a separate context). `[REC]` `[OWNER]`
- **Dedicated cloud staging accounts** (never the Production `ps4` account). `[FACT]`/`[REC]`
- **Environment-scoped GitHub configuration and credentials** (see §6.3), serialized reset/reseed, and an explicit **plan + region** decision (D1). `[OWNER]` `[DECISION]`

### 6.3 Configuration vs secrets (applies to both models)

Distinguish **public client configuration** (safe to expose to the built client; may still be scoped in an Environment for tidiness) from **actual secrets** (must be protected and never logged):

| Public client configuration | Sensitive secrets / operational config |
|---|---|
| `VITE_SUPABASE_URL` | `TEST_USER_PASSWORD` |
| Supabase **project ref** | Cloud **reset/DB password** (Model B only) |
| `VITE_SUPABASE_PUBLISHABLE_KEY` (anon/publishable — a public client key by design) | Supabase **access token** (Model B only) |
| | Any service-role / service credential |
| | `TEST_USER_EMAIL` — treat as **sensitive operational config** (not cryptographic, but do not broadcast) |

Do **not** describe the publishable client key as a private credential. For **Model A**, the sensitive set reduces to `TEST_USER_PASSWORD` (+ `TEST_USER_EMAIL` handled carefully); there is **no** cloud reset/DB password, access token, or service credential.

### 6.4 CI hardening (both models)

- **Least privilege:** `permissions: contents: read` plus only what a step needs. `[REC]`
- **Fork-PR protection:** any secret-using job triggers on `workflow_dispatch` and/or same-repo `push`/`pull_request` only, and/or is gated behind a protected GitHub **Environment** with required reviewers — **no** fork PR receives secrets. (Model A's local path needs no cloud secret, shrinking this surface to `TEST_USER_*`.) `[REC]`
- **Concurrency:** one run at a time (`concurrency` + `cancel-in-progress`) so count-sensitive specs never overlap. `[REC]`
- **Timeout:** an explicit bounded `timeout-minutes` on every job — originally recommended as ≤ 20, and raised to 30 for `e2e-local` alone under CI-E2E-TIMEOUT-HARDENING-001 once that lane's normal runtime approached the 20-minute cap. The bound is a ceiling for infrastructure variance, never a target runtime. **Artifacts:** Playwright `trace`/`screenshot` with bounded retention (~7 days); **never** upload env files or secrets. `[REC]`
- **Two-layer Production-ref guard** (§5). `[REC]`

### 6.5 What no test backend replicates

No Google Monitoring credentials, no `GOOGLE_*`, no provider-quota path (C29); no Production owner/manager bootstrap (internal roles are seeded with synthetic UUIDs, as the SQL test already does); no real commercial/billing identifiers. `[FACT]`/`[REC]`

---

## 7. Recommended fixture, reset, and cleanup model

### 7.1 Options considered `[REC]`

| Approach | Determinism | Failure tolerance | Fit here |
|---|---|---|---|
| Unique per-run data + guaranteed cleanup | Medium | Low (cleanup can crash) | Status quo; insufficient alone |
| Bounded clean-before + clean-after by marker/prefix | Medium-High | Medium | Good complement (specs already do this) |
| **Reset-to-seed each run (rebuild, not repair)** | **High** | **High** | **Recommended primary** |
| Known static seed baseline (no reset) | Medium | Low (drifts) | Weak alone |
| Separate account per workflow/run | High isolation | Medium | Useful for cross-user + parallelism later |
| Ephemeral Supabase branch per run (cloud) | High | High | Model B only, if branch feature/cost is owner-verified `[DECISION]` |

### 7.2 Recommended model `[REC]`

**Reset-to-seed as the isolation boundary, plus prefix cleanup as defense-in-depth.** Each run: (1) reset to a known seed (migration replay + deterministic seed: fixed papers ≥ specs' needs, ≥ 2 note-less papers, dedicated account(s), a disposable paper reserved for `search-attribution`, storage bucket present); (2) run the suite serially; (3) best-effort prefix/marker cleanup — but correctness comes from step 1, **not** from `afterAll`.

Because correctness is reset-based, a partially-failed run, browser crash, cancelled workflow, retry after partial mutation, and stale prior-run data are all handled by the next reset. Parallel runs are prevented by the concurrency guard. External-metadata variability no longer enters the suite at all: since D4 was resolved (§12), `import-order` takes its metadata from a deterministic stand-in at the `fetch-paper-metadata` boundary rather than from a live provider. Attachment objects, Auth users, duplicate imports, date-sensitive records, and quota counters are all restored by reseed.

- **Model A:** the reset is inherent — the ephemeral stack is created fresh (migration replay + seed) and **destroyed after the run**. No cloud reset credential. `[REC]`
- **Model B:** reset is a serialized `db reset`/targeted-truncate + reseed against the cloud project, requiring a reset credential (§6.3) and strict serialization. `[REC]`

### 7.3 D2 — the real architecture decision `[DECISION]`

Choose one: **local-first** (Model A only, recommended); **cloud-first** (Model B from the start); or **local-first with optional later cloud parity** (Model A now, add Model B only when a wave demonstrably needs it). The roadmap (§10) **must not force cloud provisioning when local-first is selected.**

**`[DECISION — RESOLVED 2026-07-31]` D2 = local-first.** The owner selected **local-first** (Model A). The required Phase A path is **`C03A1-L → C03A2-L`**; the cloud phases **`C03A1-C → C03A2-C`** are **not** selected and begin only if a later owner decision identifies a concrete cloud-parity need. **D1** (cloud plan/region) does **not** block the local path.

**Grant-parity prerequisite uncovered by C03A1-L — resolved (merged + applied 2026-07-31).** Implementing C03A1-L surfaced a tracked defect that had to land **before** the local six-spec run could pass: a fresh local `supabase db reset` does not reproduce the Data API table GRANTs the app needs (the tracked chain relied on Supabase's old auto-grant default, now moving to **opt-in Data API exposure**; Production still carries the broad legacy grants), so `authenticated` received `42501` and the app could not run on a clean replay. This was fixed by the **separate bounded migration PR `PFA-C03A1-L-GRANT-PARITY-001`** (one least-privilege `20260731162729_reconcile_data_api_grants.sql`; local-replay-proven), now **merged** to `main` (merge commit `1f00800e5c2f1e1c2d405aa0061ecc38c8738004`) and **applied to Production** via the tracked linked CLI — the ledger is aligned at **69 (Local = Remote), latest `20260731162729`**, and the Production application was an **effective no-op** (existing ACLs already broader; no privilege revoked, no `anon`/`PUBLIC` grant added). **The grant-parity prerequisite remains complete, and C03A1-L has now been implemented, merged, and independently accepted** (`PFA-C03A1-L-LOCAL-STACK-FOUNDATION-MERGE-001`). PR #173 merged to `main` via a **regular two-parent merge commit `299b19a08da0a61984bf2333d12406cbfd62aef7`** (parents: base `bf78406bba9f57c6e5dbe5b6b09a27a6778df9a5`, head `e2374b108cb56712b573dc4820a4e9a2e50c1037`; merge tree identical to the approved-head tree `c9f0993…`), with a four-commit / eleven-file scope (`test: add local Supabase E2E foundation` → `docs: record PFA-C03 local-first implementation` → `fix: harden local E2E safety lifecycle` → `fix: suppress local Supabase credential output`). Merged-main **`Validate` run `30697869074`** (event `push`, branch `main`, head `299b19a…`) succeeded, and the automatic Vercel **Production** deployment **`dpl_9zhjYVmjLKTKqrt7PW6haZej9rLf`** is **READY** on `app.paperlume.app`. Re-verified on the **accepted merge tree**: the local lifecycle (`npm run test:e2e:local`) starts an **ephemeral local Supabase stack**, **replays all 69 tracked migrations**, seeds two local-only confirmed Auth users plus a deterministic fixture (120 primary + 5 secondary papers, ≥ 2 note-less newest rows, a disposable highest-`insert_order` paper), and proves RLS cross-user isolation with authenticated (non-service-role) clients. The **two-layer fail-closed guard is merged** (Layer 1 pre-server in `playwright.config.ts` via Vite `loadEnv`; Layer 2 browser-observed in `e2e/global-setup.ts` **before any credential read or fill**), its pure logic is unit-tested (**32 guard tests**), and the **guard negative controls pass** (`npm run test:e2e:local:verify-guards`: the Layer 1 Production-target control fails closed before Vite starts with no secret leaked; the Layer 2 ordering check confirms the guard precedes both credential reads, the fill, and sign-in). The **six selected read-only specs pass on the merge tree** (`auth`, `bulk-actions`, `eager-load`, `filters`, `paper-import`, `pools` — **32 tests green**) and a **second reset/reseed cycle is deterministic** (fresh ephemeral stack, all 69 migrations replayed again, identical fixture counts and ownership-relative assertions, bounded `auth.spec` — **6 tests green**, clean teardown). **Lifecycle safety is hardened:** teardown failures are authoritative (a nonzero `supabase stop` fails the command; a lifecycle+teardown double failure surfaces both via `AggregateError`); `SIGINT`/`SIGTERM` trigger one-time cleanup and exit **130**/**143**; malformed backend values are fully redacted (never echoed); and the raw key-bearing `supabase start` banner is **captured and discarded** — never inherited, logged, written, or thrown. The **complete Vitest suite contained 694 passing tests at the merge acceptance point** (point-in-time evidence, not an evergreen invariant). Accepted credential-output scans over both lifecycle runs produced **zero credential-marker matches** across all categories (values never displayed). **No Production or cloud Supabase access occurred** (only local `supabase start` / `db reset --local` / `status` / `stop`; no `--linked` command, no remote query, no Edge Function deploy/invoke) and **no GitHub workflow was created**; *at that C03A1-L acceptance point* Production stood at **69 migrations** (latest `20260731162729`) with Edge Functions unchanged (it has since advanced to **70**, latest `20260802025704`, via the §9.6 remediation). **C03A1-L is merged and independently accepted; C03A2-L is merged and accepted (PR #175, merge `1d091e4…` — see §8.1); Phase A is complete for the selected local-first path.** D3 is resolved (2026-08-02, pgTAP); D4 was unresolved and non-blocking at that point, and D4 and D5 were both later resolved on 2026-08-16 — D4 → `DETERMINISTIC_STAND_IN` (§12), D5 → `REQUIRE_DB_TESTS` (§17). (Phase B has since completed as well — the pre-existing DB-security defects found while authoring C03B1 were remediated, merged, and deployed to Production (§9.6); C03B1's implementation was merged and accepted (PR #179, merge `0f786dac…`; §9.7); and C03B2's hosted `db-tests` workflow was merged and accepted (PR #181, merge `b5761418…`; §9.8). **PFA-C03 is complete** — §14.)

---

## 8. Recommended initial non-required Playwright CI contract

`[FACT]`/`[REC]` — this section records the original design contract. **For the selected local-first path the design was implemented and accepted by C03A2-L (see §8.1);** the optional cloud-staging variant (`E2E (staging)`, Model B) remains **future/unselected**. **One coherent backend per run.** The shape below is shown for the **recommended local-first default (Model A, local ephemeral stack)**; under a **cloud-first** D2 selection the equivalent job targets cloud staging (Model B) via `E2E (staging)`. The two are never mixed in one run and neither is ever merged into `Validate`.

- **Workflow name:** `E2E (local)` (Model A) or `E2E (staging)` (Model B) — a distinct workflow per backend. Under cloud-first the job is `E2E (staging)` from the start; under local-first it is `E2E (local)` (and `E2E (staging)` is added only if cloud parity is later chosen). Neither is ever merged into `Validate`. **Job/check name:** `e2e-local` / `e2e-staging`. `[REC]`
- **Triggers:** `workflow_dispatch` (always) + same-repo `push`/`pull_request` only; **no** fork-PR secret exposure. Optional nightly `schedule`. `[REC]`
- **Non-required by design:** not added to `main` branch protection's required checks initially; `Validate / validate` remains the **only** required merge gate and is unchanged. `[REC]`/`[FACT]`
- **Steps (Model A shape):** checkout → setup Node 22 + npm cache → `npm ci` → `npx playwright install --with-deps chromium` → start local Supabase stack → replay migrations → seed → **Layer 1 guard** (materialize `VITE_` for the local host; reject blanks/Production/remote) → run the **initial subset** with **Layer 2 guard** active in `global-setup.ts` → upload artifacts → tear down the stack. `[REC]`
- **Environment/variable names (no values):** Model A needs only public client config for the local stack (`VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY` — the local stack's URL + anon key, typically emitted by `supabase start`), plus `TEST_USER_EMAIL`, `TEST_USER_PASSWORD` (seeded locally), optional `BASE_URL`, and the guard constant naming the Production ref to reject. **No cloud reset credential, access token, or service credential is required for Model A.** Model B additionally needs a staging ref + a reset credential **by name only** (e.g. `STAGING_SUPABASE_DB_PASSWORD` or `STAGING_SUPABASE_ACCESS_TOKEN`). `[REC]`
- **Initial subset (green first):** the 6 **read-only** specs — `auth`, `bulk-actions`, `eager-load`, `filters`, `paper-import`, `pools`. **Wave 2:** `filter-presets`, `file-import-order` (count-sensitive → isolation lane). **Wave 3:** `mutations`, `notes`, `search-attribution` (disposable-paper fixture), `attachments`. **Wave 4 — resolved (D4, 2026-08-16 → `DETERMINISTIC_STAND_IN`, §12):** `import-order`, now a member of the supported default local lane. Its metadata comes from a deterministic stand-in fulfilled at the `fetch-paper-metadata` HTTP boundary, so it needs no live PubMed/Crossref, no provider egress and no served metadata Edge Function. *(It was planned here as a gated external-provider wave; that gate no longer exists.)* `[REC]`
- **Serial/concurrency/timeout:** keep `workers: 1`, serial; one run at a time; `retries: 2` in CI; an explicit bounded `timeout-minutes` (≤ 20 as first recommended; **30** as implemented today — see §6.4); per-test `timeout: 30_000`. `[REC]`
- **Evidence before "stable":** the initial subset passes green ≥ 3 consecutive runs; the Layer 1 + Layer 2 guards are proven to abort on a deliberately-wrong (Production) URL; no secret appears in logs/artifacts; `Validate` untouched. `[REC]`

### 8.1 C03A2-L implementation status `[FACT]`

**C03A2-L is merged and accepted.** PR #175 merged to `main` via a **regular two-parent merge commit `1d091e476461b50375083ac4c1a156e7c38a0b4b`** (parents: base `17d32621a904e0724cbdd4457972bcaedf8383e3`, head `459aac67cba721c89c7969b5bd8561645506dcda`; merge tree identical to the approved-head tree `fd914067…`), with a **two-commit / three-file** scope (`ci: add non-required local Playwright workflow` = `.github/workflows/e2e-local.yml`; `docs: record PFA-C03A2-L implementation` = this file + [start-here.md](start-here.md)). It realizes the §8 local-first (Model A) contract as a single new workflow file `.github/workflows/e2e-local.yml`, which now **exists on `main`**. It records:

- **Workflow name** `E2E (local)`; **job / check name** `e2e-local` (resulting check `E2E (local) / e2e-local`). It is a **separate workflow**, not a job added to `Validate`.
- **Non-required.** `Validate / validate` remains **unchanged and required**; `E2E (local)` is not in `main` branch protection — and it still is not after the D5 activation (§17), which promoted only `db-tests`.
- **Triggers:** `workflow_dispatch`, same-repository `pull_request` → `main`, and `push` → `main`. No `pull_request_target`, `repository_dispatch`, `workflow_call`, schedule, or tag trigger.
- **Fork safety:** a job-level condition runs `e2e-local` only for non-`pull_request` events or when the PR head repository equals the base repository, so **fork-origin PR jobs skip before any dependency install, Docker/Supabase use, browser install, or application execution**.
- **Permissions:** `contents: read` only (no write, `id-token`, `packages`, `checks`, `statuses`, `pull-requests`, or deployment scope).
- **No GitHub secret, variable, or Environment is required** — the ordinary read-only `GITHUB_TOKEN` is the only platform credential. No `secrets.*` / `vars.*` / `environment:` reference exists.
- **Supply-chain pinning:** every `uses:` is pinned to a full 40-char commit SHA with a release-version comment — `actions/checkout` (v7.0.1) and `actions/setup-node` (v7.0.0) reuse the exact SHAs from `Validate`; `supabase/setup-cli` (v3.0.0) pins an **exact** Supabase CLI version (never `latest`); `actions/upload-artifact` (v7.0.1).
- **Reuses the merged C03A1-L lifecycle** rather than duplicating it: the workflow runs `npm run test:e2e:local:verify-guards` (guard verification — 32 guard tests; Layer 1 rejects Production/remote before Vite; Layer 2 precedes credential reads/fills) and `npm run test:e2e:local` (the exact **six read-only specs** `auth`, `bulk-actions`, `eager-load`, `filters`, `paper-import`, `pools`). The lifecycle stays the sole owner of stack startup, migration replay, status parsing, credentials, seed, Playwright env injection, teardown, and signal cleanup; the workflow never constructs `VITE_SUPABASE_*` / `TEST_USER_*` / service or DB credentials.
- **Ephemeral local Supabase only.** No Production, linked, or cloud operation occurs.
- **Cleanup enforced:** an `if: always()` step runs `npm run test:e2e:local:stop` (no `continue-on-error`; a cleanup failure fails the job) followed by an `if: always()` assertion that `e2e/.auth/user.json` is absent and no local stack is running (status output redirected so key-bearing JSON never reaches logs).
- **Failure artifacts** are restricted to `test-results/**/*.png`, uploaded **only on job failure**, `if-no-files-found: ignore`, retention 7 days, artifact name keyed by run ID + attempt. Traces, HAR files, raw logs, Supabase output, `.env*`, `e2e/.auth`, storage-state, and test-result JSON cannot be uploaded.
- **Pre-merge CI acceptance:** three consecutive exact-head `E2E (local)` executions on fresh GitHub-hosted runners passed (each provisioning a fresh ephemeral stack and running guard verification + six-spec lifecycle + cleanup assertions); all three job-log scans contained **zero credential-value matches**; no green-run artifact was produced; and exact-head `Validate` was green.
- **Merged-main CI acceptance:** on the merge commit `1d091e4…`, the push-triggered **`E2E (local)` run `30711943858`** (job `91400822930`, event `push`, branch `main`, checked out exactly `1d091e4…`) **executed (not skipped) and succeeded** in ~268s — Supabase CLI `2.111.0`; **32 guard tests** pass (Layer 1 fails closed, Layer 2 ordering OK); **69 migrations** replayed; deterministic **120 primary + 5 secondary** seed with authenticated cross-user isolation verified; the **six read-only specs** run **32 Playwright tests green**; the `if: always()` teardown and clean-state assertions both succeed (no generated auth state, no running local stack); the failure-screenshot upload step is skipped; the full job-log scan across all credential categories shows **zero credential-value matches**; and the run produced **0 artifacts**. The push-triggered required **`Validate` run `30711943868`** also succeeded on `1d091e4…`, and the automatic Vercel **Production** deployment **`dpl_4fZ4zGs2itrzKamigJF4gcPGGkVv`** is **READY** on `app.paperlume.app` (commit `1d091e4…`). Production Supabase was **unchanged** by that merge (*at that point* 69 migrations, latest `20260731162729`; Edge Functions v10/v15/v3 — the ledger has since advanced to **70** via the §9.6 remediation).
- **Status:** C03A2-L is **merged and accepted**; **Phase A is complete for the selected local-first path**; **Phase B is complete — the pre-existing DB-security defects found while authoring C03B1 were remediated, merged, and deployed to Production (§9.6); C03B1's implementation was merged and accepted (PR #179, merge `0f786dac…`; §9.7); and C03B2's hosted non-required `DB Tests / db-tests` workflow was merged and accepted (PR #181, merge `b5761418…`; §9.8)**; **D3 is resolved (2026-08-02, pgTAP); D4 was resolved on 2026-08-16 → `DETERMINISTIC_STAND_IN` (§12); D5 was resolved on 2026-08-16 → `REQUIRE_DB_TESTS` (§17)**; **PFA-C03 is complete (§14).**

---

## 9. Recommended Phase B database-security-test contract

`[FACT]`/`[REC]` — the Phase B contract. **Phase B is complete.** **C03B1 (the local database-security suites + `test:db:local` runner) is merged and accepted** (PR #179, merge `0f786dac…`; §9.7); **C03B2 (the hosted non-required `DB Tests / db-tests` workflow) is merged and accepted** (PR #181, merge `b57614182fdabff94bc26d128585c7754b7ae45c`; §9.8). The merged suites **reuse and preserve** `supabase/tests/owner_access_and_quota_verification.sql` byte-identical and run it alongside the pgTAP suites. The design subsections §9.1–§9.5 record the **original** contract; §9.6–§9.8 record what was **implemented, merged, and accepted**.

### 9.1 Framework choice `[DECISION]` → recommendation

| Option | Pros | Cons |
|---|---|---|
| Keep framework-free transaction/`ASSERT` scripts | Zero new deps; matches existing 18-case file; runs via `psql`; trivially CI-able | Bespoke assertions; no standard TAP output |
| Adopt **pgTAP** | Standard TAP output, rich assertions | New extension/dependency; rewrite/relearn |
| **Hybrid (recommended)** | Keep the proven file; add new framework-free siblings for uncovered areas; adopt pgTAP later only if TAP reporting is needed | Two idioms briefly coexist |

**Recommendation (historical — superseded by D3):** framework-free hybrid (transaction-wrapped, `ROLLBACK`, `ON_ERROR_STOP=1`), adding new sibling scripts per area. **D3 was resolved on 2026-08-02 to pgTAP, so this earlier hybrid recommendation is *not* the selected framework** — the merged C03B1 suites use **pgTAP** while preserving the framework-free 18-case file byte-identical and running it alongside them (§9.6–§9.7). `[REC]`

### 9.2 Execution model `[REC]`

- **Local ephemeral stack (Model A) — as implemented and merged (§9.7):** `supabase start`, replay all **70** migrations, then run the focused + four comprehensive pgTAP suites via `supabase test db supabase/tests/database --local` and the framework-free 18-case file separately via `psql -v ON_ERROR_STOP=1`. Deterministic, no cloud secret, matches the E2E Model A. Fixture isolation is per-transaction `ROLLBACK`. **C03B2 hosts this existing local DB-test lifecycle unchanged in a separate non-required workflow — merged and accepted (PR #181, merge `b5761418…`; §9.8).**

### 9.3 Required coverage (reuse + additions) `[REC]`

Existing file covers role resolution, direct-table denial on `internal_user_access`, the access-RPC EXECUTE boundary, AI-quota consume/refund/exemption/floor, missing/inactive safety, grant hardening (case 18). **Phase B adds:**

- **Cross-user RLS isolation** on domain tables — `papers`, `projects`, `tags`, junctions, notes-bearing rows, `paper_attachments`, `filter_presets`, `usage_counters`, `user_entitlements`: user A cannot SELECT/UPDATE/DELETE user B's rows.
- **S1 SECURITY DEFINER caller/ownership** — for each `p_user_id`-taking RPC (`consume_ai_quota`, `refund_ai_quota`, `get_ai_quota_status`, and read-path `search_papers`, `search_papers_short`, `filter_papers_by_keywords`, `get_keyword_options`): calling with **another** user's UUID is rejected/scoped to `auth.uid()`. (The existing file proves the 0-arg access RPC + happy-path consume/refund; **caller-mismatch rejection** is the addition.)
- **RPC EXECUTE grants** — broaden the case-18 style assertions to the other SECURITY DEFINER RPCs.
- **AI-quota consume/refund atomicity + concurrency** — concurrent consume near the cap does not exceed it; refund floors at 0.
- **Storage-quota enforcement + usage accounting** — `user_storage_usage` + check-and-consume/refund triggers on `paper_attachments`: insert past cap blocked; usage inc/dec correct; never negative. **Not covered today.**
- **Internal grant hardening + ordinary-user non-escalation** — already covered (cases 4/5/18); keep and reference.

### 9.4 Expected-failure negative-control harness `[REC]`

An intentionally-failing `ASSERT` under `ON_ERROR_STOP=1` **aborts the whole `psql` invocation immediately** — execution does **not** continue to a later ordinary `ROLLBACK`, and the normal green suite cannot be reused to "prove" detection because its own abort would look like a real failure. The negative control therefore uses an **outer expected-failure harness** that treats a nonzero inner exit as success:

1. A **separate** negative-control SQL script (or a generated temporary script), distinct from the normal green suite.
2. The script **starts a transaction**.
3. It **deliberately weakens exactly one bounded invariant** (e.g. drop/loosen one RLS policy, or grant a forbidden privilege) in that transaction.
4. It **invokes the normal detector/assertion** for that invariant.
5. The inner `psql -v ON_ERROR_STOP=1` process is **expected to exit nonzero** (the assertion fires; the transaction aborts).
6. An **outer shell/test harness treats nonzero as success** for this expected-failure step.
7. A **zero exit is a failure** of the negative control — the detector did not catch the regression.
8. The connection **closes / the transaction is rolled back** (the abort already discards the weakening; the harness never issues a `COMMIT`).
9. A **fresh connection** then verifies the weakened policy/grant/function state **did not persist** (baseline intact).
10. The **normal green suite** runs (or re-runs) against the restored baseline and must pass.

The negative-control artifact **never leaves a weakened committed schema or policy** and is never committed in a weakened state. Phase B ships at least one such control (e.g. cross-user RLS on `papers`, plus one quota/storage invariant).

### 9.5 Phase B CI `[REC]`/`[FACT]`

Phase B runs in a **separate, non-required** workflow/check — proposed `db-tests` (a distinct workflow, not a job appended to `Validate`). It **must not** be silently added to the required `Validate` workflow while claiming `Validate` is unchanged. Promotion to required (if ever) is a later owner decision (D5) — since resolved on 2026-08-16 to `REQUIRE_DB_TESTS`, promoting `db-tests` while leaving `Validate` untouched (§17).

### 9.6 C03B1 status — pre-existing security defects remediated, merged, and deployed; C03B1 blocker removed `[FACT]`

- **D3 resolved (2026-08-02): pgTAP.** New C03B1 suites use **pgTAP**; the existing framework-free `supabase/tests/owner_access_and_quota_verification.sql` is **preserved byte-identical** (blob `ce45e4b…`) and executed **alongside** the pgTAP suites (via `psql -v ON_ERROR_STOP=1`), never fed to `pg_prove`. §9.1's earlier "framework-free hybrid" recommendation is superseded by this owner decision.
- **C03B1 was attempted and stopped before implementation.** Authoring the C03B1 security invariants against the then-current, unmodified **69-migration base** proved that the **base schema permitted** the prohibited behaviors listed below, triggering the pre-existing-defect stop rule. **The current 70-migration schema includes migration `20260802025704` and closes those confirmed paths** (Local = Remote); it does **not** still permit them. **At that original stop point, the four-suite architecture and runner had not yet been built; only the focused remediation regression file (`000_preexisting_security_regressions.test.sql`) existed.** After the remediation lifecycle completed, **C03B1 resumed and was later implemented, review-corrected, merged, and accepted via PR #179 (§9.7)** — the four comprehensive pgTAP suites and the `test:db:local` runner now exist on `main`.
- **Confirmed findings (reproduced on the unmodified 69-migration base; controls pass; re-confirmed on a second fresh reset):**
  1. **Unauthenticated cross-user read** through four `SECURITY DEFINER` read RPCs — `search_papers`, `search_papers_short`, `filter_papers_by_keywords`, `get_keyword_options` — each `PUBLIC`/`anon`-executable with an ownership guard missing the explicit `auth.uid() IS NULL` clause (NULL caller slips through three-valued logic).
  2. **Least-privilege EXECUTE not enforced** on those RPCs (PUBLIC + anon).
  3. **Missing bounded `search_path`** on `search_papers`.
  4. **Cross-owner insertion** into `paper_projects` (RLS checked paper ownership only).
  5. **Cross-owner insertion** into `paper_tags` (same).
  6. **Cross-user attachment→paper association** in `paper_attachments` (RLS checked `auth.uid() = user_id` only; the storage-quota trigger trusted `NEW.user_id`).
- **Independent-review expansion incorporated:**
  7. **`safe_bulk_insert_papers` — critical unauthenticated cross-user *write*:** same PUBLIC/anon EXECUTE plus a guard (`IF p_user_id <> auth.uid()`) missing **both** NULL checks, so an anon caller created paper rows for an arbitrary `user_id` (verified: one row created on the base).
  8. **`anon` EXECUTE on `consume_ai_quota`/`refund_ai_quota` (Production) + `service_role` EXECUTE across the surface:** verified read-only against Production, which carries explicit `anon` EXECUTE on `consume_ai_quota`/`refund_ai_quota` **and** explicit `service_role` EXECUTE on (nearly) the entire `SECURITY DEFINER` surface, **including the three trigger-only functions**. `20260731162729_reconcile_data_api_grants` did **not** close these — it grants **table** privileges only and never touches function EXECUTE. (An earlier draft of this doc wrongly credited that migration with closing the quota-RPC `anon` grants; corrected here.) A clean local replay **diverges** from Production: on local, `consume_ai_quota`/`refund_ai_quota` are already `authenticated`-only. The migration therefore REVOKEs `anon` **and `service_role`** and normalizes **both** baselines to the same target — corrective on Production, idempotent where a grant is already absent.
  9. **Broader privileged-function ACL inventory:** a complete `public` `SECURITY DEFINER` inventory (20 functions: 17 directly-callable + 3 trigger-only) was taken and an explicit intended ACL matrix encoded, including explicit `service_role` revocation.
- **Bounded remediation — merged and deployed to Production.** Task `PFA-C03B1-PREEXISTING-SECURITY-DEFECT-REMEDIATION-001`, then correction `PFA-C03B1-SECURITY-REMEDIATION-CORRECTION-001`, then merge+deploy `PFA-C03B1-SECURITY-REMEDIATION-MERGE-DEPLOY-001`, on branch `fix/pfa-c03b1-security-remediation`: one CLI-generated migration `20260802025704_harden_rpc_and_relational_ownership.sql` + one focused pgTAP file `supabase/tests/database/000_preexisting_security_regressions.test.sql` (plus these docs), **three commits** / **four files** total. It adds explicit NULL-auth guards to all affected user-ID RPCs; a bounded `search_path` on `search_papers`; **referenced-object ownership validation inside all four setter RPCs** (see next bullet); least-privilege EXECUTE across the whole `SECURITY DEFINER` surface (directly-callable → `{owner, authenticated}` with **no PUBLIC / anon / `service_role`**; trigger-only → owner-only); both-owner RLS for `paper_projects`/`paper_tags`; and attachment↔paper ownership RLS plus a defense-in-depth ownership check in `check_and_consume_storage_quota`. The focused pgTAP file creates the `pgtap` extension **inside** its `BEGIN … ROLLBACK` (schema `extensions`), so a run leaves the database's extension state unchanged — verified absent → absent on a **fresh connection** after rollback, with no test helper or fixture persisting. Verified across fresh 70-migration replays: focused pgTAP **139/139**; the preserved 18-case legacy file passes (blob unchanged `ce45e4b1301b6885b0e2f9e566d36293b275e8c7`); **eleven** application regression probes pass; standard `Validate` passes with `package-lock.json` byte-identical.
- **Setter RPC ownership — fixed in this migration (previously deferred; corrected after independent review).** The `set_paper_tags` / `set_paper_projects` / `bulk_set_paper_tags` / `bulk_set_paper_projects` setter RPCs previously validated **paper** ownership only (the bulk variants silently filtered to owned papers) and never checked the referenced **project/tag**, so a caller could link an owned paper to another user's project/tag through the `SECURITY DEFINER` path. Direct RLS did not constrain these RPCs. The migration now validates, **before any delete/insert**, that the caller is authenticated (explicit NULL-auth rejection) and that **every** referenced paper **and** project/tag belongs to `auth.uid()`; a single foreign or unknown identifier rejects the whole call **atomically** (no partial delete, no partial insert), and legitimate same-owner assignment and empty-array clearing behavior is preserved. pgTAP proves, per setter, success on own+own, rejection of own+foreign, rejection of foreign+own, NULL-auth rejection, all-or-nothing rejection of mixed arrays (bulk), and assignment-preservation on rejection. Complete relational-ownership remediation now covers **both** direct RLS **and** the setter RPC bodies (**no deferred setter residual**).
- **Remediation lifecycle — complete.** (1) C03B1 authoring discovered the pre-existing defects above; (2) implementation stopped before broader C03B1 work; (3) remediation PR #177 was created; (4) initial independent review requested four corrections; (5) the four correction findings were resolved in one additional commit; (6) the corrected exact head `bf33dcf8d43f5d051f303c079b6ddcdef93f14f0` was independently approved; (7) PR #177 was merged via a **regular exact-head two-parent merge commit** `f4088705a2d3b8869579b253b5d8f7b3162414cf` (base `fd87fdcef0e04b6052f56f29f0f3930541ebf070`, head `bf33dcf…`; merge tree byte-identical to the approved-head tree); (8) merged-main `Validate` run `30742663297` and `E2E (local)` run `30742663283` (job `91482745022`) both succeeded on `f408870`; (9) migration `20260802025704` was applied to Production **exactly once** via the tracked linked CLI; (10) Production verification passed; (11) sanitized aggregate-integrity counts were **0 / 0 / 0** before and after; (12) no historical data reconciliation was required; (13) the remediation lifecycle is complete.
- **Accepted evidence:** PR #177; base `fd87fdcef0e04b6052f56f29f0f3930541ebf070`; approved head `bf33dcf8d43f5d051f303c079b6ddcdef93f14f0`; merge SHA `f4088705a2d3b8869579b253b5d8f7b3162414cf`; migration `20260802025704_harden_rpc_and_relational_ownership`; Production ledger **70** (latest `20260802025704`); `Validate` run `30742663297`; `E2E (local)` run `30742663283` / job `91482745022`; Vercel Production `dpl_GRUaAdNzuJxGExbBNtEq16nVt9Vy` (READY, `app.paperlume.app`, commit `f408870`); pgTAP **139/139**; legacy verification **18/18**; legacy blob `ce45e4b1301b6885b0e2f9e566d36293b275e8c7`; aggregate integrity **0 / 0 / 0**; E2E artifacts **0**.
- **Production security state now active.** Production enforces: explicit authentication guards (NULL-auth rejection on all affected read/write RPCs + bounded `search_path` on `search_papers`); least-privilege function execution (directly-callable → `{owner, authenticated}`, no PUBLIC/anon/`service_role`; trigger-only → owner-only); setter ownership validation; relational both-owner policies on `paper_projects`/`paper_tags`; attachment↔paper ownership on `paper_attachments`; and pre-quota ownership validation in `check_and_consume_storage_quota`. Edge Functions are unchanged (`fetch-paper-metadata` v10, `analyze-paper` v15, `get-gemini-provider-quota` v3).
- **State:** the remediation is **merged and deployed**; **Production carries migration `20260802025704` (ledger 70)** and the security defects above are **closed**. **The C03B1 security blocker was removed; C03B1's implementation was merged and accepted (PR #179, merge `0f786dac…`; §9.7); C03B2's hosted `db-tests` workflow was merged and accepted (PR #181, merge `b5761418…`; §9.8); Phase B is complete; D3 resolved, D4–D5 unresolved and non-blocking at that point (both were resolved on 2026-08-16 — D4 → `DETERMINISTIC_STAND_IN`, §12; D5 → `REQUIRE_DB_TESTS`, §17); PFA-C03 complete; C27–C29 unchanged.**

#### 9.6.1 Separate product defect — `merge_exact_duplicates` (never part of PFA-C03; since repaired) `[FACT]`

Tracked separately from the PFA-C03 security remediation as **`MERGE-EXACT-DUPLICATES-JSONB-REPAIR-001`**, and **now closed**.

**The defect (historical).** `merge_exact_duplicates(UUID, UUID[])` was always authenticated and ownership-guarded (an unowned discard is rejected), but its **successful merge path** called `unnest()` on the `jsonb` columns `keywords`, `mesh_terms`, and `substances`. `unnest(jsonb)` is invalid (`42883`), so every successful duplicate merge failed. The defect **predated PR #177**; PR #177 neither introduced nor worsened it, and migration `20260802025704` did **not** fix it (it only re-scoped the function's EXECUTE ACL). Throughout PFA-C03 it was correctly **not** counted as successful functional regression coverage — the C03B1 pgTAP evidence proved only authenticated reachability and ownership rejection, never a successful merge.

**The repair (current).** It was fixed by a separate bounded, migration-bearing product-fix task, outside PFA-C03:

- **Merged:** PR #184, regular exact-head two-parent merge commit `bd741f5676a21690ec9782bf7ac2e1d8830b53c3`; three files (the migration, the new suite `005`, and a comment-only touch to `003` whose `plan(164)` is unchanged).
- **Deployed:** migration `20260807141402_repair_merge_exact_duplicates_jsonb`, **manually applied to Production by the owner via Supabase CLI on 2026-08-07** — Production is now at **71 migrations**.
- **Mechanism:** `jsonb_array_elements_text` first-occurrence union/dedup replaces the invalid `unnest(jsonb)`; `paper_attachments` are **re-parented onto the keep paper before discards are deleted**, closing a latent `ON DELETE CASCADE` data-loss hazard that fixing the JSONB error alone would have made reachable, with storage accounting unchanged.
- **Security posture unchanged:** one overload, `RETURNS void`, `LANGUAGE plpgsql`, `SECURITY DEFINER`, `search_path = public`, owner `postgres`, EXECUTE for `authenticated` only (ACL fingerprint unchanged across the deployment).
- **Coverage:** the success path is now owned by `supabase/tests/database/005_merge_exact_duplicates_success.test.sql` (**77 assertions**); suite `003` retains the guard-only assertions. Current aggregate: **471/471 pgTAP + 18/18 framework-free**.
- **No live Production duplicate merge was performed** to validate the repair; acceptance rests on the local/CI suites plus read-only Production catalog, ACL, ledger and integrity verification.

---

### 9.7 C03B1 implementation — local database-security suites + runner (merged and accepted) `[FACT]`

The C03B1 local database-security architecture is **merged to `main` and technically accepted**. It was implemented on branch `test/pfa-c03b1-db-security-resumed` from the PR #178 merge commit `4bfe689e587c7a562cb43720678f33fc3c7536b0` (which finalized the §9.6 security-remediation documentation), reviewed through three independent correction rounds, approved at head `b8886b9492a751dcdd054360c0d76761e04112d0`, and merged as **PR #179** via one regular exact-head two-parent merge commit **`0f786dac7088b2afafc16cb76afae8eee5f206d6`** (parent 1 = base `4bfe689e587c7a562cb43720678f33fc3c7536b0`, parent 2 = approved head `b8886b9492a751dcdd054360c0d76761e04112d0`; merge tree `c3f47a8bb927c5ac98e65ed8fb9e465bd99161db` byte-identical to the approved-head tree). Scope: **eight files, five feature commits** (`test: add C03B1 database security suites` → `docs: record C03B1 implementation` → `test: harden C03B1 detector guarantees` → `test: close remaining C03B1 fail-closed gaps` → `test: validate teardown inspection output`; the merge commit is not a sixth feature commit); **no migration, no workflow, no dependency, no lockfile change; PR #179 performed no Supabase mutation**. D3 remains resolved to **pgTAP**. The four comprehensive pgTAP suites, the `npm run test:db:local` runner and its integrated local lifecycle, and all detector controls below now **exist on `main`**; the separate **C03B2 hosted `db-tests` workflow was outside PR #179's scope and was not created by this work** — it landed later, under PR #181 (§9.8).

**Independent review found, and this correction resolved, six items** (one additional commit, `test: harden C03B1 detector guarantees`, touching only `scripts/e2e-local.mjs`, `003_rpc_caller_scope_and_grants.test.sql`, and these two docs — the eight-file PR scope is unchanged): (1) a **missing expected-failure database negative control** — added; (2) a **vacuous `get_duplicate_papers()` isolation fixture** (distinct PMIDs) — replaced with a shared cross-user PMID; (3) **missing direct-owner EXECUTE assertions** — added for all 17 directly-callable RPCs; (4) **incomplete catalog-residue verification** (row counts only) — added a full catalog fingerprint; (5) **unbounded/unvalidated concurrency child processes** — made bounded and fail-closed; (6) **globally-scoped teardown detection** — scoped to this project's ref. The accepted-in-principle architecture (four-suite pgTAP design, immutable 139-suite and 18-case file, safe lifecycle, mandatory teardown, advisory-lock concurrency design, package command + CLI path isolation, no migration/dependency/workflow/Production mutation) is retained unchanged.

**Preserved immutable files (byte-identical):**
- `supabase/tests/database/000_preexisting_security_regressions.test.sql` — focused **139-assertion** remediation suite (blob `8c46e48d…`).
- `supabase/tests/owner_access_and_quota_verification.sql` — framework-free **18-case** verification (blob `ce45e4b1…`).

**Four new comprehensive pgTAP suites (255 new assertions):**
- `001_core_rls_isolation.test.sql` — **40**: per-user isolation on `papers`, `projects`, `tags`, `filter_presets`, `user_entitlements`, `usage_counters` (positive control + cross-user SELECT/UPDATE/DELETE ineffectiveness, forged-owner INSERT `42501`, anon/null-auth denial, server-only counters).
- `002_relational_rls_isolation.test.sql` — **26**: both-owner integrity + isolation on `paper_projects`, `paper_tags`, `paper_attachments` (full own/own · own/foreign · foreign/own · foreign/foreign matrix; cross-owner invisibility/undeletability; attachment ownership trigger `P0001` before accounting).
- `003_rpc_caller_scope_and_grants.test.sql` — **164**: exactly 20 SECURITY DEFINER functions (17 callable + 3 trigger-only) with no unexpected function/overload; complete least-privilege EXECUTE matrix — for every one of the 17 direct RPCs **owner true, authenticated true, PUBLIC/anon/`service_role` false** (the 17 direct-owner assertions are the correction), trigger-only owner-only and not client-/`service_role`-executable; null-auth/mismatch/valid caller identity for the four read RPCs, `safe_bulk_insert_papers`, and the three AI-quota RPCs; representative setter/bulk-update/dedup/access boundaries; **`get_duplicate_papers` caller-isolation under a shared cross-user PMID** — callers A and B (both owning a paper with PMID `PM_SHARED_C03B1`) and a missing-claims caller each receive `[]`, so a globally-grouping implementation would be caught; `search_papers` bounded `search_path`; one-overload-each.
- `004_storage_and_quota.test.sql` — **25**: exact storage increment/decrement, refund floor, over-cap rejection (no row, unchanged usage), negative-size rejection, missing/inactive-entitlement contract, cross-owner rejection before accounting, no direct client rewrite, no anon access; sequential AI-quota below-cap/at-cap/never-exceed/refund/refund-at-zero/missing/inactive/exempt semantics.

**Second independent review found, and this correction resolved, three residual proof-quality gaps** (one additional commit, `test: close remaining C03B1 fail-closed gaps`, touching only `scripts/e2e-local.mjs` and these two docs — eight-file scope unchanged; all assertion counts unchanged): (1) **concurrency worker deadlines began sequentially** (each `await`ed in turn, so a later worker gained the earlier ones' elapsed time) — now all three completion deadlines start at barrier release; (2) **post-teardown Docker inspection could fail open** (an empty catch swallowed a failed `docker ps`) — now authoritative and fail-closed; (3) **catalog fingerprinting was narrower than the no-schema-residue claim** — now covers complete function/trigger definitions, indexes, columns/defaults/constraints, policy mode, and relation ownership/ACL, with a sensitivity probe proving it detects same-name definition changes.

**Local runner** — `npm run test:db:local` (`node scripts/e2e-local.mjs db-tests`; installed Supabase CLI 2.111.0 syntax `supabase test db supabase/tests/database --local`): verifies repo root, Docker + CLI + `test db` flag/path support; starts the local stack (raw key-bearing startup output captured and discarded); resets and replays all **70** migrations; validates a loopback API URL with no Production ref; resolves the local `supabase_db_…` container; captures a pre-test pgTAP + **expanded catalog-fingerprint** baseline; runs the **catalog-fingerprint sensitivity probe** and proves it rolled back (below); runs the **expected-failure database negative control** and proves it rolled back (below); runs every pgTAP file under `supabase/tests/database` (the framework-free file is **never** passed to `pg_prove`); runs the 18-case file via `psql` (`ON_ERROR_STOP=1`); runs the true-concurrency probe; proves **no row residue**, **pgTAP state unchanged (absent → absent)**, and **catalog fingerprint unchanged**; then **always** stops the stack and deletes its volumes (the `E2E_KEEP_LOCAL_STACK` escape hatch is deliberately ignored for `db-tests`; SIGINT/SIGTERM trigger one-time mandatory cleanup; a failed teardown fails the command). The post-teardown "no stack remains" check is **authoritative and fail-closed** and **scoped to this project's ref** (`supabase_*_<ref>`): project-ref resolution failure, a `docker ps` spawn failure, a nonzero `docker ps` exit, malformed output, or a residual current-project container each fail the lifecycle (no empty catch), while an unrelated Supabase project that happens to be running is not treated as residue and is never stopped. **Final independent re-review accepted the simultaneous process deadlines, the expanded catalog fingerprint + sensitivity probe, and the nonzero inspection-failure handling, and found one residual gap: the inspection output was only type-checked, not syntactically validated.** This correction adds a **strict Docker-name parser** (`test: validate teardown inspection output`, touching only `scripts/e2e-local.mjs` and these two docs; eight-file scope and all assertion counts unchanged): `docker ps` output is parsed as strict container-name records — empty output stays valid and a single trailing newline is allowed, but names must match `^[A-Za-z0-9][A-Za-z0-9_.-]*$`, so spaces, tabs, carriage returns, NUL/control characters, embedded blank records, and duplicates are rejected and are **never trimmed into validity** (the previous `split/trim/filter` could launder malformed output). Current-project classification runs only **after** successful parsing; the malformed raw output is never logged. A second local-only test hook (`E2E_DBTESTS_FORCE_INSPECT_GARBAGE=1`) injects one fixed malformed record **after** a normal zero-exit `docker ps` so the parser's rejection path is proven (fixed marker "post-teardown Docker output rejected as malformed"; runner exits nonzero); the existing nonzero-command hook (`E2E_DBTESTS_FORCE_INSPECT_FAIL=1`) remains green as an expected failure.

**Expected-failure database negative control** — in one transaction on a fresh connection, the runner creates local-only users A/B and a paper owned by A, transactionally **disables RLS on `papers`** (the injected regression), becomes authenticated caller B, then runs the same detector the green suite runs ("caller B sees zero of A's rows"). Because RLS is disabled, B observes A's row, so the detector **RAISES with a fixed marker (`C03B1_NEGATIVE_CONTROL_LEAK_DETECTED`) and psql exits nonzero**; the transaction is never committed. The outer harness treats the control as successful **only** when the inner process exits nonzero **and** the exact marker is present; an inner exit of zero (isolation appeared to hold despite the injected leak) fails the C03B1 runner. A fresh connection then proves the catalog fingerprint equals the pre-control baseline (papers RLS and every policy/function restored) and no fixture row remains. The generated SQL is never printed.

**Catalog-residue proof (expanded)** — before the sensitivity probe and again after all tests + concurrency cleanup, the runner captures a deterministic fingerprint (per-category md5 over stably-ordered rows) of eight categories: **functions** (schema, name, identity args, **complete `pg_get_functiondef`** — so a same-signature `CREATE OR REPLACE` body change is caught — result type, language, owner, kind, `SECURITY DEFINER`, volatility, strictness, leakproofness, parallel-safety, `proconfig`, ACL); **policies** (schema, table, name, command, **permissive/restrictive mode**, roles, USING, WITH CHECK); **RLS flags** (`relrowsecurity`, `relforcerowsecurity`); **triggers** (schema, table, name, **complete `pg_get_triggerdef`**, enabled state); **relations** in public+extensions incl. indexes (schema, name, kind, owner, ACL, persistence, partition status); **columns** in ordinal order (type, nullability, identity/generated, collation, default expression); **constraints** via `pg_get_constraintdef`; and **indexes** via `pg_get_indexdef`. Temp relations, internal schemas, unstable OIDs/relfilenodes, row counts, and statistics are excluded. Final equality on a fresh connection proves **no persistent function, policy, trigger, relation, RLS-flag, extension, helper, fixture, or advisory lock changed**, alongside the retained zero-row and zero-advisory-lock checks.

**Catalog-fingerprint sensitivity probe** — before the papers-RLS negative control, one transaction on a fresh connection creates deterministically-named probe objects, captures fingerprint A, then under the **same primary names** replaces the function body (1→2), recreates the trigger (BEFORE INSERT → AFTER UPDATE), and recreates the index ((val) → (val,id)), captures fingerprint B, and rolls back. The probe passes only if A≠B **and** each mutated category (`func`, `trg`, `idx`) individually changed — proving the fingerprint would catch a same-name definition change. Nothing is committed; a fresh connection then confirms the real baseline equals the pre-probe baseline. Markers: "catalog fingerprint detected same-name definition changes" and "catalog fingerprint sensitivity rollback verified"; the generated SQL is never printed.

**True-concurrency AI-quota probe (bounded + fail-closed, simultaneous deadlines)** — a committed fixture user sits one unit below a cap of 2; a coordinator session holds an EXCLUSIVE advisory lock while two worker sessions block requesting the SHARED lock; once both are confirmed waiting on `pg_locks`, the coordinator releases so both call `consume_ai_quota` concurrently. Immediately after release, **all three bounded completion deadlines start at once** (coordinator exit 15 s / worker exit 30 s each) and are awaited together, so no worker gains extra time because another process was awaited first; each records its sanitized elapsed ms from release. Every child **must exit 0 without signal termination** — output from a nonzero or killed process is never accepted; the coordinator must additionally report a successful acquire and unlock. Required result: **exactly one `ok` + one `quota_exceeded`; final usage = cap; exactly one unit consumed; one counter row (no duplicate)**. After all three sessions terminate, a fresh connection proves **zero barrier locks and zero ungranted waiters** before the fixture is removed; fixture absence (user, entitlement, counter, storage, access, advisory lock) is then re-proven on another fresh connection. On any timeout/error the remaining processes are killed within a bounded window and the error is re-thrown so the mandatory teardown still runs.

**Evidence (local):** two complete `npm run test:db:local` runs both passed — the catalog sensitivity probe detected the same-signature function-body, same-name trigger, and same-name index changes and rolled back (real baseline restored); the negative control detected the injected papers-RLS regression (nonzero + marker) and its rollback + baseline restoration were verified; pgTAP **394/394** each (139 + 40 + 26 + 164 + 25), legacy **18/18**; all three concurrency deadlines started at barrier release and the sanitized elapsed values were well within bounds (e.g. coordinator ≈ 13–15 ms ≤ 15000, workers ≈ 15–20 ms ≤ 30000), one allowed + one quota_exceeded, usage = cap, one counter row, no barrier locks; the expanded catalog fingerprint was identical before and after (and identical across both runs); row/advisory residue zero; mandatory current-project teardown followed by an authoritative Docker inspection. Two dedicated **inspection negative controls** confirmed the post-teardown check is fail-closed and, in both, real teardown still ran and no current-project container remained: `E2E_DBTESTS_FORCE_INSPECT_FAIL=1` (injected nonzero `docker ps`, exit 125 → runner exit 1) and `E2E_DBTESTS_FORCE_INSPECT_GARBAGE=1` (real `docker ps` exits 0, then a fixed malformed record is injected → the strict parser rejects it with "post-teardown Docker output rejected as malformed" → runner exit 1); the malformed raw record is never logged, and the genuine `docker ps` output is accepted by the strict parser on every ordinary run. The `E2E_KEEP_LOCAL_STACK=1` escape hatch was verified unable to bypass db-tests cleanup. The existing E2E lifecycle still passes (`verify-guards`: **32** guard tests, Layer 1 fail-closed, Layer 2 ordering; `test:e2e:local`: 70 migrations, 120/5 seed + isolation, six specs / **32** Playwright tests, clean teardown; `stop`). Credential scans of every retained log are **zero**. Standard validation: `npm ci` lockfile byte-identical, lint 0 errors, typecheck 0, Vitest all pass, build succeeds.

**Evidence (merged-main).** After the PR #179 merge, the push-triggered required **`Validate` run `31148180841`** (job `92772038440`, event `push`, branch `main`, head `0f786dac…`) succeeded, and the push-triggered **`E2E (local)` run `31148180874`** (job `92772038757`, event `push`, branch `main`) **executed and succeeded**, checking out exactly `0f786dac…`: **32 guard tests** (Layer 1 fail-closed, Layer 2 ordering), **70 migrations** replayed, deterministic 120/5 seed + authenticated cross-user isolation, the **six specs → 32 Playwright tests** green, ordinary + `if: always()` teardown and clean-state assertions all pass (no generated auth state, no local stack), failure-screenshot upload skipped, **0 artifacts**, and a full job-log scan with **zero credential-value matches**. The automatic Vercel **Production** deployment **`dpl_DD773Mz8cVmWUoDqoYrkunQ1XysG`** is **READY** on `app.paperlume.app` (commit `0f786dac…`). Production Supabase is **unchanged** (70 migrations, latest `20260802025704`; Edge Functions v10/v15/v3; integrity 0/0/0). The `E2E (local)` workflow **does not run the pgTAP DB-security lifecycle** (`test:db:local`); that hosted execution belongs to the separate **`DB Tests`** workflow, which at the C03B1 acceptance point had not yet been built — so its absence there was expected and was never a missing C03B1 acceptance condition. `DB Tests` has since been delivered by C03B2 (§9.8).

**State:** C03B1 implementation is **merged to `main` and accepted** (PR #179, merge `0f786dac…`); **C03B2** (the hosted `db-tests` workflow) was separately authorized and is now **merged to `main` and accepted** (PR #181, merge `b5761418…`; §9.8), so `DB Tests / db-tests` existed on `main` as a **non-required** check *at that point* (it became **required** on 2026-08-16 under D5 — §17); **Phase B is complete**; **D4 was unresolved and non-blocking at that point** (resolved 2026-08-16 → `DETERMINISTIC_STAND_IN`, §12); **PFA-C03 is complete**; **C27–C29 unchanged**. The separate `merge_exact_duplicates` defect (§9.6.1), still open at that point, has since been repaired and deployed (PR #184; migration `20260807141402`).

---

### 9.8 C03B2 — hosted non-required `DB Tests / db-tests` workflow (merged to `main` and accepted) `[FACT]`

**C03B2 is merged and accepted.** It was implemented on branch `ci/pfa-c03b2-hosted-db-tests`, cut from `main` at exactly **`50ca693239c42cbdaeda62e22c2fbb1c0955508d`** (the PR #180 documentation-finalization merge, the terminal merge of the complete C03B1 lifecycle), opened as **PR #181** against `main`, approved at head **`e33768321b9ecbb857984e3c61c3bb77a8086ab3`**, and merged via one regular exact-head two-parent merge commit **`b57614182fdabff94bc26d128585c7754b7ae45c`** (parent 1 = base `50ca693239c42cbdaeda62e22c2fbb1c0955508d`, parent 2 = approved head `e33768321b9ecbb857984e3c61c3bb77a8086ab3`; merge tree **`b2a5feff2dac726d2ad1454b64c9a8969887d586`**, byte-identical to the approved-head tree). Scope is exactly **two commits / three files**: `ci: add non-required database security workflow` (**A** `.github/workflows/db-tests.yml`) → `docs: record C03B2 implementation` (**M** `docs/pfa-c03-staging-and-security-test-plan.md`, **M** `docs/start-here.md`). **`.github/workflows/db-tests.yml` now exists on `main`.**

**C03B2 redesigns nothing.** Its only job is to make the already-merged C03B1 lifecycle execute automatically on a fresh GitHub-hosted runner. It changes **no** runner, test, package, lockfile, config, migration, or application file: `scripts/e2e-local.mjs`, `package.json`, `package-lock.json`, `supabase/tests/database/000`–`004`, `supabase/tests/owner_access_and_quota_verification.sql`, `supabase/config.toml`, `supabase/migrations/`, `src/`, `playwright.config.ts`, `vercel.json`, `.github/workflows/validate.yml`, and `.github/workflows/e2e-local.yml` are all **untouched**. No dependency change; no migration.

**Workflow identity and gate state.** Workflow name **`DB Tests`**; job ID and displayed job name **`db-tests`**; resulting check **`DB Tests / db-tests`**. It is **separate from `Validate`** and **separate from `E2E (local)`**, and **C03B2 shipped it as non-required**: branch protection was **unchanged by this work** (required contexts remained exactly `["validate"]`, strict, admins enforced, force-push and deletion disabled), so at that point **`Validate / validate` was the sole required merge gate** and both `DB Tests / db-tests` and `E2E (local) / e2e-local` were non-required. **D5 (which non-required checks, if any, should become required) was left unresolved** — this work deliberately did not resolve it. **D5 was resolved separately on 2026-08-16 → `REQUIRE_DB_TESTS`, and `db-tests` is now required (§17); `e2e-local` remains non-required.**

**Triggers and fork safety.** `workflow_dispatch` + `pull_request` to `main` + `push` to `main` — the same safe trigger model as the accepted `E2E (local)` workflow. **`pull_request_target`, `repository_dispatch`, `workflow_call`, `schedule`, and tag triggers are not used.** The job carries the accepted same-repository restriction (`github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository`), so a **fork-origin pull request skips the job entirely — before `npm ci`, before any Docker access, before any Supabase CLI use, before local stack startup, and before any application or database execution.**

**Permissions and secret surface.** `permissions: contents: read` only — no `id-token`, `packages`, `checks`, `statuses`, `pull-requests`, or `deployments` write. The workflow references **no `secrets.*`, no `vars.*`, and no `environment:`**, and needs none: it runs exclusively against an **ephemeral local Supabase stack**, so no cloud Supabase credential exists or is required. It never touches Production or any cloud/linked project.

**Concurrency, runner, timeout.** Concurrency group `db-tests-${{ github.event.pull_request.number || github.ref }}` with `cancel-in-progress: true` — **distinct from both the `Validate` group and the `e2e-local-*` group**, so it can never cancel or serialise the required gate. `runs-on: ubuntu-24.04`; bounded `timeout-minutes: 20`.

**Supply-chain pinning.** Every action SHA is reused verbatim from the accepted `e2e-local.yml`: `actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1` (v7.0.1), `actions/setup-node@820762786026740c76f36085b0efc47a31fe5020` (v7.0.0) with `node-version: 22.x` + `cache: npm` + `cache-dependency-path: package-lock.json`, and `supabase/setup-cli@46f7f98c7f948ad727d22c1e67fab04c223a0520` (v3.0.0) with the exact `version: 2.111.0` — **never `latest`**. No new third-party action was added.

**Steps — a thin wrapper only.** Checkout → set up Node → set up Supabase CLI → `npm ci` → report safe tool versions (`node`, `npm`, `docker version --format '{{.Server.Version}}'`, `supabase --version`, `psql --version` only — never `supabase status -o json`, environment dumps, Docker environment, database URLs, keys, passwords, JWTs, connection strings, or generated SQL) → **`npm run test:db:local`** → `if: always()` defense-in-depth `npm run test:e2e:local:stop` (exit code **not** suppressed; a cleanup failure fails the job) → `if: always()` clean-state assertion that fails the job if a local stack is still running, with `supabase status` output redirected to `/dev/null` so key-bearing JSON can never reach the log. **No SQL and no lifecycle logic is duplicated into YAML**: stack startup, the 70-migration replay, the catalog-fingerprint sensitivity probe, the expected-failure papers-RLS negative control, pgTAP execution, the framework-free 18-case verification, the true-concurrency probe, the residue proof, and the mandatory project-scoped teardown inspection all remain owned by `scripts/e2e-local.mjs`.

**Artifacts.** There is **no artifact-upload step**. Expected and verified hosted artifact count for `DB Tests` is **0** on success and on failure alike — no raw logs, SQL, Supabase output, database dumps, `.env*`, credentials, Docker state, temporary scripts, or test-generated catalog data are ever uploaded.

**Pre-merge hosted evidence (first automatic hosted execution, PR #181 — run `31160897766`, job `92810745219`, event `pull_request`, head `d07abbe811732753bb7a97139e026a9122572148`).** `DB Tests / db-tests` **ran automatically on a fresh GitHub-hosted `ubuntu-24.04` runner** and **succeeded in ~4 minutes** (08:14:41Z → 08:18:46Z, well inside the 20-minute budget), with every step green (Checkout → Set up Node → Set up Supabase CLI → Install dependencies → Report tool versions → Run local database security lifecycle → Enforce local stack teardown → Assert clean local state). Reported hosted tool versions were Node `v22.23.2`, npm `10.9.8`, Docker server `28.0.4`, Supabase CLI `2.111.0` (the pinned version), and `psql (PostgreSQL) 16.14` — **`psql` is present on the hosted image, so no runner change was needed**. It exercised the unmodified C03B1 lifecycle: **70 tracked migrations** replayed (latest `20260802025704_harden_rpc_and_relational_ownership`); the **catalog-fingerprint sensitivity probe** detected and rolled back the same-signature function-body, same-name trigger-definition, and same-name index-definition changes; the **expected-failure papers-RLS negative control** transactionally weakened the invariant, was **detected** (fixed marker + nonzero inner exit), exited through the expected-failure path, rolled back, and proved restoration on a **fresh connection**; **pgTAP 394/394** (`000` = 139, `001` = 40, `002` = 26, `003` = 164, `004` = 25); the preserved framework-free verification **18/18**; the **true concurrency probe** proved coordinator and both worker deadlines starting at barrier release (sanitized elapsed: coordinator 8 ms ≤ 15000, worker1 24 ms ≤ 30000, worker2 18 ms ≤ 30000), all children exiting cleanly, exactly one consume succeeding and exactly one `quota_exceeded`, final usage equal to the cap (usage = 2, cap 2), exactly one counter row, and zero advisory locks/waiters remaining; the **residue proof** showed zero row residue, an unchanged catalog fingerprint, unchanged pgTAP extension state, no persistent helper/test object, and no barrier lock residue; **mandatory teardown** ran with project-scoped Docker inspection and strict output parsing leaving no current-project container; the `if: always()` defense-in-depth cleanup and clean-state assertion both succeeded; **0 artifacts** (verified via the Actions artifacts API); and a full job-log scan found **zero credential-value leaks** across every category (`sb_secret_`, `sb_publishable_`, service-role/secret key values, JWT-like triplets, PostgreSQL URLs carrying passwords, database passwords, generated local keys, S3 credentials, raw `supabase status` output) — the only `***` occurrences are GitHub's own masking of the read-only checkout token, which is not a leak. pgTAP state was proven **absent → absent** and the catalog fingerprint identical before and after. **No detector was weakened, skipped, or modified to reach this result, and no hosted-runner incompatibility was found** — the accepted runner ran unchanged on GitHub-hosted Ubuntu. Each subsequent PR head received its own automatic run of the same workflow, and the final approved head `e337683…` was green on all three checks (`Validate` run `31161479279` / job `92812573642`; `E2E (local)` run `31161479276` / job `92812573568`; `DB Tests` run `31161479331` / job `92812573687`; **0 artifacts** each). Those `pull_request` jobs checked out GitHub's synthetic integration commit `8a59908ba187de6d898fdd5d746262951bb6929e` (ordered parents `50ca693…`, `e337683…`), not the raw branch head.

**Merged-main acceptance evidence (the decisive C03B2 proof).** After the PR #181 merge, all three push-triggered workflows ran automatically on the **actual merge commit** `b57614182fdabff94bc26d128585c7754b7ae45c` and **succeeded**: required **`Validate` run `31162869710`** (job `92816960346`), **`E2E (local)` run `31162869601`** (job `92816959893`), and — the workflow this subphase delivers — **`DB Tests / db-tests` run `31162869485`** (job `92816960014`). The `DB Tests` push job fetched and checked out `+b57614182fdabff94bc26d128585c7754b7ae45c` — the **real merge commit**, not a synthetic PR merge SHA. On that merged-main run the unmodified C03B1 lifecycle again passed end to end: **70 migrations** replayed (latest `20260802025704_harden_rpc_and_relational_ownership`); the **catalog-fingerprint sensitivity probe** detected same-name definition changes and its rollback was verified; the **expected-failure papers-RLS negative control** was detected and its rollback verified; **pgTAP 394/394** across all five files (`000` = 139, `001` = 40, `002` = 26, `003` = 164, `004` = 25, `Result: PASS`); the framework-free verification **18/18**; the **true-concurrency probe** passed with all deadlines measured from barrier release (coordinator 12 ms ≤ 15000; worker1 36 ms ≤ 30000; worker2 40 ms ≤ 30000), exactly one allowed consume + one `quota_exceeded`, usage = cap 2, one counter row, no barrier locks; the **residue check** was clean (no test rows or locks, pgTAP state unchanged/absent, catalog fingerprint unchanged); **authoritative project-scoped teardown** confirmed no current-project stack remained; and the `if: always()` cleanup plus clean-state assertion both succeeded. **0 artifacts** for all three merged-main runs, and a full credential scan of the merged-main `DB Tests` log found **zero credential-value leaks** in every category (`sb_secret_`, `sb_publishable_`, service-role/secret key values, JWT-like triplets, PostgreSQL URLs carrying passwords, database passwords, S3 credentials, raw `supabase status` output) — the only `***` occurrences are GitHub's masking of the read-only checkout token. The merged-main `E2E (local)` run likewise passed (32 guard tests; 70 migrations; 120/5 seed + authenticated isolation; six specs / **32 Playwright tests**; teardown + clean-state assertions; screenshot upload skipped; 0 artifacts). The automatic Vercel **Production** deployment **`dpl_5rVkNjnWfXKeXoKYnhDWiGb3xn7T`** is **READY** on `app.paperlume.app` (commit `b5761418…`, target production, GitHub deployment `5791538001`); it was **not** manually deployed. **No detector was weakened, skipped, or modified.**

**Safety.** C03B2 — implementation and merge alike — performed **no Production mutation of any kind**: no migration apply or repair, no DDL, no application-row mutation, no Auth or Storage mutation, no Edge Function deployment or invocation, and no Supabase branch creation. Verified read-only after the merge: Production Supabase remains at **70 migrations** (latest `20260802025704`) with Edge Functions `fetch-paper-metadata` v10, `analyze-paper` v15, `get-gemini-provider-quota` v3 (`ezbr_sha256` unchanged) and sanitized aggregate integrity **0 / 0 / 0**.

**State:** **C03B2 is merged to `main` and accepted** (PR #181, base `50ca693…`, merge `b57614182fdabff94bc26d128585c7754b7ae45c`). `main` therefore **has a hosted database-security check**, `DB Tests / db-tests`, which **C03B2 shipped as non-required** (it became **required** on 2026-08-16 under D5 — §17). **Phase B is complete**; **PFA-C03 is complete** (§14); **D4 was unresolved and non-blocking at that point** (resolved 2026-08-16 → `DETERMINISTIC_STAND_IN`, §12); **C27–C29 unchanged**. The separate `merge_exact_duplicates` defect (§9.6.1), still open at that point, has since been repaired and deployed (PR #184; migration `20260807141402`).

---

## 10. Phased implementation roadmap

`[REC]` — bounded PRs; **not** one epic. Each phase is separately authorized; none is started by this task. **The required Phase A path is chosen by D2 (§7.3); local-first is the recommended default, not a mandate:**

- **D2 = local-first (recommended):** Phase A = `C03A1-L → C03A2-L`. The cloud phases are **not** required (add them later only if separately justified and authorized).
- **D2 = cloud-first:** Phase A = `C03A1-C → C03A2-C`. The local Phase A phases (`C03A1-L` / `C03A2-L`) are **not** prerequisites and are taken only if independently chosen for extra coverage.
- **D2 = local-first + optional cloud parity:** Phase A begins `C03A1-L → C03A2-L`, then adds `C03A1-C → C03A2-C` **only after** a concrete unmet requirement is identified and separately authorized.
- **Phase B is shared under every D2 option:** `C03B1 → C03B2` (local, deterministic), regardless of the Phase A branch — unless a later owner decision changes it.

In the table below each phase is tagged **(shared)**, **(local-first)**, or **(cloud-first / optional-hybrid)** to show where it is required.

| Phase | Purpose | Repo mutations | External mutations | Owner actions | Sensitive secrets (names only) | Acceptance | Prod access | Separate deploy auth? |
|---|---|---|---|---|---|---|---|---|
| **C03A0** (this) *(shared)* | Contract + readiness audit | 2 docs | none | none | none | Contract complete; §14 DoD-ready; `Validate` green | Prohibited | No |
| **C03A1-L** *(local-first / hybrid)* — **merged and accepted** (PR #173, merge `299b19a…`) | Local-stack seed/reset + env + guard contract (repo) | `playwright.config.ts`/`e2e/*` env wiring + guards + seed/reset scripts | none (local only) | none (local path needs no cloud action) | none for the local path (`TEST_USER_*` seeded locally) | **met on the accepted merge tree:** local stack replays all 69 + seeds (120/5); both guard layers reject Production/remote (32 guard tests; Layer 2 before credential reads and fills); six read-only specs green (32); deterministic second reset/reseed (auth 6 green); teardown/signal hardening; startup credential output suppressed (zero-match scans); merged-main `Validate` `30697869074` green | Prohibited | No |
| **C03A2-L** *(local-first / hybrid)* — **merged and accepted** (PR #175, merge `1d091e4…`) | Non-required **local** Playwright CI (`E2E (local)` / `e2e-local`) | new `.github/workflows/e2e-local.yml` (now on `main`) | CI only (ephemeral local stack) | approve running the workflow | none cloud (local anon key + seeded `TEST_USER_*`); no secret/variable/Environment | **met on `main`:** merged-main `E2E (local)` run `30711943858` executed & green (32 guard tests; 69 migrations; 120/5 seed + isolation; six read-only specs, 32 tests); teardown + clean-state assertions pass; fork PRs skip; `contents: read`; all actions SHA-pinned; zero credential-value log matches; 0 artifacts; merged-main `Validate` `30711943868` green; Vercel Production `dpl_4fZ4zGs2itrzKamigJF4gcPGGkVv` READY; `Validate` unchanged & required | Prohibited | No |
| **C03A1-C** *(cloud-first; optional under hybrid)* | Cloud staging provisioning + read-only verification | none (or docs note) | **owner** creates cloud project + accounts + secrets | provision cloud project, dedicated accounts, GitHub Environment + secrets, confirm **plan/region (D1)** | staging ref (public) + reset credential (`STAGING_SUPABASE_DB_PASSWORD`/`STAGING_SUPABASE_ACCESS_TOKEN`), `TEST_USER_PASSWORD` | staging reachable; migrations replay clean; guards reject Production | Prohibited (Production) | Yes (cloud setup owner-authorized) |
| **C03A2-C** *(cloud-first; optional under hybrid)* | Non-required **cloud** Playwright CI (`E2E (staging)`) | new `E2E (staging)` workflow | CI runs against cloud staging | approve secrets/Environment | as C03A1-C | subset green vs staging; secrets fork-protected; `Validate` unchanged | Prohibited (Production) | Uses C03A1-C |
| **C03B1** *(shared)* — **merged and accepted** (PR #179, merge `0f786dac…`; §9.7) | DB-test framework + representative **local** coverage | new `supabase/tests/database/001`–`004` (RLS, relational isolation, RPC caller-scope + grants, storage/AI quota) + `test:db:local` runner | none | none | none | four pgTAP suites (255) + focused 139 → 394/394 on clean local replay; 18-case file still passes; merged-main Validate + E2E green | Prohibited | No |
| **C03B2** *(shared)* — **merged and accepted** (PR #181, merge `b5761418…`; §9.8) | **Separate** non-required `db-tests` CI + expected-failure negative-control | new `.github/workflows/db-tests.yml` (**now on `main`**), ephemeral local stack | CI only | approve running the workflow | none; no secret/variable/Environment | **met on `main`:** merged-main `DB Tests / db-tests` run `31162869485` (job `92816960014`) checked out the actual merge commit and passed on a fresh hosted runner (70 migrations; sensitivity probe; negative control **fails-then-reverts**, restoration proven on a fresh connection; pgTAP **394/394**; legacy **18/18**; true concurrency; residue + teardown clean); `contents: read`; fork PRs skip; all actions SHA-pinned, CLI `2.111.0`; **0 artifacts**; zero credential-value log matches; merged-main `Validate` `31162869710` and `E2E (local)` `31162869601` green; Vercel Production `dpl_5rVkNjnWfXKeXoKYnhDWiGb3xn7T` READY; separate workflow, **non-required**; `Validate / validate` unchanged & sole required gate | Prohibited | No |
| **finalization** *(shared)* — **this reconciliation** | Docs reconciliation + acceptance | docs | none | owner accepts PFA-C03 done | none | §14 DoD met; both phases shipped; documents reconciled to the merged state | Prohibited | No |

**D1 (cloud plan/region) blocks only the cloud phases (`C03A1-C` / `C03A2-C`), never the local phases.** E2E subset expansion waves (§8) run within whichever Phase A branch is chosen (`C03A2-L` under local-first, `C03A2-C` under cloud-first); `import-order` was planned as the last, externally-gated wave; that gate was removed when D4 was resolved (§12), and it now runs in the default local lane with no provider dependency.

---

## 11. Owner-action and secret-name checklist

`[OWNER]` — no values, names only. **Local-first needs no cloud action.**

**Decisions (both paths):**

- [x] **D2 — test-backend architecture:** **RESOLVED 2026-07-31 → local-first** (path `C03A1-L → C03A2-L`; cloud not selected). `[OWNER]` `[DECISION — RESOLVED]`
- [x] **D3 — DB-test framework:** **RESOLVED 2026-08-02 → pgTAP** (new C03B1 suites use pgTAP; the framework-free `owner_access_and_quota_verification.sql` is preserved byte-identical and run alongside them). `[OWNER]` `[DECISION]`

**Local-first (Model A) — no cloud provisioning required:**

- [x] `E2E (local)` (non-required local Playwright CI) — **implemented, merged, and accepted** (PR #175). `[OWNER]`
- [x] `db-tests` (the C03B2 hosted DB-security workflow) — **implemented, merged, and accepted** (PR #181, merge `b5761418…`; §9.8). It exists on `main` as `DB Tests / db-tests` and PFA-C03 itself did **not** change branch protection. Promotion to required was D5, **resolved by the owner on 2026-08-16 → `REQUIRE_DB_TESTS`**; `db-tests` is now a **required** check (§17). `[OWNER]`
- [ ] Confirm seeded local `TEST_USER_EMAIL` / `TEST_USER_PASSWORD` handling (sensitive operational config; the anon key + local URL are public client config). `[OWNER]`

**Cloud (Model B) — required for a cloud-first D2 selection; optional for later hybrid parity:**

- [ ] Create the **cloud** Supabase project (`paperlume-staging`); confirm **plan** and **region** (D1). `[OWNER]`
- [ ] Create **dedicated cloud staging accounts** (never `ps4`; secondary account for cross-user tests). `[OWNER]`
- [ ] Create a GitHub **Environment** (`staging-e2e`) and add: **public** — `VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`, staging ref; **sensitive** — `TEST_USER_PASSWORD`, a reset credential (`STAGING_SUPABASE_DB_PASSWORD` or `STAGING_SUPABASE_ACCESS_TOKEN`); handle `TEST_USER_EMAIL` as sensitive operational config. `[OWNER]`
- [ ] Confirm **fork-PR secret protection** + required-reviewer gating on the Environment. `[OWNER]`
- [x] For Wave 4 only: decide whether to serve/provision `fetch-paper-metadata` + egress for `import-order`, or defer (D4). **Resolved 2026-08-16 → `DETERMINISTIC_STAND_IN`** (§12): neither is needed — `import-order` runs deterministically against a stand-in at the metadata-function boundary, so no served Edge Function, egress allowance or provider credential was provisioned. `[OWNER]`
- [ ] Explicitly **exclude** `GOOGLE_MONITORING_*` / provider-quota from any test backend (C29). `[OWNER]`

---

## 12. Owner decision packet

`[DECISION]` — only genuinely open items. (Not reopened: PFA-C03 selection, C27–C29, no Production-backed Playwright, no real-Gemini Playwright, the required `Validate` gate, staged cloud/Production authorization.)

- **D1 — Cloud staging plan/region.** *Why:* cost/latency of the **optional** cloud project. *Recommended default:* **not needed** under local-first; if cloud is selected, lowest tier supporting the full migration chain + Auth + Storage, same region as Production. *Trade-off:* cost/realism. *Blocks:* **only** C03A1-C/C03A2-C (never the local path).
- **D2 — Test-backend architecture (local-first / cloud-first / local-first + optional cloud). `[RESOLVED 2026-07-31 → local-first]`** *Why:* the substantive architecture choice; it selects the required Phase A branch (§10) and drives determinism, cost, secret surface, and which specs run first. *Recommended default:* **local-first**, adding cloud only when a wave demonstrably needs it. *Alternatives:* cloud-first; local-first + optional cloud. *Trade-off:* realism vs cost/complexity/secret surface. *Selects the Phase A branch:* local-first → `C03A1-L → C03A2-L`; cloud-first → `C03A1-C → C03A2-C`; hybrid → `C03A1-L → C03A2-L` first, optional `C03A1-C → C03A2-C` later. *Blocks:* the Phase A branch shape (local-first unblocks `C03A1-L` immediately without any cloud decision; cloud-first additionally needs D1). Phase B (`C03B1 → C03B2`) is shared and unaffected. **Owner outcome:** local-first selected; path `C03A1-L → C03A2-L`; cloud phases not selected; D1 still does not block the local path. The Data API grant-parity prerequisite (`PFA-C03A1-L-GRANT-PARITY-001`, §7.3) is **complete — merged and applied to Production 2026-07-31**, and **C03A1-L has now been implemented, merged, and independently accepted** (`…-MERGE-001`: PR #173, merge `299b19a…`; local stack replays all 69 migrations, deterministic 120/5 seed, two-layer fail-closed guards + negative controls, six read-only specs green (32), deterministic second reset/reseed (auth 6)). **C03A1-L is merged and accepted; C03A2-L is merged and accepted (PR #175, merge `1d091e4…` — see §8.1, §10); Phase A is complete for the selected local-first path. Phase B has since completed as well, and PFA-C03 is complete (§14).**
- **D3 — DB-test framework (framework-free hybrid vs pgTAP). `[RESOLVED 2026-08-02 → pgTAP]`** *Why:* maintenance/reporting. **Owner outcome:** new C03B1 suites use **pgTAP**; the framework-free `owner_access_and_quota_verification.sql` is preserved byte-identical and executed alongside them. *Trade-off:* zero-dep vs standardized TAP. *Formerly blocked:* C03B1's framework choice. **The former pre-existing-security blocker (§9.6) was remediated and removed; C03B1 subsequently resumed, was implemented, and merged via PR #179 (§9.7). D3 remains resolved to pgTAP; C03B2 was separately authorized and is now merged and accepted via PR #181 (§9.8), so the pgTAP lifecycle runs automatically on `main` through the non-required `DB Tests / db-tests` check.**
- **D4 — External-metadata E2E (import-order). `[RESOLVED 2026-08-16 → DETERMINISTIC_STAND_IN]`** *Why:* PubMed/Crossref are nondeterministic and would need served Edge Functions + egress. *Outcome:* the second recommended option was taken — a deterministic stand-in — so no provider egress, credential, secret or cloud environment was introduced. *Trade-off accepted:* live-provider realism is given up in CI in exchange for a regression that cannot flake on a third party.
  - **The predecessor spec was not usable as CI evidence.** `e2e/import-order.spec.ts` existed but imported three real PMIDs and asserted exact PubMed article-title prefixes, so it required live provider egress; it was therefore excluded from `DEFAULT_SPECS` and never ran in the hosted lane.
  - **`import-order` now runs automatically in the safe local E2E lane.** It is a member of `DEFAULT_SPECS` in `scripts/e2e-local.mjs`, so `npm run test:e2e:local` and the hosted `E2E (local)` workflow execute it without any workflow change.
  - **Metadata is supplied at the function boundary.** Playwright fulfils exactly one endpoint — `/functions/v1/fetch-paper-metadata` — asserting that the real client issued a single authenticated (bearer-shaped) POST carrying the identifiers in exact user-input order, and returning synthetic `PaperMetadata` records in that same order. Auth, PostgREST, RPC, Storage, the other Edge Functions and Vite are never intercepted.
  - **Everything downstream stays real:** the real Add Papers UI and session, the real normalization, the real `safe_bulk_insert_papers` against the ephemeral local database, real `insert_order`, the real refetch and table, and persistence proven across two reloads. The imported rows are disposable and the spec deletes them again, restoring the seeded baseline.
  - **Live PubMed/Crossref responses are not CI contracts.** No asserted title belongs to a real publication, and CI does not test provider availability. Real-provider validation remains manual, non-gating post-deploy smoke (see the established metadata case in [start-here.md](start-here.md)).
  - *Blocks:* nothing. PFA-C03's Definition of Done never depended on it (§14).
- **D5 — Promotion of `e2e-local` or `db-tests` to a required check. `[RESOLVED 2026-08-16 → REQUIRE_DB_TESTS — IMPLEMENTED]`** *Why:* changes the merge gate. *Evaluation:* a read-only audit of live branch protection and full run history was completed **2026-08-16** against `main` `86fe1d78…` and recommended **`REQUIRE_DB_TESTS`** (confidence **MEDIUM**) — evidence in **[§16](#16-d5-required-check-evaluation--point-in-time-evidence-2026-08-16)**. **Owner outcome (2026-08-16):** accept `REQUIRE_DB_TESTS` — keep **`validate`** required, add **`db-tests`** as required, keep **`e2e-local`** non-required. *Implemented state:* branch protection on `main` now requires exactly **`validate`** and **`db-tests`**, both bound to GitHub Actions app **`15368`**, with **strict/up-to-date retained** and admins still enforced; **`e2e-local` is not required**, no ruleset exists, and no other protection field changed — activation record in **[§17](#17-d5-owner-decision-and-required-gate-activation-2026-08-16)**, durable decision in **[decisions-and-triggers.md](decisions-and-triggers.md)**. *Why `db-tests` and not `e2e-local`:* `DB Tests` measured 100% first-attempt success on scored pull-request runs, a flat-to-tightening duration well inside its timeout, the smallest added merge latency, and it emitted its check on every eligible pull-request head; `e2e-local` measured a materially higher and rising duration trend and its only red was a defect in its own spec, not in the product. *Trade-off accepted:* a stronger merge gate in exchange for `DB Tests`'s added merge latency. *Blocks:* **nothing**. **A failing `db-tests` now blocks merge — that is the intended behaviour, not grounds for rollback.**

---

## 13. Risk register

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| Regression or bypass of the fail-closed Production guard | Low | **High** | **Two-layer guard** (§5) now merged (C03A1-L): Layer 1 Node/CI rejects blanks/Production/remote before server start **and** Layer 2 browser-observed verifies the actual backend before credentials; 32 guard tests; local-only lifecycle. Production-backed Playwright **fails closed** (no longer the default) |
| Node check "proves" config but the loaded bundle differs | Medium | High | Layer 2 observes the frontend's real Supabase host at runtime, before auth |
| Secrets exposed to fork PRs | Low under local-first (no cloud secret) / Medium under cloud | High | Local-first removes cloud secrets from the initial path; cloud path uses same-repo/`workflow_dispatch` + protected Environment + required reviewers |
| Cleanup-crash leaves **local fixture** residue (esp. `search-attribution`) | Medium | Low-Medium | Reset-to-seed as the isolation boundary on the **ephemeral local stack**; disposable fixture paper for the six-field spec. The supported Playwright lifecycle is **local-only** — no Production data is reachable (fail-closed guard), so residue is confined to a disposable local stack |
| Count/order-sensitive specs collide under overlap | Medium | Medium | Concurrency guard (one run) + reseed + isolation lane for import specs |
| External metadata drift (`import-order`) | Medium | Medium | **Mitigated (D4, §12):** the deterministic stand-in was implemented, so no live provider response is a CI contract |
| Secret values leak into logs/artifacts | Low | High | Mask; assert on host/ref substrings; never upload env files; publishable key is public but still not broadcast |
| Negative control leaves a committed weakening | Low | High | Outer expected-failure harness; abort-driven rollback; **fresh-connection** verification of restored baseline |
| Phase A/B job creeps into required `Validate` | Low | High | New workflows are separate and non-required; `Validate` untouched |
| Test backend drifts from Production schema | Medium | Medium | Reset via full migration replay every run |
| Unverified pricing/limit assumptions | Low | Medium | This doc makes **no** pricing/limit claims; owner confirms at (optional) cloud provisioning |

---

## 14. Definition of done for PFA-C03

PFA-C03 is complete only when **both** phases have shipped (across separate bounded PRs). **All three criteria are now met.**

- **Phase A done — met for the selected local-first path:** an isolated test backend exists (local-first ephemeral local Supabase; cloud not selected); Playwright is wired to it fail-closed with the **two-layer** Production guard proven (Layer 1 rejects blanks/Production/remote before server start; Layer 2 verifies the browser-observed backend before credentials); the **non-required** `E2E (local)` job runs the initial six-spec subset green on `main` with documented setup (C03A2-L, PR #175, merge `1d091e4…`; merged-main run `30711943858`); `Validate / validate` remains unchanged and required. `[FACT]`
- **Phase B done — met:** the database-security suite (reusing `owner_access_and_quota_verification.sql` byte-identical and running it alongside the five pgTAP files that existed at that point — a sixth, `005`, was added later by the separate duplicate-merge repair, §9.6.1, taking the current aggregate to **471/471**) covers cross-user RLS isolation, S1 caller/ownership, RPC EXECUTE grants, AI-quota consume/refund atomicity under **true concurrency**, storage-quota enforcement, and internal-access non-escalation — **394/394 pgTAP + 18/18 framework-free**; it runs in the **separate, non-required `DB Tests / db-tests` workflow** (C03B2, PR #181, merge `b57614182fdabff94bc26d128585c7754b7ae45c`; §9.8), which **exists on `main`** and passed on the merge commit itself (run `31162869485`, job `92816960014`); and it includes the **expected-failure papers-RLS negative control** that fails-then-reverts, with restoration verified through a fresh connection, plus a catalog-fingerprint sensitivity probe and a full residue proof. `[FACT]`
- **Finalization — met:** this document and [start-here.md](start-here.md) are reconciled to the merged state; owner acceptance is recorded by merging this reconciliation. `[FACT]`

**PFA-C03 is COMPLETE.** The C03A0 contract shipped; **C03A1-L is merged and independently accepted** (local-first foundation; PR #173, merge `299b19a…`); and **C03A2-L is merged and accepted** (non-required `E2E (local)` / `e2e-local` CI reusing the merged C03A1-L lifecycle — PR #175, merge `1d091e4…`; see §8.1). The `E2E (local)` workflow **exists on `main`** and was green on its merge commit (run `30711943858`), so **Phase A is complete for the selected local-first path** (the optional cloud phases `C03A1-C → C03A2-C` were not selected). Phase B then completed in three steps: C03B1 authoring uncovered pre-existing database-security defects (§9.6) and the **bounded remediation was merged and deployed to Production** (PR #177, merge `f4088705a2d3b8869579b253b5d8f7b3162414cf`; migration `20260802025704` applied, ledger 70), removing the C03B1 security blocker; **C03B1's implementation was merged to `main` and accepted** (PR #179, merge `0f786dac7088b2afafc16cb76afae8eee5f206d6`; §9.7), putting the four pgTAP suites and the `test:db:local` runner on `main`; and **C03B2's hosted `DB Tests / db-tests` workflow was merged to `main` and accepted** (PR #181, merge `b57614182fdabff94bc26d128585c7754b7ae45c`; §9.8), so `.github/workflows/db-tests.yml` now **exists on `main`** and passed on the merge commit itself (run `31162869485`, job `92816960014` — 70 migrations, pgTAP 394/394, legacy 18/18, negative control and sensitivity probe both detected-and-reverted, true concurrency, residue and teardown clean, 0 artifacts, zero credential-value leaks), alongside green merged-main `Validate` (`31162869710`) and `E2E (local)` (`31162869601`) and a READY Vercel Production deployment (`dpl_5rVkNjnWfXKeXoKYnhDWiGb3xn7T`) — with Production Supabase unchanged (70 migrations, Edge Functions v10/v15/v3, integrity 0/0/0). The required **`Validate / validate`** gate is **unchanged**, and **PFA-C03 itself never modified branch protection** — it shipped `DB Tests / db-tests` and `E2E (local) / e2e-local` as **non-required** checks. **D3 is resolved (2026-08-02, pgTAP); D4 is resolved (2026-08-16) → `DETERMINISTIC_STAND_IN`** — completion required neither `import-order` automation (D4) nor promotion of any check to required (D5); both arrived afterwards. **D5 was resolved separately on 2026-08-16 → `REQUIRE_DB_TESTS`**, and `db-tests` is now a required gate alongside `validate` (§17); that promotion is a post-completion governance change, not part of PFA-C03's definition of done. The known `merge_exact_duplicates` `unnest(jsonb)` product defect (§9.6.1) was tracked separately as `MERGE-EXACT-DUPLICATES-JSONB-REPAIR-001`, was **never** part of PFA-C03, and was **not** fixed by its completion; it has since been repaired and deployed under its own task (PR #184, merge `bd741f56…`; migration `20260807141402` applied to Production 2026-08-07). **C27–C29 are unchanged**, and completing PFA-C03 selects **no** further PFA candidate — **PFA-C02 and PFA-C04–PFA-C09 remain unselected** and require a new explicit owner decision.

---

## 15. Evidence appendix

All read-only; gathered on branch `docs/pfa-c03-staging-contract` (from `origin/main` `5b78b08d`). **This appendix is a point-in-time C03A0 record and is deliberately not updated** — its counts (e.g. the 69-migration chain ending `20260731162729`) describe the repository as it stood at the audit. For current state see §2.4 (**71** migrations, latest `20260807141402`) and §9.6–§9.8.

- **Preflight:** `git status --short` clean; `git rev-parse origin/main` = `5b78b08d074703b6ed5cdd3a1c7a5b118f6a6a38`; no overlapping `staging`/`pfa-c03`/`e2e-ci` branch or open PR.
- **CI:** `.github/workflows/validate.yml` (lines 1–73).
- **Playwright:** `playwright.config.ts` (1–70); `e2e/global-setup.ts` (1–41, Node/Playwright — does not read browser `import.meta.env`); `e2e/helpers.ts` (1–262).
- **E2E specs (13):** `auth`, `bulk-actions`, `eager-load`, `filters`, `paper-import`, `pools`, `filter-presets`, `file-import-order`, `mutations`, `notes`, `search-attribution`, `attachments`, `import-order` (all read in full).
- **Frontend env:** `src/lib/clientEnv.ts`; `src/integrations/supabase/client.ts`; `vite.config.ts`; `package.json` (`dev = vite`); `.env.example`; `.env.test.example`; `.gitignore`.
- **Production-fallback evidence:** untracked `.env` defines `VITE_SUPABASE_URL` + `VITE_SUPABASE_PUBLISHABLE_KEY` and references `lioxtgiputfniqbktcsz` (name/count only; **no values printed**); `.env.test` defines only `TEST_USER_EMAIL` + `TEST_USER_PASSWORD`; no `.env.local`/`.env.development` present.
- **Supabase:** `supabase/config.toml` (ref `lioxtgiputfniqbktcsz`; three `verify_jwt=false` functions); 69 migrations (chain end `20260731162729`); storage/quota migrations in §2.4; `supabase/functions/_shared/env.ts`; secret names + external hosts grepped from `supabase/functions/`.
- **DB test:** `supabase/tests/owner_access_and_quota_verification.sql` (18 cases, `BEGIN … ROLLBACK`).
- **Decisions:** C27/C28/C29 and S1 in [decisions-and-triggers.md](decisions-and-triggers.md); unlock order in [owner-decisions.md](owner-decisions.md); PFA-C03 in [product-feature-audit.md](product-feature-audit.md) §14/§15; line budget in [documentation-policy.md](documentation-policy.md).

---

## 16. D5 required-check evaluation — point-in-time evidence (2026-08-16)

`[FACT]` **Read-only audit `CI-GATE-EVALUATION-001`.** Every number below is **point-in-time evidence measured on 2026-08-16** against `main` `86fe1d78324c99588dfa1ed95b3088f17d7f5eca`, not an evergreen invariant. Re-measure before relying on any of it. **No branch protection, ruleset, or workflow was modified by this evaluation, and no workflow run was re-run, dispatched, or cancelled.** At the time this section was written D5 was an **open owner decision** and §16 recorded only a recommendation. **The owner has since accepted `REQUIRE_DB_TESTS` and the gate was activated on 2026-08-16 — see §17.** This section is retained **unchanged as pre-promotion evidence**: everything below describes the state and measurements *before* any protection change, and must not be read as current protection state.

### 16.1 Live protection baseline

Read from `GET /repos/Papi299/paper-whisperer-62/branches/main/protection`, `…/rulesets`, and `…/rules/branches/main`:

- **Classic branch protection only.** `rulesets` = `[]` and branch-applicable `rules` = `[]` — **no ruleset overrides classic protection**, and no merge queue applies. A future mutation therefore only has to reason about classic protection.
- **Required status checks:** `strict: true` (up-to-date required); `contexts: ["validate"]`; `checks: [{ "context": "validate", "app_id": 15368 }]` (GitHub Actions).
- **Reviews:** `required_approving_review_count: 0`; stale-dismissal, code-owner review and last-push approval all off.
- **Other:** `enforce_admins: true`; `required_conversation_resolution: true`; `allow_force_pushes: false`; `allow_deletions: false`; `block_creations: false`; `lock_branch: false`; `required_linear_history: false`; `required_signatures: false`.
- Repository visibility is **public** (owner-controlled), so fork-origin pull requests are possible in principle.

### 16.2 Check identities — the required context is the bare job name

Verified against actual emitted check runs, not UI labels. **Branch protection uses the bare job name, not the `Workflow / job` display string** — the live required context is `validate`, *not* `Validate / validate`.

| Lane | Workflow file | Workflow name | Job key | Displayed job | **Emitted check name / protection context** | App | Required today |
|---|---|---|---|---|---|---|---|
| Validate | `.github/workflows/validate.yml` | `Validate` | `validate` | `validate` | **`validate`** | GitHub Actions (`15368`) | **YES** |
| E2E | `.github/workflows/e2e-local.yml` | `E2E (local)` | `e2e-local` | `e2e-local` | **`e2e-local`** | GitHub Actions (`15368`) | no |
| DB | `.github/workflows/db-tests.yml` | `DB Tests` | `db-tests` | `db-tests` | **`db-tests`** | GitHub Actions (`15368`) | no |

Vercel occupies a separate namespace (commit-status context `Vercel`; check run `Vercel Preview Comments`, app `8329`) and **no external service emits `e2e-local` or `db-tests`**, so neither candidate context is ambiguous or spoofable by a third party.

### 16.3 Methodology and sample

All runs since each workflow's introduction were inspected — each lane's total history is smaller than the 60-run target window, so the sample is complete rather than truncated. **Zero `workflow_dispatch` runs exist in any lane.**

| Lane | Introduced | Total runs | PR runs | `main` push runs | Dispatch |
|---|---|---|---|---|---|
| Validate | 2026-07-21 | 174 | 118 | 56 | 0 |
| E2E (local) | 2026-08-01 | 122 | 81 | 41 | 0 |
| DB Tests | 2026-08-07 | 103 | 68 | 35 | 0 |

Pull-request runs are scored separately from `main`-push runs because **only pull-request behavior determines suitability as a pre-merge gate**. Superseded concurrency cancellations are excluded from success rates (they are correct behavior, not failures). First-attempt success is distinguished from eventual success by reading every attempt of every re-run run. Every non-success pull-request run was inspected at step level, and job logs were read where the step alone was not conclusive.

**Material-change check.** `validate.yml` and `db-tests.yml` have exactly **one commit each** (their creation). `e2e-local.yml` has two, the second (`bfbddf4`) being a **comment-only** edit. No workflow rename, job-key change, display-name change, trigger change, or concurrency change has ever occurred, so the measured history corresponds to today's configuration. The *test corpus* did grow during the window (E2E specs added 2026-08-10/11/14/15; DB suites `005`–`008` added 2026-08-07/09/11), which is reflected in the recent-window duration trend below.

### 16.4 Reliability — pull-request runs

| Lane | PR runs | Superseded cancels | Scored | **First-attempt success** | Eventual success | Legitimate failures | Flakes | Infra transients | Timeouts | Same-SHA re-runs needed |
|---|---|---|---|---|---|---|---|---|---|---|
| Validate | 118 | 0 | 118 | **117/118 (99.2%)** | 117/118 | 1 | 0 | 0 | 0 | 0 |
| E2E (local) | 81 | 6 | 75 | **73/75 (97.3%)** | 74/75 (98.7%) | 1 | 0 | 1 | 0 | 1 |
| DB Tests | 68 | 4 | 64 | **64/64 (100%)** | 64/64 (100%) | 0 | 0 | 0 | 0 | 0 |

`main`-push runs were green in every lane across the whole sample except one E2E run (`31175196562` attempt 1, an infrastructure transient that passed on attempt 2 of the same SHA).

**Every non-success pull-request run, classified:**

- **Validate — 1 `LEGITIMATE_PRODUCT_OR_TEST_FAILURE`.** Run `29817120858`, the workflow's own first run ever, on the branch that authored it: `Report tool versions` ran *before* `Install dependencies`, so `npx tsc --version` had no project TypeScript to resolve. Fixed by reordering the steps within that same authoring PR. Predates the check becoming required; Validate has been **117/117 green on pull requests since**.
- **E2E — 1 `LEGITIMATE…FAILURE` (test harness, not product).** Run `31456354260`, SHA `332c6745…` (PR #203). Analysed in §16.6.
- **E2E — 1 `CI_INFRASTRUCTURE_TRANSIENT`.** Run `31796835366` attempt 1, SHA `9d74f70b…`: `supabase start` exited 1 while bringing up the ephemeral stack. Attempt 2 on the identical SHA passed. `DB Tests` and `Validate` both passed on that same SHA on their first attempt.
- **E2E — 6 `SUPERSEDED_CANCEL`; DB Tests — 4 `SUPERSEDED_CANCEL`.** All ten fall on just two branches during rapid successive pushes (`fix/pfa-c08-security-hardening`, `fix/import-nbib-enw-formats`). In every case a newer run on a newer SHA of the same branch started immediately and the branch's final SHA passed in all lanes. This is `cancel-in-progress` working as designed, **not** a false failure. `DB Tests` was cancelled less often than `E2E` simply because it finishes sooner.

**Shared latent hazard.** The two E2E transients were both ephemeral-Supabase bring-up failures: `supabase/setup-cli` resolving no CLI binary (`No matching Supabase CLI binary package found for linux-x64`) and `supabase start` exiting 1. **`DB Tests` uses the identical pinned action and CLI version and is exposed to the same hazard** — it has simply not been hit (0 in 103 runs). Across both candidate lanes the observed rate is **2 in 225 runs (~0.9%)**, i.e. roughly one false block per ~110 required runs, recoverable by a single re-run of a ~3-minute job. In the worst observed case the fail-closed teardown and residue assertions still confirmed no leftover stack and no generated auth state, so **cleanup held even when setup failed**.

### 16.5 Runtime, timeout headroom, and added merge latency

Measured from actual job start/completion timestamps of successful pull-request runs. Both candidates declare `timeout-minutes: 20`.

| Lane | min | p50 | p90 | max | p90 % of timeout | max % of timeout |
|---|---|---|---|---|---|---|
| Validate | 0m38s | 1m02s | 1m33s | 1m45s | 7.8% | 8.8% |
| E2E (local) | 3m39s | 4m37s | 6m58s | **10m38s** | 34.9% | **53.2%** |
| DB Tests | 2m41s | 3m09s | 4m03s | 4m19s | 20.3% | 21.6% |

**Recent-window stability (latest 20 eligible pull-request runs per lane, 2026-08-11 → 2026-08-15).** All three lanes were 20/20 green. Durations diverge sharply: `DB Tests` is **flat-to-tightening** (p50 3m03s, p90 3m14s, max 3m22s — a p90 *improvement* on the full sample), while `E2E` is **trending upward** (p50 5m18s, p90 8m49s, max 10m38s — p50 +15% and p90 +27% versus its own full sample) as its spec corpus grows.

**Added blocking latency.** The lanes run in parallel, so their runtimes are *not* additive. For each paired pull-request head the candidate's added gate delay is `max(0, candidate completion − Validate completion)`:

| Promotion modelled | Paired heads | p50 added | p90 added | max added |
|---|---|---|---|---|
| `REQUIRE_E2E_LOCAL` | 74 | 3m37s | 6m54s | 13m49s |
| `REQUIRE_DB_TESTS` | 64 | **1m59s** | **2m34s** | **2m52s** |
| `REQUIRE_BOTH` | 61 | 3m38s | 6m58s | 9m15s |

Both candidates finished after `Validate` on **every** paired head, so promotion always adds some delay. `DB Tests` is the only candidate whose added delay is tightly bounded — its worst observed case is under three minutes, whereas `REQUIRE_BOTH` is dominated by `E2E` and effectively costs the same as `REQUIRE_E2E_LOCAL`.

**Runner consumption is unchanged by promotion.** Both candidates *already* run on every same-repository pull request, so making one required adds **no** runner minutes — it changes only merge-blocking semantics. Median successful runner time per PR is ~1m (Validate), ~3m (DB Tests), ~4.6m (E2E). GitHub plan/billing data is **not readable** with the available credentials, so **no cost figure is claimed**; the repository is currently public, which is an owner-controlled setting.

### 16.6 Unique merge-safety value — candidate red while `Validate` green

Exactly **one** such event exists in the entire sample.

| SHA | PR | Candidate | Validate (same SHA) | DB Tests (same SHA) | Root cause | Legitimate catch | Code change required |
|---|---|---|---|---|---|---|---|
| `332c6745…` | #203 | `e2e-local` **failed** | **success** | **success** | The PR's own **new** spec `e2e/account-export.spec.ts` closed the export dialog with a synthetic `Escape`; triggering a browser download had moved keyboard focus out of the page, so the dialog never closed | **Yes, but test-harness only** | Yes — fix `8b60a817…` touched **only** `e2e/account-export.spec.ts` (+5/−1) |

The failure was **deterministic**, not flaky: all three Playwright attempts (initial plus the two CI retries) failed, and 33 other tests passed. But the defect was in the spec the same pull request was adding, **not in the application or database** — no product code changed to fix it. So `E2E (local)`'s measured unique-catch rate against application defects is **0 in 81 pull-request runs**; its one catch was a broken new test.

`DB Tests` has never been red, so it has produced **zero** empirical unique catches. That absence is weaker evidence than a demonstrated catch, but it is **not** the same as an unexercised detector: every `DB Tests` run executes an **expected-failure papers-RLS negative control** that must detect an intentionally injected cross-user RLS regression and then verify the rollback, plus a **catalog-fingerprint sensitivity probe** that must detect same-name definition changes. A green `DB Tests` run therefore carries a per-run proof that its detectors still fire — the suite validates its own sensitivity on every execution. Its coverage (RLS isolation, relational ownership, RPC caller scope and grants, storage/quota invariants, duplicate-merge behaviour, publication-type provenance, function `search_path` hardening, account-deletion cascades, ~70-migration replay, true-concurrency AI-quota cap, residue/catalog equality) is a layer `Validate` cannot reach at all, since `Validate` runs no Supabase stack.

### 16.7 Fork-origin behaviour and check emission

Both candidate jobs carry a **job-level** same-repository `if` condition and **neither workflow has path filters**. Per GitHub's official documentation, these two cases differ decisively: a workflow skipped by *path or branch filtering* leaves its checks **Pending and blocks merging**, whereas **a job skipped by a conditional reports its status as `Success` and will not prevent a pull request from merging, even if it is a required check.**

Consequences if a candidate were promoted: a fork-origin pull request would still trigger the workflow, the job would skip before any dependency install, Docker use, or stack startup, and the required check would report success — so **no fork-origin pull request becomes permanently unmergeable**. The honest caveat is the mirror image: that green is **vacuous**, giving no actual protection on fork PRs. This is not material today (**all 215 pull requests in repository history are same-repository, single-author; zero fork PRs ever**) but it would become material the moment outside contributions are accepted. **Weakening the same-repository restriction to make a required check meaningful on forks is explicitly not proposed here** — it is a security boundary, and no workflow change was authorised by this evaluation.

**Emission reliability.** A required check must actually appear on every eligible head. It does: `E2E (local)` emitted on **81/81** eligible pull-request heads since its introduction and `DB Tests` on **68/68**, with no missing, duplicated, or ambiguous check. Both contexts have been **stable since creation** (§16.3).

### 16.8 Eligibility rubric

| # | Criterion | `E2E (local)` | `DB Tests` |
|---|---|---|---|
| 1 | Local-only safe under normal execution | **Yes** | **Yes** |
| 2 | No secret / Environment / Production dependency | **Yes** | **Yes** |
| 3 | Stable, identifiable check on every eligible PR | **Yes** (81/81) | **Yes** (68/68) |
| 4 | Required-check semantics for intentional skips understood | **Yes** | **Yes** |
| 5 | Meaningful current-history evidence | **Yes** (81 PR runs) | **Yes** (68 PR runs) |
| 6 | No unresolved recurring flake/transient pattern | **No** — 1 PR + 1 push stack-bring-up transient; 1 same-SHA re-run needed | **Yes** — 0 in 103 runs (shared latent risk noted) |
| 7 | No unexplained timeout/duration trend | **No** — p90 +27% recent, max 53.2% of timeout | **Yes** — flat/tightening, max 21.6% |
| 8 | Cleanup/residue behaviour reliable | **Yes** | **Yes** |
| 9 | Logs actionable enough for a required gate | **Yes** | **Yes** |
| 10 | Unique value material enough to justify its latency | **No** — only red was its own spec; highest latency | **Yes** — unreachable-by-`Validate` security layer at ~2m p90 |
| | **Technically eligible today** | **NO** | **YES** |

### 16.9 Recommendation

**`REQUIRE_DB_TESTS`** — confidence **MEDIUM**.

- **Strongest reason for:** `DB Tests` is the only lane that is perfect on the metrics a required gate actually depends on — 100% first-attempt success on all 64 scored pull-request runs, a check emitted on 68/68 eligible heads, a duration distribution that is *tightening* rather than drifting (p90 3m14s recently, 20% of its timeout), and the smallest added merge latency of any option (p90 2m34s, worst case under three minutes) — while covering the RLS, grant, RPC-scope, quota and cascade invariants that the required `Validate` lane cannot test at all, with per-run negative controls proving its detectors still fire.
- **Strongest reason against:** it has never been red, so its pre-merge catch value is inferred from coverage and self-validating negative controls rather than demonstrated on a real regression; and it shares the `supabase/setup-cli` + `supabase start` bring-up dependency that produced both E2E transients, a hazard it has avoided so far in a relatively young 8-day, 68-run window (~0.9% observed across both lanes, ≈ one false block per ~110 runs, recoverable by one re-run).
- **Why `KEEP_BOTH_NON_REQUIRED` lost:** the mechanical evidence for `DB Tests` clears every rubric criterion, and the status quo permits a red `db-tests` to be merged past silently — precisely the failure mode (an unnoticed RLS/grant regression) that this repository's security work exists to prevent. Deferring buys little: the lane is already paid for on every PR, so the only thing withheld is enforcement.
- **Why `REQUIRE_E2E_LOCAL` lost:** it fails rubric criteria 6, 7 and 10. Its only red in 81 pull-request runs was a defect in its own new spec rather than in the product; it needed a same-SHA re-run to go green once; its p90 has risen 27% in the recent window as the corpus grows, with its maximum already at 53% of its timeout; and it carries the largest added merge latency (p90 6m54s, max 13m49s).
- **Why `REQUIRE_BOTH` lost:** it inherits every `E2E` deficiency above and its modelled blocking latency (p50 3m38s, p90 6m58s) is dominated by `E2E`, so it costs essentially the same as promoting `E2E` alone while adding the same unresolved flake and duration-trend risk.

**Operational caveats the owner should weigh.** Protection is `strict: true`, so a base update requires the branch to be brought up to date and the gate re-run; at the recent cadence (4–8 merges/day) `DB Tests`'s ~2-minute cost stays cheap, whereas `E2E`'s would repeat at ~7 minutes per base update. Neither candidate has path filters, so a promoted gate **also blocks documentation-only pull requests** — including the pull request carrying this very evaluation. Adding path filters, retries, sharding or any other workflow improvement was **out of scope** and deliberately not implemented.

### 16.10 Branch-protection mutation plan — designed here, executed later under §17

**No protection mutation was performed *by this audit*.** This is the design a *separate, future, owner-authorised* task would follow if the owner selected `REQUIRE_DB_TESTS`. The owner did select it, and the plan below was executed **verbatim** on 2026-08-16 — the execution record, with the live pre- and post-state, is **§17**.

1. **Narrowest official endpoint:** `PATCH /repos/Papi299/paper-whisperer-62/branches/main/protection/required_status_checks`. This sub-resource touches **only** the required-status-check policy and leaves reviews, admin enforcement, conversation resolution, force-push and deletion settings untouched. **Do not use `PUT /branches/main/protection`** — it replaces the *entire* protection policy and would silently erase any unrelated setting omitted from the payload.
2. **The `checks` list is replacement-based, not additive.** The payload must therefore restate the retained context alongside the new one: `{"strict": true, "checks": [{"context": "validate", "app_id": 15368}, {"context": "db-tests", "app_id": 15368}]}`. (`POST …/required_status_checks/contexts` is additive but accepts bare context strings only, which would drop the GitHub App binding — prefer the PATCH above so both contexts stay app-pinned to `15368`.)
3. **Precondition snapshot, asserted immediately before the write:** `contexts == ["validate"]`, `strict == true`, `enforce_admins == true`, `required_conversation_resolution == true`, `required_approving_review_count == 0`, `allow_force_pushes == false`, `allow_deletions == false`, and `rulesets == []` (§16.1). If a ruleset has appeared since, **stop** — classic protection would no longer be the whole picture.
4. **Expected post-state:** `contexts == ["validate", "db-tests"]`, both app-bound to `15368`, `strict` still `true`, every other protection field byte-identical to the snapshot.
5. **Rollback:** PATCH the same sub-resource back to `{"strict": true, "checks": [{"context": "validate", "app_id": 15368}]}`.
6. **Read-back verification:** re-`GET` the full protection object and diff it against the precondition snapshot, confirming that **only** `required_status_checks.checks` changed; re-`GET` `rulesets` to confirm it is still empty; then confirm on the next same-repository pull request that `db-tests` appears as required and that `validate` is still required.

### 16.11 Reevaluation contract for `E2E (local)`

`e2e-local` should be reconsidered **second**, only after all of the following hold over a defined window (values derived from its own measured behaviour, not chosen arbitrarily):

- **≥ 40 further eligible pull-request runs** after 2026-08-16 with **zero same-SHA re-runs** required to reach green;
- **zero ephemeral-stack bring-up transients** (`supabase/setup-cli` or `supabase start`) over that window;
- **p90 successful duration ≤ 7m00s and maximum ≤ 12m00s** (≤ 60% of the then-declared 20-minute timeout; these absolute thresholds stand as written and were deliberately **not** rescaled when that ceiling was raised to 30 minutes), with **no upward p90 trend across two consecutive windows** — this is the criterion it currently fails, at p90 8m49s and rising;
- **≥ 1 legitimate unique catch whose root cause is application or database code** (candidate red / `Validate` green), as opposed to a defect in its own spec.

Should the repository begin accepting fork-origin contributions, the vacuous-green fork behaviour in §16.7 must be resolved for **any** promoted candidate before its required status can be trusted.

---

## 17. D5 owner decision and required-gate activation (2026-08-16)

`[FACT]` **This section is the activation record. It is deliberately separate from §16.** §16 is frozen pre-promotion audit evidence; §17 is what the owner then decided and what was actually written to GitHub. The three are strictly ordered: **audit (§16) → owner decision (§17.1) → protection activation (§17.2)**.

### 17.1 Owner decision

On **2026-08-16**, after reviewing the §16 evidence packet, the owner selected:

> **`REQUIRE_DB_TESTS`** — keep `validate` required, add `db-tests` as required, keep `e2e-local` non-required.

This resolves **D5**. The durable decision, its rationale, and its re-evaluation triggers live in **[decisions-and-triggers.md](decisions-and-triggers.md)** ("D5 — Required DB-security merge gate"); §12 carries the canonical PFA-C03 entry. The other three candidate outcomes (`KEEP_BOTH_NON_REQUIRED`, `REQUIRE_E2E_LOCAL`, `REQUIRE_BOTH`) were not selected — see §16.9 for why each lost.

### 17.2 Protection activation — what was actually written

Executed as task `D5-REQUIRED-DB-TESTS-PROMOTION-001` against `main` at `3933aa2b2f8c10f0011b8843f25136ae0fdf627b`, following the §16.10 design without deviation.

| | Before | After |
|---|---|---|
| Required contexts | `validate` | `validate`, `db-tests` |
| `validate` app binding | `15368` | `15368` (unchanged) |
| `db-tests` app binding | — (not required) | `15368` |
| `e2e-local` | non-required | **non-required (unchanged)** |
| `strict` (up-to-date) | `true` | `true` (unchanged) |
| Repository rulesets | none | none (unchanged) |
| Branch-applicable rules | none | none (unchanged) |

- **Endpoint:** `PATCH /repos/Papi299/paper-whisperer-62/branches/main/protection/required_status_checks` — the narrow sub-resource. The broad `PUT /branches/main/protection` was **not** used, because it replaces the entire policy and would silently drop any field omitted from the payload.
- **Payload:** `{"strict": true, "checks": [{"context": "validate", "app_id": 15368}, {"context": "db-tests", "app_id": 15368}]}`. The `checks` list is **replacement-based**, so `validate` had to be restated explicitly; omitting it would have removed the existing gate.
- **Writes performed:** exactly **one**. No rollback was needed.
- **Unrelated fields verified byte-identical before and after:** required approving reviews (`0`), stale-review dismissal (`false`), code-owner review (`false`), last-push approval (`false`), conversation resolution (`true`), administrator enforcement (`true`), force pushes (`false`), deletions (`false`), block creations (`false`), branch lock (`false`), linear history (`false`), signatures (`false`), fork syncing (`false`).

### 17.3 Consequences and boundaries

- **A red `db-tests` now blocks merge.** That is the point of the gate. A future `db-tests` failure is **not** grounds for rolling the promotion back; rollback is reserved for an incorrect protection write.
- **`strict: true` interacts with the new gate.** When a pull request falls behind `main` it must be brought up to date and `db-tests` re-runs before merge. At the measured cadence its ~2–3 minute cost is small (§16.5); `E2E`'s would not have been.
- **No path filters exist on either candidate workflow**, so `db-tests` now also gates documentation-only pull requests — including the one carrying this section. Adding path filters, retries, or sharding remains **out of scope** and unimplemented.
- **Runner cost is unchanged.** Both lanes already ran on every eligible pull request; promotion changes only what blocks a merge, not what executes.
- **Fork-origin behaviour is unchanged and still latent (§16.7).** Both workflows carry a same-repository job condition, so on a fork-origin pull request `db-tests` would **skip and report success** — a vacuous green on a now-required gate. This does not affect the current same-repository contribution model, but it **must** be resolved before fork-origin contributions are accepted.
- **`e2e-local` was not promoted and remains non-required.** Reconsider it only against the measurable contract in §16.11, which is mirrored into the durable re-evaluation triggers in [decisions-and-triggers.md](decisions-and-triggers.md).
- **Nothing else changed.** No workflow file, source file, test, package, migration, Supabase object, Vercel setting, secret, variable, or Environment was touched by the activation; no workflow run was re-run, dispatched, or cancelled.
