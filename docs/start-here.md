# Start Here — Current-State Handoff

> **Purpose.** The bounded current-state handoff for a fresh engineering, product, or AI-assistant session. It describes what is true **now**. It is not a project journal: chronology lives in Git, merged PRs, `supabase/migrations/`, and [migration-history.md](migration-history.md).
>
> **Maintenance rule.** Target 150–250 lines; hard maximum 300. Update **in place**: replace or delete statements that stop being true. Never append PR-by-PR narrative. Prefer a link to the authoritative document over copied detail. Prefer a verification command over a volatile snapshot (version numbers, counts). See [documentation-policy.md](documentation-policy.md).

## 1. Product and repository identity

- **Repository:** `Papi299/paper-whisperer-62`. The repository and package name remain **Paper Whisperer** (`paper-whisperer`); Supabase/Vercel/GitHub resource names are unchanged. **Repository visibility is owner-controlled and may alternate between public and private** — check GitHub for the setting in force today, and never treat either state as a fixed architectural or security assumption.
- **Working commercial brand:** **Paperlume** (working brand only — **not** a registered trademark; see C19 in [decisions-and-triggers.md](decisions-and-triggers.md)). Every reachable user-facing surface presents exactly one visible name, **`PaperLume`** (sidebar, Auth card, `<title>`/`og:title`). Internal identifiers were deliberately not renamed.
- **Domains:** `paperlume.app` secured via Cloudflare Registrar; the app is live at **`app.paperlume.app`** (Vercel). The marketing site at the root domain is not built.
- **What it is:** an academic paper library manager — import, organize, search, analyze, and export research papers — using a **single-user workspace model with per-user multi-tenant isolation** (every row is owned by one user; Postgres RLS is the isolation boundary).

## 2. Current architecture

- **Frontend:** React 18 + TypeScript + Vite SPA; shadcn/ui (Radix + Tailwind); TanStack Query for server state; React Router with six routes (`/`, `/auth`, `/dashboard`, `/extension-import`, `/reset-password`, `/privacy`). Route guarding is a client-side redirect for UX only — **RLS is the real security boundary** — and `/privacy` deliberately carries no guard at all: it is the one public, unauthenticated route.
- **Backend:** Supabase — Postgres 17, Auth (JWT, localStorage-persisted sessions), Storage, PostgREST, and Edge Functions (see §5 — all six deployed).
- **Read path:** all filtering, sorting, pagination, keyword matching, and full-text search happen in Postgres; the client holds one page at a time. Detail: [architecture-read-path.md](architecture-read-path.md).
- **Search:** four mutually exclusive modes (empty / short ILIKE for 1–2 chars / prefix-aware FTS for 3+ chars / quoted literal phrase), across six fields (title, abstract, authors, journal, notes, keywords) with a server-driven "Matched in:" attribution line.
- **Attachments:** private Storage bucket, owner-scoped path policies, size/MIME limits, server-side storage-quota triggers.
- **Chrome extension:** a second build target in the same repository and dependency graph (`extension/`, its own Vite config → `dist-extension/`, no second `package.json`). On a toolbar click it classifies the active tab's URL as a PubMed record or a DOI and — **only** where the URL identifies neither, on an ordinary http(s) page — reads four standard bibliographic `<meta>` keys (`citation_doi`, `dc.identifier`, `dc.identifier.doi`, `prism.doi`) out of that one tab's `document.head`, because a DOI resolver redirects to the publisher before a user can reach the toolbar. For a recognised paper it offers **Continue in PaperLume**, which opens **one new tab** at the Production `/extension-import` handoff (below) through `chrome.tabs.create`. It requests `activeTab` and `scripting`, and nothing else: **no declared host permissions**, no `tabs`, no `storage`, no content script, no background context. Invoking the toolbar action is what makes `activeTab` grant temporary host access to that one current tab, and that grant is what permits both reading its `Tab.url` and injecting into it — `scripting` enables the API but reaches no page on its own, which is what makes the pair narrower than a host permission; a real Chromium confirms `chrome.permissions.getAll()` → `origins: []` and refuses a real `executeScript` with no toolbar grant. A PubMed record or a `doi.org` link is still answered from the address with the page never touched, and a page publishing two conflicting DOIs fails closed rather than picking one. It makes **no network or API request of its own** (the DOI is never resolved against doi.org or Crossref), holds **no** PaperLume or Supabase session, persists nothing, sends no page content or source URL, and **cannot import**: the new tab's page owns authentication, Projects/Tags, duplicate handling and the explicit confirmation that writes. Its entire privileged surface is `chrome.tabs.query`, `chrome.tabs.create` and `chrome.scripting.executeScript`, pinned by a source-boundary test and re-checked on the built artefact by the package contract. It ships the production PaperLume icon set at 16/32/48/128 in both `icons` and `action.default_icon`; those PNGs are **not** committed under `extension/` — the extension's Vite config copies them out of the canonical brand pack (`assets/brand/png/`) at build time, byte for byte, so the packaged icon cannot drift from the mark. It packages to a deterministic Store-ready ZIP (`npm run package:extension`, gitignored `release/`) and is exercised in a real Chromium against the **built** output (`npm run test:extension:browser`), where Chrome itself reports the granted permissions as `["activeTab","scripting"]` with zero host origins. Loaded unpacked; **not** published to the Chrome Web Store. As of 2026-08-30 a **draft** Store item exists (`cfanjbamcemoeglgkpbidnclkomaocmo`, package `0.1.0` uploaded) whose **Store Listing, Privacy, Test instructions and Distribution pages are populated and saved** — Category `Workflow & Planning`, Language `English (United States)`, visibility `Unlisted`, regions `All regions` — and it is **NOT submitted and NOT published**. Readiness, policy risk and the remaining gates are in [chrome-web-store-readiness.md](chrome-web-store-readiness.md).
- **Extension import handoff:** `/extension-import?kind=pmid|doi&value=…` is an authenticated route that receives one identifier — from the extension above or from anyone else, with no sender treated as privileged — and nothing else; no token, no session material, no user id, no taxonomy id, no metadata. The value is validated against the application's existing PMID/DOI grammar with no title fallback, opening the route **mutates nothing**, and the import runs through the same `bulkImportPapers` the Add Papers dialog uses, with the same `NormalizationConfig`, only after an explicit confirmation.

### Security invariants — mandatory for new code

- **S1:** every `SECURITY DEFINER` RPC verifies caller identity against `auth.uid()` (explicit guard or internally derived ownership), including an explicit `auth.uid() IS NULL` rejection. Full inventory and rule: [decisions-and-triggers.md](decisions-and-triggers.md) §Security decisions.
- **S2:** client-side mutations/reads on `user_id`-bearing tables carry an explicit `.eq("user_id", userId)` predicate as defense-in-depth, with nullable-safe `userId` threading.
- **No secret key ever reaches the client.** No `VITE_*` variable carries an elevated key. The one Edge Function that needs elevated privileges (`delete-account`) builds a server-only client and never returns or logs that key.
- Least-privilege `EXECUTE` across the SECURITY DEFINER surface (no `PUBLIC`/`anon`/`service_role`), both-owner relational RLS, and bounded `search_path` on all functions are live in Production and pinned by the database suites (§7).

## 3. Implemented capabilities

- Papers CRUD, bulk actions (select-all across the full filtered set), and column/layout customization.
- Import by PMID/DOI identifiers — including structurally authenticated PubMed record URLs and DOI resolver URLs — and from BibTeX / RIS / PubMed NBIB / EndNote tagged / CSV files, with atomic server-side bulk insert. Structured sources state publication types as discrete values, persisted in the nullable `papers.raw_publication_types`; the application never reconstructs them by splitting the legacy joined `raw_study_type`, which stays as the human-readable/fallback representation. The same principle covers authorship: what a source stated about each mention (personal vs collective, name components, affiliations, identifiers, ORCID) is persisted in the nullable `papers.author_provenance`, index-aligned with `authors`, and is never derived by parsing an author string.
- **In-app PubMed discovery (PUBMED-IN-APP-SEARCH-001) — live in Production.** Add Papers has a fourth mode, **PubMed Search**: a query goes to the read-only `search-pubmed` Edge Function (ESearch → ESummary), results are paged 20 at a time, and the user multi-selects records and imports them. The load-bearing boundary is that **discovery finds PMIDs and the existing importer imports them**: the selected PMID strings go into the exact `onBulkImport` callback the Import IDs tab uses, so there is no second normalization, no second insert payload and no second duplicate check, and no ESummary field is ever persisted. Search is explicit (never on keystroke), the query reaches PubMed unrewritten, a stale response cannot repopulate a newer or reset session, the four modes share **one** `selectedProjectIds`/`selectedTagIds` intent, and the whole discovery session is ephemeral — nothing is persisted to storage, URL or profile. A result's publication-type badges are filtered through the current user's existing Study Type Exclusion Pool by trim-aware, case-insensitive, whole-value matching (never substring, so `Clinical Trial` does not hide `Clinical Trial, Phase II`); that is **display-only** and changes neither what is imported nor the study type the importer records. The endpoint-before-UI deployment rule for any future contract change is in [deployment.md](deployment.md) §7b.
- Duplicate detection — **PMID/DOI-only by decision**. The duplicate-merge RPC's successful merge path is live in Production and covered by a dedicated database regression suite.
- Projects, tags, and four curation pools: keywords, synonyms, study types, exclusions.
- Per-paper notes (indexed into search) and saved searches / filter presets (server-side per user, RLS-isolated).
- Exports to CSV / RIS / BibTeX of the current selection/filter (chunked pipeline for large libraries), plus a separate **full account export** — **Account menu → Account → Account data** downloads one versioned ZIP (`paperlume-account-export`, format version 2) covering the signed-in user's whole dataset regardless of filters, including attachment binaries, a whitelisted non-secret profile projection, and the saved AI-model preference. Client composition over existing SELECT-own reads and private Storage reads only. Secrets are excluded by construction (`profiles.pubmed_api_key` is never selected; no Session/User object is serialized), and the export is complete-or-fail.
- Per-paper file attachments in the private Storage bucket, within the server-enforced storage quota. **Settings → Storage** renders a read-only used/quota/remaining gauge (transparency only — no upgrade or checkout path; the database trigger stays the enforcement boundary).
- AI analysis (TL;DR, study type, statistical methods) via Gemini, behind the server-enforced AI quota, with a header usage indicator and an actionable message when the server returns the AI `402`.
- **AI organization suggestions (AI-PROJECT-TAG-SUGGESTIONS-001) — live in Production and Production-accepted.** Both halves are live: the `001A` backend contract and the `001B` Edit Paper experience, which is **frontend-only** against that pre-existing endpoint contract. The `suggest-paper-organization` Edge Function compares one owned paper against the caller's own Projects and Tags and returns four suggestion lists (existing Projects, existing Tags, proposed new Projects, proposed new Tags), each with a short reason. It is **advisory**: it creates nothing, assigns nothing and persists nothing, and its only writes are the existing `consume_ai_quota` / `refund_ai_quota` RPCs — one AI request per successful generation, refunded when the provider fails or returns an unusable result. **No database identifier reaches Gemini**: Projects/Tags cross the boundary as request-local `P1`/`T1` refs whose map lives for one request, and an existing-entity suggestion resolves **only** through that map — no name-based fallback, no fuzzy matching — so an invented ref resolves to nothing. Name comparison is used for one separate purpose only: deciding whether a proposal the model labelled *new* already exists. That promotion happens only when the comparison identifies exactly one row; an ambiguous match (possible because the application key trims and the database key does not) is dropped rather than resolved to a guess. A library past the supported bound fails honestly rather than being silently truncated, and a title-only paper is refused before a unit is spent. **The endpoint is deployed and ACTIVE, and the `001A` backend contract was verified against Production on 2026-08-23** — refusal paths (preflight, method, auth, malformed, title-only, foreign paper) all behaved correctly and cost no quota, and one real generation returned the four suggestion arrays for exactly one `ai_analysis` unit with no application-domain row changed. **`001B` Edit Paper Production acceptance completed separately on 2026-08-24**: a real generation returned suggestions with reasons, and both persistence paths were manually verified end to end against Production — selecting a suggested *existing* Project/Tag and pressing Save Changes, and **Create & select** on a proposed *new* one followed by Save Changes; in each case the assignment was still present after reopening the paper. Generation itself persisted nothing. **`001B` added the caller**: a compact **AI organization** section in Edit Paper, above the Projects selector, invoked only by an explicit click — never on open, on typing, on Save or on opening a selector — and disabled when the draft could not produce a useful answer. The request is built from the **unsaved** draft (title, abstract, parsed keywords, study type) plus the current local selection ids, because the user is asking about what they are editing, not about the stored row. **Accepting an existing Project/Tag changes local dialog state only** and persists through the ordinary Save Changes path, so cancelling assigns nothing; **a proposed new entity needs its own "Create & select" click**, which creates it immediately through the existing `createProject` / `createTag` mutations while the paper assignment still waits for Save — stated in the UI rather than implied. The client re-checks identity at action time: an existing suggestion is actionable only while its id is still in the taxonomy, and a proposed-new name is reconciled against the *current* taxonomy under the same `trim + lower` comparison, selecting a single match instead of duplicating it and refusing to guess when several match. Stale responses cannot land: a result commits only if the generation is still the newest, for the same paper, and for the same semantic draft — and once results are on screen, editing the draft marks them stale rather than letting old reasons describe new text. `001B` changed **no** Edge source and required no deployment, because the contract it calls is the one `001A` deployed and verified. Both actions spend the one `ai_analysis` counter, which is why the visible allowance is now named **"AI requests"** (display copy only — no RPC, column or hook was renamed). Decision C32 holds the durable architecture.
- Per-user optional PubMed API key (stored in `profiles`, used server-side by `fetch-paper-metadata`).
- Actionable first-run onboarding for an empty library, keyed off the **unfiltered** count so a filtered zero-result view is a distinct state. No persistent onboarding flag.
- **Author identity resolution is user-scoped and explicit** (AUTHOR-IDENTITY-RESOLUTION-001C — **live in Production**; see §5). Three deliberately separate layers: 001A compares author *strings* (`authorMentionKey`), 001B records what a *source* stated (`papers.author_provenance`), and 001C records what *this user decided*. Only the third creates a person, and only from an explicit action — there is no backfill, no import-time resolution and no background linker, so two historical papers sharing a valid ORCID stay two authors until someone links them. Identities never cross users: all five relationships are composite on `(user_id, …)` — alias → identity, link → identity, link → **paper**, and both merge endpoints — so a cross-user edge cannot be *stored*, not merely cannot be reached. A merge is a reversible edge (`source → target`), never a reassignment, so undo is one row deleted rather than a state reconstruction. Links are bound to `paper_id + author_index + author text`; a trigger clears a paper's links in the same transaction as any real change to its `authors` array, and the read layer independently refuses any link whose snapshot no longer matches. **Suggestions are advice, not authorization:** candidates come only from an exact valid ORCID or an exact 001A key and a contradicting ORCID suppresses the weaker name match, but the user can always search their own people and link or merge by hand — the contradiction is shown, both values stated, neither called wrong, and a two-step confirmation required. Because preferred names are deliberately not unique, every surface that names a person annotates it with that person's own evidence (ORCIDs, aliases, linked spellings, counts, a paper title), falling back to creation order when two are genuinely indistinguishable — never a UUID. There is no fuzzy, affiliation, coauthor or AI matching anywhere. A person's evidence is read account-wide, never from the current Analytics filter, so filtering changes what is *examined* and never what exists. Analytics counts one entity per resolved identity and one per unresolved 001A mention, and selections reconcile across link/merge/unmerge on read (`lib/authorSelection`) — internal entity keys are never displayed. **A failed identity read fails closed**: the hook distinguishes "not installed here" from "could not be read", so a real failure never masquerades as the benign 001A fallback — it is stated, nothing can be written against a graph nobody could read, and a last-known graph is kept on screen only while labelled as such.

- Internal `owner`/`manager` roles decoupled from the commercial plan (`internal_user_access` + read-only `get_current_user_access`), with an owner AI-quota exemption. Runtime authorization is **UUID/role-based, never email-based**.
- **AI model selection — foundation and server-side routing are live in Production; the Settings control is implemented in PR #268 and pending merge (AI-MODEL-SELECTION-001A + 001B + 001C, C33/C34).** The database foundation is applied to Production: a server-controlled `user_entitlements.ai_model_selection_enabled` capability flag, the `ai_model_catalog` allowlist (**live in Production with exactly Gemini 3.5 Flash and Gemini 3.6 Flash**; AI-MODEL-SELECTION-001D adds Gemini 3.7 Flash and Gemini 3.8 Flash in migration `20260903120000`, which is **in the repository and not applied to Production**, C35), a one-row-per-user `user_ai_preferences` table, a fail-closed `can_select_ai_model` on `get_current_user_access` (mirrored as `useCurrentUserAccess().canSelectAiModel`), and two `auth.uid()`-derived RPCs — `set_current_user_ai_model` / `clear_current_user_ai_model` — that are the only write path. **001B is the deployed runtime:** `analyze-paper` and `suggest-paper-organization` share one resolver, `_shared/aiModelSelection.ts`, which re-checks `can_select_ai_model` on **every** AI operation and routes the Gemini call to a valid saved preference. Not entitled, no preference, dormant preference after a downgrade, a retired model (`enabled = false`), an unsupported provider, or **any** failure to read that metadata all fall back to the system-default `GEMINI_MODEL` — fail-closed on the paid capability, without turning into a quota error, a refund or a failed request. `enabled = true, selectable = false` still honours an already-saved choice; requiring both is the setter's job. Only the model component of the provider URL changes: prompts, bodies, parsing, quota, refunds and the 90s/0-retry transport are untouched, both models share the one existing `GEMINI_API_KEY`, and `get-gemini-provider-quota` stays system-default observational monitoring. **001C adds the Settings control** (implemented in PR #268, pending merge/deployment): an **AI Model** section gated on `access.canSelectAiModel` alone — no plan name, email, role or browser-storage substitute — offering **Paperlume default** (a UI sentinel meaning *no saved row*, which calls the clear RPC and is never sent to the setter) plus the `enabled AND selectable` catalog rows from supported providers. Preference reads are `user_id`-scoped singletons and **every** write goes through the two RPCs; no frontend `INSERT`/`UPDATE`/`UPSERT`/`DELETE` touches either table, and opening Settings creates no row. Nothing is optimistic — the saved preference is refetched, only `saved = true` counts as success, and saving does not close the dialog. Dormant, non-selectable, disabled and missing saved models are each reported truthfully rather than auto-rewritten; a downgraded user can clear a dormant preference but not change it. Access loading/error and read failures all fail closed to no control, and the non-entitled state carries **no upgrade, checkout or pricing affordance**. Paperlume's system default is **Gemini 3.5 Flash**, resolved server-side from `GEMINI_MODEL` (C34), with 3.6 — and, once 001D rolls out, 3.7 and 3.8 — remaining explicit entitled choices and **no automatic failover** between them. **Adding a catalog model changes nothing about the default**, and 001D added none of it in code: the runtime and Settings surfaces hold no model list, so the two new models are a reviewed migration row plus tests and docs. **Neither 3.7 nor 3.8 has been called**, in Production or anywhere else. The saved preference **is** user-owned portable data, so it ships in the full account export from day one as the singleton `data/user_ai_preferences.json` (JSON `null` when unset). The global catalog is not exported.
- **Self-service account deletion** — **Account menu → Account → Danger zone** permanently deletes the signed-in account after the user types `DELETE MY ACCOUNT` exactly, re-validated server-side. It is a **hard** deletion: the privileged `delete-account` Edge Function derives the target **solely from the authenticated bearer token** (the client sends no user id), removes every Storage object under the account's `{userId}/` prefix **before** touching Auth, then calls `auth.admin.deleteUser(userId, false)`; database rows go with it through pre-existing `ON DELETE CASCADE` foreign keys. Storage-before-Auth is required and the operation is safe to retry. The full account export is the offered export-before-delete path.

- **Authenticated navigation split (PAPERLUME-PRIVACY-001C).** **Settings** holds application configuration only — the PubMed API key and the storage gauge. Account lifecycle lives in a separate **Account** dialog (Account data / export, then Danger zone / delete), opened from the **Account menu**: the sidebar's signed-in-email dropdown, which contains *Account*, *Privacy Policy* and, below a separator, *Sign out*. The Privacy Policy item is an in-app router link to `/privacy` — never the absolute canonical URL — so it resolves on localhost, on a Preview and in Production; the signed-out link in the `/auth` legal footer is unchanged, so the policy is discoverable both signed out and signed in. Opening the Account dialog from the narrow-screen drawer reuses the existing deferral: the action is parked and run from the Sheet's own `onCloseAutoFocus`, so a Dialog focus trap is never stacked on the Sheet's and the dialog records a connected opener. There is **no** `setTimeout` anywhere in that path.

### Responsive, mobile, and touch state

- The Dashboard has responsive smartphone controls: below `md` the navigation rail becomes a `Sheet` drawer, and the permanent mobile toolbar is compacted (Header + Search + Filters/More) so the paper table holds the majority of the viewport; secondary controls live in mobile overlays. Desktop composition is unchanged.
- Mobile searchable multi-selects render a shared touch-safe sheet: the heading is focused on open, the search field is **never** autofocused, and the option list is a plain scroll container sized from `window.visualViewport` so a software keyboard cannot bury it.
- **Coarse-pointer behavior is decoupled from the `<768px` layout breakpoint.** `(pointer: coarse)` describes how the user touches the screen; `useIsMobile()` (768px) describes how wide the screen is. A tablet legitimately gets the desktop composition *and* touch-safe focus. The 768px contract is unchanged and `useIsMobile()` remains its only JS reader.
- Reported tablet issues are addressed in the accepted implementation: Analytics is reachable at coarse-pointer tablet sizes (the controls region is an explicit bounded scroll owner) and touch hit targets meet size minimums.
- **Touch-safe initial focus is verified per surface, not claimed product-wide.** The surfaces owner testing has covered now decline Radix's text-field autofocus on a coarse pointer and focus their heading (or their popover panel) instead: the sidebar management dialogs, Save/Rename saved search, the Filters and Add Papers selectors, Analytics' Target Keywords/Authors selectors, Settings, Edit Project, Edit Tag, Add/Edit Synonym Group, Edit Paper, Edit Paper's Projects/Tags selectors, and the author identity manager. Tapping a field still focuses it, and a fine pointer keeps its existing autofocus everywhere. Surfaces outside that list have not been audited.
- Edit Paper bounds its shell to the **dynamic** viewport and gives its long form one deliberate scroll region (`overflow-y-auto` with `overscroll-contain` and `touch-pan-y`), so every section from Title to Save Changes is reachable by panning inside the dialog rather than by scrolling the page behind it. A `vh`-bounded shell is not reliable here, because `vh` resolves against the large viewport while browser chrome is on screen.
- Close-focus restoration is handled centrally in all three modal primitives — `DialogContent`, `SheetContent`, and `AlertDialogContent` — by capturing the opener on `onOpenAutoFocus` and restoring it on `onCloseAutoFocus`, because Radix otherwise restores through a trigger ref that is `null` for a controlled surface and drops focus on `<body>`. A consumer's own handlers still compose, and a consumer that calls `preventDefault()` wins. Confirmations that *do* have an `AlertDialogTrigger` (the exclusion-pool Clear controls) still restore their trigger correctly: the shared layer captures that trigger as the opener, and Radix's own trigger restore remains the fallback when no connected opener was captured. Where the opener is unmounted by the act of opening — the saved-search Delete/Update confirmations, launched from a dropdown item that closes with the menu — the call site names the persistent Presets trigger as its deliberate fallback; there is no global focus manager and no heuristic "nearest focusable" search.
- **Confirming a paper deletion is a handoff, not a restoration**, because the optimistic cache update unmounts the row and the Delete button that opened the confirmation. `PaperList` captures the deleted paper's neighbours by id *before* dispatching the delete, then moves focus to the **next** row's Delete button, falling back to the **previous** row when the last visible row was deleted, to whatever now occupies the same slot if both neighbours also vanished, and to the empty-state heading (`tabIndex={-1}`, never a Tab stop) when no row survives. Targets come from the `papers` array and refs registered per paper id — never a DOM search — and the handoff only claims focus that has nowhere to be, so a user who moved on during the asynchronous delete keeps their place. Dismissal is unchanged: Cancel and Escape never arm it.
- **Real-device iPhone/iPad verification remains a valuable validation source.** Chromium matching `(pointer: coarse)` proves which element receives focus and the geometry of hit targets; it cannot prove physical iOS software-keyboard or touch-pan behavior.
- This is responsive **web** usability only. **C7 still defers native/mobile packaging**, and none of it is a formal whole-product WCAG conformance audit.

## 4. Key files (orientation map)

| Path | Role |
|---|---|
| `src/pages/Dashboard.tsx` | Main page; orchestrates hooks and list UI |
| `src/hooks/usePapers.ts` | Papers infinite query + server filter/sort |
| `src/hooks/useFilterState.ts` | Filter/search state + search-mode routing |
| `src/lib/buildPapersQuery.ts` | PostgREST query builder for the read path |
| `src/hooks/useFilterPresets.ts` | Saved searches: schema, queries, mutations |
| `src/hooks/usePaperAnalysisActions.ts` | AI-analysis orchestration (single + bulk) |
| `src/hooks/useAttachments.ts` | Attachment upload/download/delete |
| `src/hooks/useCoarsePointer.ts` + `src/lib/columnWidths.ts` | Input-modality detection; single source of column width/clamp policy |
| `src/lib/accountExport/` + `src/hooks/useAccountExport.ts` | Full account export: category registry, S2-scoped reads, manifest, streaming ZIP |
| `src/hooks/useAccountDeletion.ts` + `src/components/settings/DeleteAccount*.tsx` | Account-deletion client: invocation, typed-phrase gate, session/cache cleanup |
| `supabase/functions/delete-account/` + `_shared/accountDeletion.ts` | Privileged boundary: auth, confirmation, Storage cleanup, hard Auth delete |
| `src/lib/importParsers.ts` | BibTeX / RIS / PubMed NBIB / EndNote tagged / CSV parsing |
| `src/components/papers/PubMedSearchPanel.tsx` + `src/hooks/usePubMedSearch.ts` | PubMed discovery: result presentation and the ephemeral query/page/selection state machine |
| `supabase/functions/search-pubmed/` + `_shared/pubmedSearch.ts` | Discovery boundary: auth, request bounds, NCBI ESearch/ESummary, order-preserving parsing |
| `supabase/functions/suggest-paper-organization/` | Suggestion boundary: auth, paper ownership, taxonomy read, provider allow-list + ephemeral refs, strict response parsing, AI-quota consume/refund |
| `extension/manifest.json` + `extension/src/detectPaperFromUrl.ts` | Chrome extension: the permission contract, and the URL classifier that composes the two application recognisers with no title fallback |
| `extension/src/classifyActiveTab.ts` + `extension/src/detectPaperFromMetadata.ts` | The URL-first / DOI-metadata-second order and why three of the four URL answers never touch the page; the four approved `<meta>` keys, the self-contained injected function, and the fail-closed ambiguity rule |
| `extension/src/paperLumeHandoff.ts` + `extension/src/popupView.ts` | The extension's only external origin and the URL it builds from a detection, and the popup behaviour that opens it — one press, one `chrome.tabs.create`, an in-flight latch, and a safe failure state |
| `src/pages/ExtensionImport.tsx` + `src/lib/extensionImportHandoff.ts` | The authenticated handoff route and its untrusted-URL parser; imports one identifier through the canonical importer after explicit confirmation |
| `scripts/lib/extension-package.mjs` + `e2e-extension/support/extensionHarness.ts` | The Chrome Web Store package contract asserted on the built artefact, and the real-Chromium harness that loads it — including how an extension ID is derived without the service worker Playwright's documentation assumes |
| `src/lib/safeReturnTo.ts` | The sign-in return target: one-route allowlist, destination rebuilt from validated parts so `returnTo` cannot become an open redirect |
| `src/hooks/useNormalizationConfig.ts` | The single assembly of the user's `NormalizationConfig`, shared by Dashboard and the handoff route |
| `src/components/papers/AssignOnImportSection.tsx` | The shared Projects/Tags assign-on-import selector used by Add Papers and the handoff route |
| `src/lib/pubmedIdentifiers.ts` + `src/lib/doiIdentifiers.ts` | Structural PubMed/DOI recognition and canonical-URL construction; parity-pinned to the Edge classifier |
| `src/integrations/supabase/client.ts` | Supabase client (env fail-fast via `src/lib/clientEnv.ts`) |
| `supabase/migrations/` | Full schema, RLS, and RPC definitions (chronological) |
| `scripts/e2e-local.mjs` | The whole local E2E / DB-test lifecycle: stack, seed, guards, teardown |

## 5. What is deployed now (Production)

Linked Supabase project ref: **`lioxtgiputfniqbktcsz`**. Verify before trusting any claim below.

- **Database:** every tracked migration in `supabase/migrations/` is applied to the linked project **through and including** `20260902120000_add_ai_model_selection_foundation.sql` (AI-MODEL-SELECTION-001A, C33). The repository additionally carries `20260903120000_add_gemini_37_38_catalog_models.sql` (AI-MODEL-SELECTION-001D, C35), which is **not applied to Production** — so Production's catalog still holds exactly the two 001A rows. Everything through 001A — so the model-selection entitlement flag, `ai_model_catalog`, `user_ai_preferences`, `can_select_ai_model` and the two write RPCs are **live in Production**, and the 001B runtime routes on them (§13 item 1). The AUTHOR-IDENTITY-RESOLUTION-001C identity schema is likewise **live**: four user-owned tables, nine hardened functions, the stale-link trigger, and the `papers (user_id, id)` UNIQUE constraint the identity links depend on. **No historical identity backfill occurred** — applying the migration created no identity row, so an author mention stays unresolved until a user resolves it, and two papers sharing a valid ORCID remain two authors. **The live ledger is authoritative**; read it back with `supabase migration list --linked` rather than trusting a number written here.
- **Edge Functions — all six in `supabase/functions/` are deployed and ACTIVE:** `fetch-paper-metadata`, `analyze-paper`, `get-gemini-provider-quota`, `delete-account`, `search-pubmed`, and `suggest-paper-organization`. Deployed versions are volatile; read them back (`supabase functions list --project-ref <project-ref>`) instead of quoting a snapshot.
  - **`suggest-paper-organization` is deployed and live**, and its contract was verified against Production on 2026-08-23. It is advisory and caller-authenticated, uses no elevated key, and needed no new secret and no migration. **Edit Paper calls it in Production** (`001B`), against this exact already-deployed contract — that PR touched no file under `supabase/`, so it needed no Edge deployment of its own, and `001B` Production acceptance completed on 2026-08-24 with the deployed function unchanged. A future change to its contract, or to any shared module in its bundle, must be deployed **before** the frontend depending on it merges — [deployment.md](deployment.md) §7c holds that rule and the reusable verification.
  - **`search-pubmed` is deployed and live**, backing the PubMed Search tab. It is read-only, caller-authenticated, uses no elevated key, and needed no new secret and no migration. Because the tab is useless without it, a future change to its contract must be deployed **before** the frontend depending on it merges — [deployment.md](deployment.md) §7b holds that rule and the non-destructive verification.
  - `get-gemini-provider-quota` is **deployed but intentionally unused**: its manager-facing dashboard is **deferred under C29**, no frontend calls or renders it, and it remains as deferred infrastructure.
  - `delete-account` is **deployed and live**, and the Account → Danger zone flow calls it in Production.
- All six authenticate the caller with the caller's JWT via in-function `auth.getUser()`; `verify_jwt = false` at the gateway is intentional, and CORS `*` is an accepted decision under bearer-token auth. Only `delete-account` uses an elevated key, and only after the caller is authenticated.
- **Edge Function deploys are separate from frontend deploys.** A GitHub merge alone never updates a function.
- **Frontend:** Vercel Git integration. Every PR head gets a Preview deployment; every merge to `main` produces a **Production** deployment on `app.paperlume.app`. There is no manual promote step.
- **Auth email:** Supabase Auth Custom SMTP routes through Resend on `auth.paperlume.app`.
- Schema reconciliation is complete: local and linked `public`-schema generated types are semantically identical, and the committed types match the linked output. Inventory and canonical decisions C20–C26: [schema-reconciliation.md](schema-reconciliation.md).

Operational procedures, secrets, migration sequence, smoke checklists: [deployment.md](deployment.md).

## 6. Commercial and entitlement state

**Implemented and live:**

- **Entitlement/usage schema** (`20260521010000`): `user_entitlements`, `subscriptions`, `subscription_events`, `usage_counters`, `usage_credits`; `handle_new_user()` seeds a Free entitlement on signup. `subscriptions` / `subscription_events` / `usage_counters` are intentionally deny-all under RLS (server-only).
- **AI quota enforcement** (`20260521020000`): `consume_ai_quota` / `refund_ai_quota` SECURITY DEFINER RPCs with S1 guards; `analyze-paper` consumes a unit **before** calling Gemini, refunds best-effort on provider failure, and returns a structured **HTTP 402** when quota is unavailable.
- **Storage quota enforcement** (`20260521030000`): `user_storage_usage` plus atomic check-and-consume / refund triggers on `paper_attachments`.
- The schema is **provider-neutral**; no billing provider is wired into it.

**Not implemented — do not describe these as existing:**

- Paddle integration: checkout, webhook ingestion, subscription synchronization, customer portal.
- Billing / paywall / upgrade UI of any kind. The AI-quota and storage indicators are **transparency only** and expose no upgrade, checkout, or purchase path.
- Free-tier feature gating of the Synonyms and Exclusions pools (launch blocker per [quotas-and-pricing.md](quotas-and-pricing.md)).
- Terms of Service, AI-disclosure page, support page, marketing site, paid launch. **The product is not commercially launched.** (The **Privacy Policy** is the one legal page that does exist — public at `/privacy`.)

**Direction (decided, not started):** Merchant-of-Record-first billing (C17); **Paddle** selected for the web MVP (C18), gated on owner-side Paddle setup; Free → Pro two-tier MVP with baselines in [quotas-and-pricing.md](quotas-and-pricing.md) (C9–C11); web-first, mobile deferred (C7); Paperlume brand + domain (C19).

## 7. Testing and CI model

- **There are two required merge gates: `validate` and `db-tests`.** `main` protection requires both — strict/up-to-date, administrators included, **zero** required human approvals, unresolved conversations block the merge, force-push and branch deletion disabled, regular merge commits allowed, Vercel **not** a required check, no repository rulesets.
  - **`Validate / validate`** — `.github/workflows/validate.yml` runs `npm ci`, lint, `npm run typecheck`, Vitest, the web production build and the **Chrome extension production build** on Node 22 for every pull request to `main` and every push to `main`. Both build targets are covered by the one required check, so neither can become an application CI does not execute.
  - **`DB Tests / db-tests`** — became required on **2026-08-16** when the owner resolved decision **D5** to `REQUIRE_DB_TESTS`. A red `db-tests` blocks merge; that is intended, not a reason to roll the gate back.
- **`E2E (local) / e2e-local` and `Extension / extension` run on eligible PRs but are NOT required checks.** All four workflows run on same-repository pull requests, skip fork-origin PRs before any execution, use `contents: read` only, and need **no** GitHub secret, variable, or Environment. `Validate`, `DB Tests` and `Extension` additionally run on every push to `main`; **`e2e-local` does not** — CI-E2E-EXECUTION-POLICY-001 removed its post-merge `push` trigger, because it re-ran the identical already-green candidate for another ~19 minutes, and replaced it with a **daily 02:17 UTC schedule** (`cron: '17 2 * * *'`) that GitHub runs from the latest commit on the default branch. That scheduled lane is a drift/health check, not a gate; the pre-merge PR run is unchanged. `Extension` is additionally **path-scoped on pull requests only**: a PR triggers it on the extension, its build config, its packaging, its browser lane, the lockfile — and on the shared web modules it bundles (`src/lib/pubmedIdentifiers.ts`, `src/lib/doiIdentifiers.ts`, `src/lib/extensionImportHandoff.ts`), because a change there is an extension change even though no file under `extension/` moved. Unrelated application PRs therefore do not pay for a browser download. Its `main`-push trigger carries **no** path filter, so every push to `main` runs it. `e2e-local` was deliberately **not** promoted under D5; reconsider it only against the recorded trigger — [decisions-and-triggers.md](decisions-and-triggers.md).
- **Required contexts are bare job names.** Branch protection matches the emitted check name — `validate` and `db-tests`, each bound to the GitHub Actions app — **not** the `Workflow / job` labels shown in the GitHub UI. Use the bare names in any protection change.
- **Fork-origin PRs would produce a vacuous green** on the required `db-tests` gate: the same-repository job condition makes it skip and report success. Harmless under today's same-repository-only model, but it must be resolved before fork contributions are accepted.
- **Local-first Playwright is the supported safe lifecycle.** `npm run test:e2e:local` starts an **ephemeral local Supabase stack**, replays every tracked migration, applies a deterministic local-only seed, and runs the suite behind a **two-layer fail-closed** Production/remote guard (Layer 2 runs in the browser before any credential read or fill). Raw key-bearing stack-startup output is suppressed.
  - A bare **`npm run test:e2e` deliberately fails closed** without an explicit local backend contract. **Production-backed Playwright is prohibited by the merged guard** and is not an available path.
  - The `E2E (local)` workflow reuses this same local-first lifecycle. It never contacts Production or any cloud project.
  - **External-metadata import order is covered deterministically.** The `import-order` spec is part of this lane: Playwright fulfils the one `fetch-paper-metadata` request so the metadata is fixed, while the real Add Papers UI, bulk-insert RPC, `insert_order`, refetch, table and refresh persistence all run for real. It needs no live PubMed/Crossref, no served Edge Function and no provider credential, and it deletes the papers it imports.
  - **In-app PubMed discovery is covered deterministically.** The `pubmed-search` spec is part of this lane: Playwright fulfils the `search-pubmed` and `fetch-paper-metadata` requests, while the real four-tab dialog, result geometry, selection, `bulkImportPapers`, bulk-insert RPC, duplicate handling and Project/Tag assignment all run for real. It asserts at the metadata boundary that the selected PMIDs — and nothing else — reach the canonical importer, so a future change that inserts search summaries directly fails it. It needs no live NCBI egress and deletes every paper it imports.
  - **There is no hosted staging environment.** The accepted path is local-first; no cloud test project, secret, or Environment is provisioned, and selecting one is optional and unselected.
- **Database-layer tests exist.** `supabase/tests/database/` holds pgTAP suites covering core and relational RLS isolation, RPC caller scope and grants, storage and quota, duplicate-merge success, publication-type provenance, function `search_path` hardening, and account-deletion cascade — plus a preserved framework-free verification file. `npm run test:db:local` runs them all on an ephemeral local stack with a catalog-fingerprint baseline, an expected-failure negative control, a true-concurrency AI-quota probe, and fail-closed residue/teardown inspection. The hosted `DB Tests` workflow is a thin wrapper around that same lifecycle.
- **Local pre-push baseline:** `npm run lint`, `npm run typecheck`, `npm test`, `npm run build` — plus `npm run test:extension:browser` and `npm run package:extension` when extension code changes (both build it first), `npm run test:e2e:local` when UI behavior changes, and `npm run test:db:local` when database code changes.
- **TypeScript:** the root solution-style `tsconfig.json` has an empty file set, so a bare `npx tsc --noEmit` checks **no application files** and is **not valid evidence**. Use `npm run typecheck`, which runs `typecheck:app`, `typecheck:node` and `typecheck:extension`.
- **What does not exist:** a required E2E gate, and Edge Function tests executed by a **Deno** runtime. `delete-account`, `search-pubmed` and `suggest-paper-organization` are the partial exceptions — each keeps its whole request path in a runtime-agnostic `handler.ts` plus pure sibling/`_shared` modules, so the real handler is covered by Vitest (CORS-before-auth, method gating, `getUser()`, request bounds, retry/backoff, and the rule that no identity is read from the body). `suggest-paper-organization` additionally proves what must *not* happen: its fake Supabase client throws on any `insert`/`update`/`upsert`/`delete`, and its privacy tests assert on the exact serialized Gemini request rather than on an intermediate object. `delete-account` is additionally served for real under the local lifecycle; `search-pubmed` is not, because its E2E stubs at the browser boundary to keep CI off the live NCBI network. Other Edge Function validation remains manual post-deploy smoke (established metadata case: PMID `41912805`).
- **Do not cite exact test counts here** — run the suites for current numbers.

## 8. Dependency security

Authoritative current state, remediation policy, and re-evaluation triggers: [dependency-security.md](dependency-security.md). `npm audit` (full and `--omit=dev`) on the current checkout is the live source — **do not treat a count written in any document as current**.

- **All five dependency clusters are complete, and `npm audit` is at zero in both the full and the production-only graph.** In Clusters 1–4 the implementation delta was confined to `package-lock.json`; Cluster 5 additionally changed `package.json` and six import specifiers, because clearing it required crossing a major version.
- **React Router Cluster 5 is implemented**: `react-router` **7.18.2** is now the single declared Router dependency, and `react-router-dom` and `@remix-run/router` have left the graph entirely. It was a direct-package migration of import provenance only — **React and ReactDOM stay 18.3.1**, the route tree and every navigation target are unchanged, and no future flag was enabled. React Router v8 remains **out of scope** (it requires React 19.2.7+ and Node 22.22+).
- The **`nanoid` high advisory remains remediated** by a name-scoped, lockfile-only update within its parent's existing semver range.
- **Zero is a measurement, not a guarantee.** Advisory databases move, so a new finding can appear without any repository change. Never restate a count from memory — run `npm audit` and read [dependency-security.md](dependency-security.md).

## 9. Active decisions and constraints — do not casually reopen

Authoritative record with rationale and re-evaluation triggers: [decisions-and-triggers.md](decisions-and-triggers.md). Owner gates and unlock order: [owner-decisions.md](owner-decisions.md).

- **Duplicate detection is PMID/DOI only.** Do not propose fuzzy or title-based matching.
- **PubMed Search discovers PMIDs; the existing importer imports them (C31).** A search result is transient display data and must never become persisted paper metadata, a second insert payload, or a second duplicate check.
- **Title-based import** auto-selects the first PubMed/Crossref match; the accepted mitigation is the static warning in the Add Papers dialog. Do not propose per-paper preview/confirmation flows.
- **CORS `*` on the Edge Functions is intentional** under header-based bearer-token auth. Revisit only if auth becomes cookie-based.
- **No real-Gemini / AI Playwright E2E** — rejected as non-deterministic; the AI path is covered by mocked Vitest tests plus manual smoke. `e2e/ai-organization-suggestions.spec.ts` is consistent with that rule rather than an exception to it: it fulfils `/functions/v1/suggest-paper-organization` at the **browser** boundary with deterministic contract data, so no Edge Function is served, no Gemini request is made and no AI quota is spent — and it fails if the page reaches a Google host at all. What it proves is the integration a mocked unit test cannot: that the *unsaved* draft is what actually leaves the browser, and that accepting a suggestion really does not reach the database until Save.
- **Read-path architecture is stable** (server-side filter/sort/paginate, keyword RPCs, on-demand abstracts, cache-key split, select-all-IDs). Changing it requires new evidence.
- **The 768px layout contract is fixed**, and `(pointer: coarse)` is a separate axis from it. Do not merge the two.
- **Deferred with documented triggers:** Phase C DB optimization (jsonb GIN indexes, RPC rewrites), unused-index cleanup, write-path optimization, Hebrew/RTL (C15).

### Owner-deferred work — NOT active engineering backlog

These are paused by owner decision. Starting any of them requires a **new explicit owner decision**, not an engineering judgement call.

| Decision | Deferred | Re-evaluation trigger |
|---|---|---|
| **C27** | Commercialization and public-launch implementation (Paddle, checkout, webhooks, paywalls, billing, launch execution) | Owner explicitly decides to return to commercialization |
| **C29** | Automatic Gemini provider-quota dashboard/monitoring; Google Cloud billing stays disabled; usage checked manually via Google AI Studio | Commercialization decision **plus** revalidation of the Monitoring metric families |
| **C30** | Supabase Auth leaked-password protection (a Pro-plan feature; the org is on Free) | Before commercialization / paid launch, or earlier if the org moves to Pro or Supabase changes the feature gate |
| **C7** | Native/mobile and app-store packaging | Post-web-launch roadmap phase |

Owner-action blockers that gate the paused C27 work — Paddle Sandbox setup, marketing-site provider and legal-page URLs (C16), business email, monitoring/error-tracking provider — are listed in [owner-decisions.md](owner-decisions.md). They are future-facing and are **not** the active next task.

## 10. Open engineering backlog

Meaningful open items. This is a pointer list, not a backlog database — none of it is auto-selected.

- **Optional hosted staging** remains unselected; local-first is the accepted path.

## 11. Before selecting the next task

- Every PFA candidate (C01–C09) is **selected or closed for its accepted scope**. Post-PFA work has been responsive/mobile/touch remediation, which is **not** a reopening of PFA-C09.
- **No completion selects a next objective.** A new explicit owner decision is required before further feature work starts, and owner-deferred items in §9 are not eligible by default.
- The required CI and branch-protection baseline in §7 is **mandatory for every PR**, feature or remediation.
- Any change under `supabase/` needs the matching deploy step from [deployment.md](deployment.md) — a merge alone deploys neither migrations nor Edge Functions.
- Verify volatile facts (ledger, deployed function versions, `npm audit`, branch protection) against the live source before restating them.

## 12. Authoritative documents

| Document | Authority |
|---|---|
| [README.md](../README.md) | Concise public/developer entry point |
| [documentation-policy.md](documentation-policy.md) | Documentation rules, incl. this file's line budget |
| [decisions-and-triggers.md](decisions-and-triggers.md) | Durable decisions (C-numbers, S1/S2) + re-evaluation triggers |
| [owner-decisions.md](owner-decisions.md) | Owner gates, blockers, implementation unlock order |
| [deployment.md](deployment.md) | Deployment runbook, env vars, domains, smoke checklists |
| [architecture-read-path.md](architecture-read-path.md) | Read-path architecture detail |
| [dependency-security.md](dependency-security.md) | npm dependency-security posture, remediation policy and triggers |
| [schema-reconciliation.md](schema-reconciliation.md) | Schema drift inventory, canonical decisions C20–C26 |
| [commercial-architecture.md](commercial-architecture.md) | Entitlement/billing architecture (planning) |
| [quotas-and-pricing.md](quotas-and-pricing.md) | Plan structure, MVP baselines, instrumentation (planning) |
| [store-launch-checklist.md](store-launch-checklist.md) | Launch/store readiness (mobile deferred; planning) |
| [migration-history.md](migration-history.md) | Historical chronology — **history, not current state** |
| [product-feature-audit.md](product-feature-audit.md) | Point-in-time capability audit and owner decision packet — **historical, not an evergreen authority** |
| [pfa-c03-staging-and-security-test-plan.md](pfa-c03-staging-and-security-test-plan.md) | PFA-C03 contract and evidence record (complete); D4 decision and resolution; §16 D5 audit evidence (frozen) and §17 D5 activation record |

## 13. Recent material changes

At most 5 items; remove the oldest when adding.

1. **AI model selection is live on the server; the Settings UI is implemented in PR #268 and pending merge (AI-MODEL-SELECTION-001A + 001B + 001C, C33/C34).** **001A foundation — applied to Supabase Production.** Migration `20260902120000_add_ai_model_selection_foundation.sql` is applied, so Production carries the server-controlled `user_entitlements.ai_model_selection_enabled` capability flag, the `ai_model_catalog` allowlist, the one-row-per-user `user_ai_preferences` table, the additive fail-closed `can_select_ai_model` on `get_current_user_access`, and the two write RPCs `set_current_user_ai_model(text)` / `clear_current_user_ai_model()`. The Production catalog still holds **exactly two rows** — `google/gemini-3.5-flash` and `google/gemini-3.6-flash`. **001D catalog expansion — in the repository, NOT applied to Production.** Migration `20260903120000_add_gemini_37_38_catalog_models.sql` adds `google/gemini-3.7-flash` (**Gemini 3.7 Flash**, provider model `gemini-3.7-flash`, `sort_order` 30) and `google/gemini-3.8-flash` (**Gemini 3.8 Flash**, provider model `gemini-3.8-flash`, `sort_order` 40), both `enabled` and `selectable`, taking the catalog to four rows in order 3.5 / 3.6 / 3.7 / 3.8 (**C35**). It touches the 3.5 and 3.6 rows not at all, rewrites **no** user preference — including the owner's saved `google/gemini-3.6-flash` — and its fail-closed self-check refuses to commit if any pre-existing catalog, preference or entitlement row was written. It required **no** runtime or frontend production-code change, because the database catalog is the allowlist; all four models use the existing Google adapter and the same existing `GEMINI_API_KEY`. Google documents both new models as **stable/GA** with structured outputs and **no announced shutdown date** (released 2026-08-13 and 2026-09-02). **Neither has been provider-verified anywhere** — the 001D work made no Gemini request — so a Production provider-path canary for 3.7 and 3.8 is pending until after the migration is rolled out. **The gate is the explicit entitlement flag, never a `plan === 'pro'` comparison and never an email**, so an internal/test grant is one server-side entitlement write with no client change. Both RPCs derive the caller from `auth.uid()` and take **no user-id parameter at all**, so writing another user's preference is unexpressible rather than merely guarded; the setter additionally re-checks status and the catalog allowlist (`enabled` **and** `selectable`). Both tables are `ENABLE` + `FORCE` RLS with `authenticated` holding **SELECT only** and `anon` / `service_role` holding **nothing** — direct preference writes are denied by grant *and* policy, and the revoked `service_role` means even an elevated key cannot read or rewrite a saved model. **Absence of a preference row remains the system-default state**, so nothing was backfilled and `handle_new_user` was not extended; a saved preference stays **dormant** after downgrade rather than being deleted, and runtime authorization is re-checked before it is ever honoured. Clearing deliberately does **not** require the entitlement. There is **no per-model API key** — both models are served by the same existing server-side `GEMINI_API_KEY`, and the catalog stores no credential. **001B runtime routing — deployed to Production** for `analyze-paper` and `suggest-paper-organization`. Both share one resolver, `_shared/aiModelSelection.ts`, that re-checks `can_select_ai_model` on every AI operation through the caller-authenticated client, reads the caller's own `user_ai_preferences` row (`.eq("user_id", …)` on top of the SELECT-own policy) and resolves it through `ai_model_catalog`. A preference is honoured only when the row's `id` matches, `enabled` is `true` and `provider` is `google` — `selectable` is deliberately **not** required, so a model closed to new choices keeps working for whoever already saved it, while `enabled = false` retires it. Not entitled, no preference, and **every** metadata read failure fall back to the global system default without becoming a 402, a refund or a failed request. The database catalog is the allowlist: no TypeScript model list exists. Only the model component of the Gemini URL varies — request bodies, prompts, parsing, quota and refund semantics, the shared Gemini Auth key and the **90-second / zero-retry** transport are all unchanged. **Current Production system default: `gemini-3.5-flash`** (C34), controlled server-side through the `GEMINI_MODEL` environment configuration rather than by any application code or client bundle. **Gemini 3.6 remains `enabled` and `selectable`** as an explicit choice for an entitled user — it is a choice, not a retired model — and there is **no automatic failover** between explicitly selected models. **Owner-verified Production canary on the 3.5 default path:** one **Suggest Projects & Tags** — PASS, ~17s, sensible result, routing log `source=system_default provider=google model=gemini-3.5-flash`, Gemini 3.5 request counter **+1**; and one **Analyze Paper** — PASS, ~25s, sensible result, the same routing log, counter **+1**. The 3.6 provider failures seen in that window were **operational evidence only and are explicitly not an official Google outage finding**. **001C Settings UI — implemented in PR #268 and pending merge/deployment at the time of this entry** (no migration, no `supabase/functions/**` change): an **AI Model** section gated on `canSelectAiModel` alone, a **Paperlume default** sentinel meaning *no saved row* that calls `clear_current_user_ai_model()` and is never passed to the setter, catalog-driven selectable choices filtered to `enabled AND selectable` and to providers the UI can route to, `user_id`-scoped singleton reads, RPC-only writes, truthful handling of dormant / non-selectable / disabled / missing saved models, fail-closed behaviour whenever entitlement or the reads are unknown, and **no billing, upgrade or checkout affordance**. **Still pending Production verification:** the explicit user-preference routing path — a real runtime log of `source=user_preference provider=google model=gemini-3.6-flash` — which requires the Settings UI to be live and the owner to intentionally select Gemini 3.6. The routing logic itself is already covered by repository tests; what remains is the Production provider-path canary. The saved preference is user-owned portable data and ships in the full account export as the singleton `data/user_ai_preferences.json` (JSON `null` when unset); the global `ai_model_catalog` stays out of the archive as Paperlume product metadata. Anthropic / OpenAI are **not implemented** — `provider` is left unconstrained so each is later a seed row plus a runtime adapter, but each still needs explicit provider, privacy, cost and runtime-adapter acceptance first. Gemini 3.7 and 3.8 needed no adapter work precisely because they are the same provider and the same Gemini API family (**C35**); they still went through explicit provider acceptance before a row was written.
2. **Chrome Web Store draft populated, audited and reconciled (CHROME-EXTENSION-IMPORT-001E3C / 001E3D) — the DRAFT item is now complete and shows zero live blockers; nothing submitted, nothing published, no product behaviour changed.** Under owner authorization limited to **draft mutations only**, `001E3C` populated and saved all four owner-facing Dashboard pages, and `001E3D` then re-read every page **read-only** and changed nothing. **Store Listing:** the package-derived title (`PaperLume`) and summary were left untouched — the summary has no editable field, so changing it would need a new package upload; the detailed description was entered from listing doc §4 as 2,602 characters of plain text; **Category `Workflow & Planning`** and **Language `English (United States)`** were selected (the language moved off the pre-set plain `English`); the required 128x128 store icon and **three** screenshots were uploaded in the intended PubMed / DOI / unsupported order; promo video, small tile, marquee tile, Homepage, Support and Official URL were all deliberately left empty, and Mature content left Off. **Privacy:** single purpose (534 chars) and both permission justifications were entered from the pre-measured short forms (**981** and **996** characters, inside the live 1,000 cap) and verified byte-identical after save; **the form's factually wrong `Yes, I am using remote code` default was corrected to `No`, with no justification stored**, and it held across save and reload; disclosure answers were entered **unchanged and unsoftened** — **Web history = Yes**, **Website content = Yes**, the other seven **No** — all three certifications certified, privacy URL exact. **A live-form trap is recorded so it is not re-litigated:** after selecting `No`, the remote-code justification field stays visible and keeps a static `required` attribute — as do all four textareas on that page — so **the DOM attribute is not the requiredness signal**; the `*` marker is, and the Dashboard's own blocker panel never demanded that field. **Test instructions:** the reviewer credentials were typed **by the owner directly into the live form**; verification was limited to field non-emptiness and length, no value was ever read, echoed, logged, screenshotted or stored, and **no credential exists anywhere in this repository**. **Distribution:** `Free of charge`, visibility **`Unlisted`** (not the displayed `Public` default), **`All regions`** plus the companion `All unlisted regions`, 155/155 controls checked. **Package untouched** — still `0.1.0`, `activeTab` + `scripting`, no Verified CRX opt-in, public key never viewed. **The one gate only the live Dashboard could reveal:** publishing also required a publisher **contact email**, both provided and **verified**, on the Settings page — two blockers that stopped publication but never draft saving. The owner completed both manually and `001E3D` confirmed the verified state; **the address is deliberately not recorded in the repository**, since Google displays it publicly in connection with the item. **The blocker panel went from nine items to zero and `Submit for review` from disabled to enabled** — which is recorded precisely as *the Dashboard exposing no known pre-submission completeness blocker*, and explicitly **not** as Google approving the extension, a prediction that review will pass, or authorization to submit. The trader-disclosure policy was re-read first-party and is **materially unchanged** (purpose-based definitions verbatim, developer self-declaration responsibility intact); the Dashboard remains **Non-trader**, still recorded as the owner's own self-declaration rather than a Google finding. The standing signed-out Production `/privacy` check passed again. **The item remains `Draft`, is NOT submitted and is NOT published; submission requires a separate explicit owner authorization.** **Documentation-only repository change:** no source, extension, test, workflow, schema, migration, Edge Function, Supabase, Vercel or legal-copy change; Production read-only.
3. **Chrome Web Store reviewer account provisioned and verified; publisher declaration frozen as Non-trader (CWS-REVIEWER-ACCOUNT-DOC-CLOSURE-001) — documentation-only; nothing entered into the Store, nothing submitted, nothing published.** Two long-open pre-submission gates closed on **2026-08-30**, and neither closure touched the Chrome Web Store or changed application behaviour. **The dedicated low-privilege reviewer account now exists in PaperLume Production and was verified rather than assumed:** exactly one Auth user and one confirmed email identity, password sign-in succeeded with a matching UUID and the session was discarded afterwards, one `profiles` row, one ordinary **Free** `user_entitlements` row (active, 1500 papers, 500 MB storage, 15 lifetime AI requests, 0 monthly, premium taxonomy and labs/team both off), one lifetime `ai_analysis` counter — and **zero** rows for internal access, subscriptions, subscription events and usage credits, with no billing identifiers, no owner/manager/admin privilege and no quota exemption. Provisioning used the supported Supabase Auth Admin `createUser` path with `email_confirm: true` (**no direct Auth-table SQL**), and the entire Production mutation was `+1` each to `auth.users`, `auth.identities`, `profiles`, `user_entitlements` and `usage_counters`, with every other audited table unchanged. **The account holds no seeded research or library content and no reviewer fixture is required** — 0 papers, projects, tags, attachments and filter presets; it does of course hold the ordinary account and authentication records a functioning identity needs. `src/pages/ExtensionImport.tsx` was re-read to establish that rather than assumed: Project and Tag selection is *optional*, the confirm control's disabled state depends only on the import phase and the context load and **never on the selections**, and the taxonomy section renders nothing at all for a user who has neither — so the canonical import completes with zero assignments. The empty-library account is the *preferable* state, not a shortcut: it minimises Production test data, shows the reviewer an ordinary new-user baseline, and demonstrates that assignment is genuinely optional. **The credentials exist, are owner-held outside Git and chat, and ~~have not been entered into the Chrome Web Store~~ were entered into Google's confidential `Test instructions` fields on 2026-08-30 under `001E3C` (item 1 above)**; they live only in those fields, and never in Git, a PR description, a report or chat. **Separately, the owner explicitly froze the publisher declaration as Non-trader for the current beta submission**, superseding the documentation that still treated the selection as an unresolved owner decision for this submission. That is recorded as **the owner's own self-declaration — which is exactly what Chrome asks for — and not as a Google finding, a legal opinion, or a conclusion this repository reached.** Two non-reasons are stated explicitly: being pre-commercial does not on its own make an account Non-trader, and not wanting to publish a home address is an owner concern rather than Chrome's trader test. **The safeguard was narrowed, not deleted** — immediately before the actual submission action the then-current trader-disclosure policy is re-read and the Dashboard declaration re-checked against the owner's intent, and it must not be silently changed during Store entry. The earlier compliance uncertainty is preserved as chronology rather than erased. ~~**Still unresolved owner decisions: Category, Language, visibility and regions** — the Distribution form's displayed `Public`/all-regions state remains a default, not a decision.~~ **Superseded 2026-08-30 by `001E3C` (item 1 above): all four were decided by the owner and entered — `Workflow & Planning`, `English (United States)`, `Unlisted` and `All regions`. The default/decision caution held: the visibility chosen (`Unlisted`) is not the `Public` default that was displayed.** **Documentation-only:** no source, extension, test, workflow, schema, Edge Function, Supabase, Vercel or legal-copy change; Production was read-only; no Store form was accessed, populated or saved; no submission; no publication.
4. **Live Chrome Web Store draft reconciled (CHROME-EXTENSION-IMPORT-001E3A / 001E3B) — one DRAFT Store item exists and the 0.1.0 package is uploaded; nothing submitted, nothing published, no product behaviour changed.** Under owner authorization limited to exactly two external mutations, `001E3A` created **one** PaperLume Chrome Web Store draft item — **`cfanjbamcemoeglgkpbidnclkomaocmo`** — and uploaded the locally validated release candidate (`release/paperlume-extension-0.1.0-rc.zip`, **15788 bytes**, SHA-256 `0feb935d914af2141c41aa129bf211cf08492a5d4ccb5e169bab8afb9f9c4634`, eight-entry inventory, manifest v3, permissions exactly `activeTab` + `scripting`, zero host permissions, no shipping `key`, no remote code; pre-upload validation green at **395/395** unit and **30/30** real-Chromium tests). The Store shows version `0.1.0`, item type Extension, a derived `main.crx`, and *"This item is not published yet"*. **The Dashboard does not expose the uploaded ZIP's SHA-256, so that hash is local pre-upload provenance and is not a Google attestation.** `001E3B` then reconciled the repository to the live forms, read-only. **Both long-open listing questions are resolved by observation rather than guessed:** the **promotional video is OPTIONAL** (live field `Global promo video`, no required marker), which retires a documentation conflict three first-party Google pages still disagree about; and the **store icon IS a separate REQUIRED 128×128 upload** (`Store icon *`), which vindicates keeping `assets/store/store-icon-128.png` as a prepared candidate. **Three stale claims were corrected:** the small promo tile is **optional**, not mandatory; the Store `Support URL` is **optional**, so Google does not compel the support surface that C16 governs on its own product-quality reasoning (C16 is unchanged); and the summary is **package-derived with no editable Dashboard field observed**, so changing it would require a new package upload — the shipped summary is deliberately kept and `extension/manifest.json` is not edited. **Two defects were caught that only the live form could reveal.** The untouched Privacy form displayed **`Yes, I am using remote code`** — factually wrong for a self-contained package with no remote JS or Wasm, no external import, no `eval`, no `new Function`, and an injected function bundled inside `popup.js` — so the intended answer is **locked to `No`** with a prominent pre-save warning and no justification text. And both permission justifications **exceed the live 1,000-character cap** (1,071 and 1,470); measured 981- and 996-character variants are drafted that preserve every substantive claim. The **`activeTab` lifetime wording was corrected** against re-read first-party documentation: the grant **survives same-origin navigation** and is revoked on a **cross-origin** navigation or tab close, not simply "when they navigate away" — a less flattering and more accurate claim. Disclosure answers are **unchanged and not softened**: **Web history = Yes**, **Website content = Yes**, all **three** certifications to be certified, `Privacy policy URL*` required. A **new pre-submission gate** is recorded: the Dashboard's separate **Test instructions** page needs a **dedicated low-privilege reviewer account**, because the extension is inspectable signed out but `Continue in PaperLume` → import is not — credentials go only into that form, never into Git, a PR, a report or chat. **Distribution defaults are not decisions:** the form displays `Public` and all regions, and that is explicitly recorded as the form's default state, not owner authorization to publish worldwide. Publisher registration is **complete** and the **$5 fee is paid**. Classification is **currently configured Non-trader — an observed Dashboard setting, not a confirmed classification**: Chrome's test is purpose-based (*"acting for purposes relating to his trade, business, craft or profession"*), **not** payment-based, so being pre-commercial is not on its own a basis for the declaration — and the published Privacy Policy describes PaperLume as operated by an individual **sole proprietor** under the business name **MutriSport** while also calling it a pre-commercial beta. **Confirming the declaration is a gate before any Store submission, not before a future paid launch**; if Trader applies, verification and the required public trader information must be completed first. **Documentation-only:** no source, extension, test, workflow, schema, Edge Function, Supabase, Vercel or legal-copy change; no Store field populated or saved; no asset uploaded; no submission; no publication. Historical no-item/no-Dashboard snapshots are preserved with dated supersession pointers rather than rewritten. **Superseded in part 2026-08-30 by `CWS-REVIEWER-ACCOUNT-DOC-CLOSURE-001` — item 1 above:** the reviewer account recorded here as a new open gate has since been provisioned and verified, **no reviewer fixture is required**, and the trader/non-trader classification is **no longer an unresolved owner decision for this submission** — the owner froze it as **Non-trader**, with a pre-submission consistency and policy re-check retained.
5. **DOI detected from publisher page metadata after a resolver redirect (CHROME-EXTENSION-IMPORT-001E2-CORRECTION-01) — merged (PR #255, `45a0516`); owner manual acceptance passed; extension-only, still not published to the Chrome Web Store.** Owner manual acceptance of the 001E2 release candidate failed on the DOI case, and the failure was a design consequence rather than a bug: `https://doi.org/10.1038/s41586-020-2649-2` redirects to `https://www.nature.com/articles/s41586-020-2649-2` almost instantly, so by the time an ordinary user reaches the toolbar the active tab is the publisher's and holds no DOI. The URL-only classifier answered *unsupported* for the single most ordinary way anyone navigates by DOI — correct by its own rules, unusable in practice. **The accepted architecture is URL-first, metadata-fallback**, and the order is the whole privacy argument: `pubmed`, `doi` and `restricted` are final and the page is never touched, so a PubMed record, a `doi.org` link and a `chrome://` page are all answered without any page access at all; only `unsupported` on an ordinary http(s) page proceeds to `chrome.scripting.executeScript`, which reads four standard bibliographic keys — `citation_doi`, `dc.identifier`, `dc.identifier.doi`, `prism.doi` — from `document.head` in the main frame. **Deliberately not a publisher registry**: a rule for Nature, then Elsevier, then Springer is wrong the day a path changes and never covers the next journal, whereas publishers already emit the DOI for indexers, so the fallback carries no per-site knowledge. Nothing else in the page is read — not the title, the abstract, the authors, the journal, headings, body text, anchor hrefs, `data-` attributes, inline scripts or JSON-LD — and there is still **no title fallback**, so an unidentifiable page remains a page nothing is sent about. Two *non-equivalent* valid DOIs fail closed rather than resolving to either, because `doi` is a per-user deduplication key and the wrong one is a data-integrity bug, not a display bug — where equivalence is the DOI Handbook's §4.3.4 rule (ASCII `A`–`Z` compares identical to `a`–`z`, and nothing outside Basic Latin folds), so case variants of one DOI are one detection while the Handbook's own `Á`/`á` counterexample stays two DOIs and is still refused; the comparison key never leaves the resolver, so the spelling the publisher registered is what is displayed and handed off. **The permission cost is exactly one**: `permissions` becomes `["activeTab","scripting"]`, and **no host permission** — `scripting` enables the API but reaches no page on its own, so the pair is strictly narrower than a `host_permissions` entry, which would grant standing access to every matching page whether or not the user ever asked. Chrome confirms both halves in the real-browser lane: `permissions.getAll()` returns `origins: []`, and a genuine `executeScript` against a real tab with no toolbar grant is refused with *"Cannot access contents of the page."* — so "the page is read only when you invoke PaperLume" is Chrome's behaviour, not the extension's promise. No content script, no background context, no storage, no network primitive, and the shipped package is still the same eight files. **Normalization reuses the existing DOI rules rather than adding a fourth**: `extractDoiFromMetadataValue` composes `extractDoiFromDoiUrl` and the module's own `DOI_NAME_PATTERN`, accepts the presentation forms publishers actually write (`doi:`, `DOI: `, a resolver URL, `dx.doi.org`, a bare name, any of them wrapped in markup whitespace), and is deliberately **narrower** than the importer's classifier — which takes a leading `10.` at its word because a person pasted it, where publisher markup asserts nothing. It is the only function in that module that trims, because it is the only one receiving a value not already authenticated as a DOI name, and it is joined by `doiEquivalenceKey` — the §4.3.4 comparison rule, written out by code point rather than as `toLowerCase()`, which would wrongly fold `Á` onto `á` and, under a Turkish locale, `I` onto `ı`. **Both new test lanes were validated as negative controls**: hoisting a single constant one line out of the injected function's body — the mistake that works in jsdom, survives bundling and throws `ReferenceError` on a real page, because Chrome serializes `func` and loses its execution context — fails 6 of the 8 real-browser metadata specs and the unit control that rebuilds the function in a scope without the module's bindings. **Disclosure delta:** the drafted Store answer for **Website content** moves **No → Yes**, because content genuinely is accessed; Web history stays Yes; nothing else was moved to Yes without evidence. The popup footnote's "never the page itself" was replaced rather than softened, and two screenshot captions were regenerated for the same reason (the icon and promo tile are byte-identical — nothing in them had become false). `privacy-data-flow-audit.md` gains a **dated addendum** rather than a rewrite, so §11 still records what was true at the commit it audited. **This correction opened a blocking Privacy Policy gate, which has since been closed.** The public Privacy Policy stated the extension *"does not … read the contents of the webpage or its DOM"*, which the metadata read made untrue. That text is owner-approved and under separate control, so CORRECTION-01 deliberately did **not** edit it — the amendment was an owner/legal action, and no Store submission could proceed while a disclosed *Website content = Yes* contradicted a posted policy saying the page is never read. The owner then approved replacement §4 wording; `PRIVACY-POLICY-EXTENSION-METADATA-001B` implemented it; that work **merged on 2026-08-30 as `8144504508df333e850c0ed38ec1352c9579ca24` (PR #258)**; and the amended `/privacy` was then verified live in public Production, signed out, showing the bounded metadata disclosure and an effective date of August 30, 2026. **That specific mismatch gate is CLOSED.** A *standing* signed-out Production check of `https://app.paperlume.app/privacy` — that it is reachable and still factually consistent with the disclosed answers — must nonetheless be repeated before **every** actual Chrome Web Store submission. **CORRECTION-01's own scope:** no Store account, upload, listing, submission or publication; no version bump; no Supabase, Edge, migration, provider-tier or Vercel change; no Privacy Policy edit; no application behaviour change — the policy was amended afterwards by PR #258 above, not by this change. **Superseded 2026-08-30 by `CHROME-EXTENSION-IMPORT-001E3A`/`001E3B`** — see item 1 above. *(Correct when written:* no Chrome Web Store item, listing, upload, submission or publication exists, the Developer Dashboard has not been reconciled, and the promotional-video requirement and the separate store-icon-field question both remain unresolved.*)* **Owner manual acceptance of the corrected 001E2 behaviour then completed successfully** — the key retest (navigate through doi.org, let the redirect finish, then click the real toolbar button) and the PubMed regression path both passed.
