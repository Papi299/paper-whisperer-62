# Start Here — Current-State Handoff

> **Purpose.** This is the bounded current-state handoff for a fresh engineering, product, or AI-assistant session. It describes what is true **now**. It is not a project journal: chronological history lives in Git, merged PRs, `supabase/migrations/`, and [migration-history.md](migration-history.md).
>
> **Maintenance rule.** Target 150–250 lines; hard maximum 300. Update **in place**: replace or delete statements that stop being true. Never append PR-by-PR narrative. Prefer a link to the authoritative document over copied detail. See [documentation-policy.md](documentation-policy.md).

## Product and repository identity

- **Repository:** `Papi299/paper-whisperer-62` (product name in-repo: **Paper Whisperer**). **Repository visibility is owner-controlled and may alternate between public and private** — check GitHub for the setting in force today, and never treat either state as a fixed architectural or security assumption.
- **Working commercial brand:** **Paperlume** (working brand only — **not** a registered trademark; see C19 in [decisions-and-triggers.md](decisions-and-triggers.md)).
- **Domains:** `paperlume.app` secured via Cloudflare Registrar; the app is live at **`app.paperlume.app`** (Vercel). Marketing site at root `paperlume.app` is not yet built.
- **What it is:** an academic paper library manager — import, organize, search, analyze, and export research papers — using a **single-user workspace model with per-user multi-tenant isolation** (every row is owned by one user; Postgres RLS is the isolation boundary).

## Current architecture

- **Frontend:** React 18 + TypeScript + Vite SPA; shadcn/ui (Radix + Tailwind); TanStack Query for server state; React Router with four routes (`/`, `/auth`, `/dashboard`, `/reset-password`). Route guarding is a client-side redirect for UX only — **RLS is the real security boundary**.
- **Backend:** Supabase — Postgres 17, Auth (JWT, localStorage-persisted sessions), Storage, PostgREST, and three **deployed** Edge Functions: `fetch-paper-metadata` (PubMed/Crossref metadata, v14), `analyze-paper` (Google Gemini analysis, v15), and `get-gemini-provider-quota` (v3). The third is **deployed but intentionally unused** — its manager-facing dashboard is **deferred under C29** (Gemini Free Tier during development; automatic provider-quota monitoring paused until commercialization), so **no frontend calls or renders it**; it remains as deferred infrastructure. A fourth function, **`delete-account` (PFA-C04), exists in the repository and is NOT deployed to Production** — its Production deployment is a separately authorized mutation that must happen *before* the frontend merge, so the Danger-zone button never points at a missing endpoint (see [deployment.md §7a](deployment.md)). All four authenticate the caller with the caller's JWT via in-body `auth.getUser()` (`verify_jwt = false` at the gateway is intentional; CORS `*` is an accepted decision under bearer-token auth). The first three use **no** elevated key; `delete-account` additionally builds a **server-only** elevated client (the Edge runtime's auto-provided `SUPABASE_SECRET_KEYS`, falling back to `SUPABASE_SERVICE_ROLE_KEY`) because Auth administration and Storage cleanup require it — that key is never sent to the browser, never returned, and never logged, and **no `VITE_*` variable carries any secret key**.
- **Read path:** all filtering, sorting, pagination, keyword matching, and full-text search happen in Postgres; the client holds one page at a time. Detail: [architecture-read-path.md](architecture-read-path.md).
- **Search:** four mutually exclusive modes (empty / short ILIKE for 1–2 chars / prefix-aware FTS for 3+ chars / quoted literal phrase), across six fields (title, abstract, authors, journal, notes, keywords) with a server-driven "Matched in:" attribution line.
- **Security patterns (mandatory for new code):**
  - **S1:** every `SECURITY DEFINER` RPC verifies caller identity against `auth.uid()` (explicit guard or internally derived ownership). Full inventory and rule: [decisions-and-triggers.md](decisions-and-triggers.md) §Security decisions.
  - **S2:** client-side mutations/reads on `user_id`-bearing tables carry an explicit `.eq("user_id", userId)` predicate as defense-in-depth, with nullable-safe `userId` threading.
- **Attachments:** private Storage bucket, owner-scoped path policies, size/MIME limits, server-side storage-quota triggers.

## Implemented capabilities

- Papers CRUD, bulk actions (select-all across the full filtered set), and column/layout customization.
- Import by PMID/DOI identifiers — including structurally authenticated PubMed record URLs and DOI resolver URLs — and from BibTeX / RIS / PubMed NBIB / EndNote tagged / CSV files, with atomic server-side bulk insert. Structured sources (the PubMed API, native NBIB) state publication types as discrete values, and those boundaries are persisted in the nullable `papers.raw_publication_types` — the application never reconstructs them by splitting the legacy joined `raw_study_type`, which stays as the human-readable/fallback representation.
- Duplicate detection — **PMID/DOI-only by decision**. The duplicate-merge RPC exists and its **successful merge path is repaired and live in Production**: `MERGE-EXACT-DUPLICATES-JSONB-REPAIR-001` was merged through PR #184 and deployed as migration `20260807141402_repair_merge_exact_duplicates_jsonb`. Successful merge behavior is covered by the dedicated `005_merge_exact_duplicates_success.test.sql` database regression suite.
- Projects, tags, and four curation pools: keywords, synonyms, study types, exclusions.
- Per-paper notes (indexed into search) and saved searches / filter presets (server-side per user, RLS-isolated).
- Exports to CSV / RIS / BibTeX of the current selection/filter (chunked pipeline for large libraries), plus a separate **full account export** — **Settings → Account data** downloads one versioned ZIP (`paperlume-account-export`, format version 1) covering the signed-in user's whole dataset regardless of filters: papers (with notes), projects, tags, both relationship tables, filter presets, all four pools, attachment metadata **and** attachment binaries, and a whitelisted non-secret profile. Client composition over existing SELECT-own reads and private Storage reads only — no migration, RPC, or Edge Function. Secrets are excluded by construction (`profiles.pubmed_api_key` is never selected; no Session/User object is serialized), and the export is complete-or-fail: any unreadable category or missing attachment binary aborts the run rather than downloading a partial archive.
- Per-paper file attachments in the private Storage bucket, within the server-enforced storage quota. **Settings → Storage** renders a read-only used/quota/remaining gauge for the signed-in user (transparency only — no upgrade, checkout, or purchase path; the database trigger stays the enforcement boundary).
- AI analysis (TL;DR, study type, statistical methods) via Gemini, behind the server-enforced AI quota.
- Per-user optional PubMed API key (stored in `profiles`, used server-side by `fetch-paper-metadata`).
- **Self-service account deletion** — **Settings → Danger zone** permanently deletes the signed-in account after the user types `DELETE MY ACCOUNT` exactly. It is a **hard** deletion, not a soft delete, deletion request, or grace period: the privileged `delete-account` Edge Function derives the target **solely from the authenticated bearer token** (the client sends no user id at all), removes every Storage object under the account's `{userId}/` prefix through the Storage API **before** touching Auth, then calls `auth.admin.deleteUser(userId, false)`; the user's database rows go with it through the pre-existing `ON DELETE CASCADE` foreign keys. PFA-C02's full account export is the **export-before-delete** path and is offered next to it. Function code is present but **not yet deployed to Production**.

## Key files (orientation map)

| Path | Role |
|---|---|
| `src/pages/Dashboard.tsx` | Main page; orchestrates hooks and list UI |
| `src/hooks/usePapers.ts` | Papers infinite query + server filter/sort |
| `src/hooks/useFilterState.ts` | Filter/search state + three-mode search routing |
| `src/lib/buildPapersQuery.ts` | PostgREST query builder for the read path |
| `src/hooks/useFilterPresets.ts` | Saved searches: schema, queries, mutations |
| `src/hooks/usePaperAnalysisActions.ts` | AI-analysis orchestration (single + bulk) |
| `src/hooks/useAbstract.ts` | On-demand abstract fetch (single + batch) |
| `src/hooks/useAttachments.ts` | Attachment upload/download/delete |
| `src/lib/accountExport/` + `src/hooks/useAccountExport.ts` | PFA-C02 full account export: category registry, S2-scoped reads, manifest, streaming ZIP |
| `src/hooks/useAccountDeletion.ts` + `src/components/settings/DeleteAccount*.tsx` | PFA-C04 client: invocation, typed-phrase gate, session/cache cleanup |
| `supabase/functions/delete-account/` + `_shared/accountDeletion.ts` | PFA-C04 privileged boundary: auth, confirmation, Storage cleanup, hard Auth delete |
| `src/lib/importParsers.ts` | BibTeX / RIS / PubMed NBIB / EndNote tagged / CSV parsing |
| `src/lib/pubmedIdentifiers.ts` | Structural PubMed PMID / record-URL recognition (import + export) |
| `src/integrations/supabase/client.ts` | Supabase client (env fail-fast via `src/lib/clientEnv.ts`) |
| `supabase/functions/analyze-paper/index.ts` | Gemini analysis + quota consume/refund |
| `supabase/functions/fetch-paper-metadata/index.ts` | PubMed/Crossref metadata fetch |
| `supabase/migrations/` | Full schema, RLS, and RPC definitions (chronological) |

## Commercial and entitlement state

**Implemented and applied to the linked Supabase project** (the 2026-07-17 Phase 0 audit verified the commercial and RLS enforcement objects in parity by read-only remote inspection; subsequent bounded deployment work brought the linked project to the current **73-row aligned migration ledger** (last `20260810152125_harden_remaining_function_search_paths`, Local = Remote) and the current **three deployed Edge Functions** — `fetch-paper-metadata` v14, `analyze-paper` v15, and `get-gemini-provider-quota` v3 (the last deployed but intentionally unused under C29). Note that ledger parity does **not** imply full structural schema parity — see the drift risk below):

- **Entitlement/usage schema** (`20260521010000_add_entitlement_usage_schema.sql`): `user_entitlements`, `subscriptions`, `subscription_events`, `usage_counters`, `usage_credits`; `handle_new_user()` seeds a Free entitlement on signup. `subscriptions`/`subscription_events`/`usage_counters` are intentionally deny-all under RLS (server-only).
- **AI quota enforcement** (`20260521020000_add_ai_quota_rpcs.sql`): `consume_ai_quota` / `refund_ai_quota` SECURITY DEFINER RPCs with S1 guards; `analyze-paper` consumes a unit **before** calling Gemini, refunds best-effort on provider failure, and returns a structured **HTTP 402** when quota is unavailable.
- **Storage quota enforcement** (`20260521030000_harden_attachment_privacy_and_storage_quota.sql`): `user_storage_usage` plus atomic check-and-consume / refund triggers on `paper_attachments`.
- The schema is **provider-neutral**; no billing provider is wired into it yet.

**Not implemented** (do not describe these as existing):

- Paddle integration: checkout, webhook ingestion, subscription synchronization, customer portal.
- Billing / paywall / upgrade UI. **PFA-C01-AI-QUOTA-UX-001** added AI-quota **transparency** — a header usage indicator plus a clear, actionable message when the AI `402` is hit — but **no** upgrade/checkout/paywall path (shipped 2026-07-25, PR #166 merge `fb876c8`; migration `20260724120000` applied in Production). **PFA-C05-STORAGE-USAGE-INDICATOR-001** added the equivalent **storage** transparency — a read-only Settings → Storage gauge — with **no** upgrade path either. The raw Postgres message shown when an **upload** is rejected over quota is unchanged and is explicitly outside PFA-C05.
- Free-tier feature gating of the Synonyms and Exclusions pools (launch blocker per [quotas-and-pricing.md](quotas-and-pricing.md)).
- Legal pages (privacy / terms / AI disclosure / support). **Account-level data export (PFA-C02) and self-service account deletion (PFA-C04) now exist** and are no longer in this list — see Implemented capabilities. PFA-C04's `delete-account` Edge Function is nevertheless **not deployed to Production yet**; until it is, the capability is complete in the repository only.
- Marketing site, paid launch. **The product is not commercially launched.**

**Direction (decided, see [owner-decisions.md](owner-decisions.md) and C-numbered entries in [decisions-and-triggers.md](decisions-and-triggers.md)):** Merchant-of-Record-first billing (C17); **Paddle** selected for the web MVP (C18), gated on owner-side Paddle setup; Free → Pro two-tier MVP with baselines in [quotas-and-pricing.md](quotas-and-pricing.md) (C9–C11); web-first, mobile deferred (C7); Paperlume brand + domain (C19).

## Deployment and operations model

- **Frontend:** Vercel Git integration. Every PR gets a Preview deployment; every merge to `main` **auto-deploys to production** (`app.paperlume.app`). There is no manual promote step.
- **Database:** Supabase migrations are **not** auto-deployed. They are pushed manually (`supabase db push`) per the runbook in [deployment.md](deployment.md) §6.
- **Edge Functions:** deployed manually and separately per function (`supabase functions deploy <name> --project-ref lioxtgiputfniqbktcsz`); a GitHub merge alone does not update them. `GEMINI_API_KEY` is a manually set Supabase secret.
- **Auth email:** Supabase Auth Custom SMTP routes through Resend on `auth.paperlume.app` (owner-completed 2026-05-22; operational verification is owner-attested, see [deployment.md](deployment.md) §8a).
- Post-deploy smoke checklist and troubleshooting: [deployment.md](deployment.md) §9–§10.

## Testing and merge-safety baseline

- **GitHub Actions CI is the required merge gate.** The **`Validate`** workflow (`.github/workflows/validate.yml`) runs `npm ci`, lint, `npm run typecheck`, Vitest and the production build on Node 22 for every pull request to `main` (and every push to `main`); `main` is protected to require the `validate` check — strict/up-to-date, administrators included, zero required human approvals, force-push and branch deletion disabled, regular merge commits allowed, Vercel **not** a required check. Because merges to `main` auto-deploy the frontend, the local commands below remain useful pre-push evidence but are no longer the sole gate.
- Local pre-push baseline (lint / typecheck / Vitest / build are also the required CI gate; local Playwright runs use the safe lifecycle below, while the separate `E2E (local)` workflow provides non-required Playwright CI):
  - `npm run lint` — passes (0 errors).
  - `npm run typecheck` — passes (0 diagnostics, both projects).
  - `npm test` (Vitest) — passes.
  - `npm run build` — passes.
  - Targeted or full Playwright via the safe local lifecycle **`npm run test:e2e:local`** when UI behavior changes (ephemeral local Supabase stack; a bare `npm run test:e2e` deliberately **fails closed** and is not the normal path).
- **TypeScript status:** the root solution-style `tsconfig.json` has an empty file set, so plain `npx tsc --noEmit` checks **no application files** and is **not valid validation evidence**. Use `npm run typecheck`, which runs both real projects — `typecheck:app` (`tsc --noEmit -p tsconfig.app.json`) and `typecheck:node` (`tsc --noEmit -p tsconfig.node.json`). Both currently pass with **0 diagnostics** (TYPESCRIPT-BASELINE-001 regenerated the authoritative Supabase types and eliminated the former ~48-diagnostic application baseline without weakening type safety).
- **Test layers that exist:**
  - Vitest unit/integration tests — pure lib logic, import parsers, export pipeline, hooks with a mocked Supabase client.
  - Playwright E2E — Chromium-only, single-worker serial. Both the **local-first foundation (PFA-C03A1-L)** and the **non-required `E2E (local)` CI workflow (PFA-C03A2-L)** are **merged on `main` and accepted** (PR #173 and PR #175): `npm run test:e2e:local` is the supported safe lifecycle — it runs the suite against an **ephemeral local Supabase stack** (all tracked migrations replayed + deterministic local-only seed of 120 primary + 5 secondary papers) behind a **two-layer fail-closed** Production/remote guard, and suppresses the raw key-bearing stack-startup output. A bare `npm run test:e2e` deliberately **fails closed** without the explicit local backend contract, so the historical Production-backed account path (`.env.test` against the production project) is **no longer an active supported path**. The `E2E (local)` workflow (`.github/workflows/e2e-local.yml`) now **exists on `main`** and runs automatically for **same-repository** pull requests and `main` pushes (fork-origin PR jobs skip before any install/Docker/Supabase/browser/app step); it reuses this lifecycle on an ephemeral local stack, uses `contents: read` only, needs **no** GitHub secret/variable/Environment, is **non-required** (not in `main` branch protection), and does not change the required `Validate` gate. Phase A is complete for the selected local-first path. No isolated cloud test environment exists.
- **C03B1 database-security suites + runner — merged to `main` and accepted (PR #179, merge `0f786dac…`):** the resumed C03B1 work (branch `test/pfa-c03b1-db-security-resumed`) added four comprehensive **pgTAP** suites — `001_core_rls_isolation`, `002_relational_rls_isolation`, `003_rpc_caller_scope_and_grants`, `004_storage_and_quota` (**255 new assertions**) — alongside the preserved focused `supabase/tests/database/000_preexisting_security_regressions.test.sql` (**139 assertions**, merged on `main` since PR #177). A new **`npm run test:db:local`** lifecycle (`scripts/e2e-local.mjs db-tests`) starts an ephemeral local stack, replays every tracked migration (**currently 72**), captures a pgTAP + **catalog-fingerprint** baseline, runs an **expected-failure database negative control** (injects a transaction-only papers-RLS regression the detector must catch, then proves it rolled back), runs every pgTAP file (extension created transactionally, state restored), runs the preserved framework-free 18-case `supabase/tests/owner_access_and_quota_verification.sql`, and proves **true concurrent** AI-quota consumption at the cap via an external advisory-lock barrier with **bounded, fail-closed** coordinator/worker processes whose deadlines all start at barrier release — then verifies **no row/catalog residue** and always tears down, followed by an **authoritative fail-closed** post-teardown inspection **scoped to this project's ref**. Three independent-review rounds have been applied: the first resolved six items (negative control; shared cross-user PMID for `get_duplicate_papers`; all 17 direct-RPC owner-EXECUTE assertions; catalog-residue fingerprint; bounded concurrency; project-scoped teardown); the second closed three fail-closed gaps (simultaneous concurrency deadlines; authoritative Docker inspection that fails the lifecycle on any inspection failure; an **expanded catalog fingerprint** — full function/trigger definitions, indexes, columns/defaults/constraints, policy mode, relation ownership/ACL — backed by a sensitivity probe proving same-name definition changes are detected); the third added a **strict Docker-name parser** for the teardown inspector (rejects spaces/tabs/control chars/blank/duplicate records without trimming them into validity; empty output stays valid; malformed raw output never logged) with two inspection negative controls (nonzero-exit and malformed-output) that both fail the lifecycle after real cleanup. Both immutable existing test files are unchanged (byte-identical). This was merged via **PR #179** (regular exact-head two-parent merge `0f786dac7088b2afafc16cb76afae8eee5f206d6`); the four suites, the `test:db:local` runner, and the full local lifecycle **now exist on `main`**, with merged-main `Validate` and `E2E (local)` green and Production unchanged.
  - **Hosted database-security CI — `DB Tests` (PFA-C03B2) — is merged on `main` and accepted** (PR #181, merge `b57614182fdabff94bc26d128585c7754b7ae45c`). `.github/workflows/db-tests.yml` (check **`DB Tests / db-tests`**) is a thin hosted wrapper that runs the merged C03B1 `npm run test:db:local` lifecycle on an ephemeral local Supabase stack, for same-repository pull requests and `main` pushes (fork-origin PR jobs skip before any install/Docker/Supabase/database execution). It uses `contents: read` only, needs **no** GitHub secret/variable/Environment, uploads **no** artifacts, and is **non-required** — it is not in `main` branch protection and does not change the required `Validate` gate. It passed on its own merge commit (run `31162869485`: 70 migrations, pgTAP 394/394, framework-free 18/18, expected-failure negative control and catalog sensitivity probe both detected-and-reverted, true-concurrency probe, residue and teardown clean). **Phase B and PFA-C03 are complete.**
  - **Current database-test inventory:** nine pgTAP suites — `000_preexisting_security_regressions`, `001_core_rls_isolation`, `002_relational_rls_isolation`, `003_rpc_caller_scope_and_grants`, `004_storage_and_quota` (the five from C03B1), plus `005_merge_exact_duplicates_success`, `006_structured_publication_type_provenance`, `007_function_search_path_hardening`, and `008_account_deletion_cascade` (each added later by the task named in its header, **not** by C03B1) — plus the preserved framework-free 18-case verification. Both `npm run test:db:local` and the hosted `DB Tests` workflow run all of them.
- **Test layers that do not exist:**
  - **Required** database-security CI. `DB Tests / db-tests` exists but is **non-required**; promotion is decision D5 and remains unresolved. So: **automated non-required DB-security CI exists; a required DB-security gate does not.**
  - Edge Function tests **executed by a Deno runtime**. Validation for the three deployed functions remains manual post-deploy smoke (established metadata smoke case: PMID `41912805`). **`delete-account` is the exception**: its whole request path lives in a runtime-agnostic `handler.ts` plus the pure `_shared/accountDeletion.ts`, so the code the function actually runs is covered by Vitest (method gating, token handling, confirmation enforcement, Storage enumeration/batching, cross-user safety, Storage-before-Auth ordering, hard delete, log/response safety) and is additionally invoked for real — as a served local Edge Function — by `e2e/account-deletion.spec.ts` under the local lifecycle.
  - **Required** Playwright CI. **Non-required** Playwright CI **does** exist — the `E2E (local)` workflow is merged on `main` (PFA-C03A2-L) and runs the default read-only spec set on an ephemeral local stack — but it is never a required merge gate. **Edge Function (Deno) tests** do not exist in any workflow. (Required CI *does* run lint, `npm run typecheck`, Vitest and the production build — see the merge-safety baseline above.)
- Do not cite exact test counts here — run the suites for current numbers.

## Dependency security

Authoritative detail, remediation policy, and re-evaluation triggers: [dependency-security.md](dependency-security.md).

- **Dependency Clusters 1, 2, 3, and 4 are complete** (toolchain; `lodash`/`ws`/`yaml`/`picomatch`/`brace-expansion`; `js-yaml`/`flatted`/`form-data`/`@tootallnate/once`/`esbuild`; React Router **within v6 only**). In all four, the **dependency implementation delta was confined to `package-lock.json`** — no `package.json`, application-source, workflow, test, migration, or schema change — and no dependency merge has ever caused a Supabase change. Cluster 4 additionally updates the current-state dependency documentation.
- **Current `npm audit`: 2 moderate findings, and no low, high, or critical findings** — in both the full and the production-only graph. The audit is **not yet at zero**.
- Both remaining findings are in the **React Router family** (`react-router` / `react-router-dom`), installed at the **terminal v6 releases** (`react-router-dom` 6.30.4 / `react-router` 6.30.4 / `@remix-run/router` 1.23.3). Cluster 4 closed GHSA-2j2x-hqr9-3h42 across the family; `@remix-run/router` is no longer vulnerable.
- **The audit cannot reach zero on v6.** All three residual advisories are first patched on the 7.x line or have no v6 fix at all, so **Cluster 5 — a major-version Router migration — is what would be required to eliminate the remaining findings under the current advisory data**. That is a statement about audit clearance, not a claim that it is the next task: Cluster 5 is **NOT STARTED**, **not owner-approved**, and **no Router major migration is authorized**. Paperlume uses declarative routing only (no data router, no SSR) with hardcoded navigation targets, so the residual advisories are assessed **not currently reachable** under today's usage — which is not a declaration that the vulnerable packages are safe. See [dependency-security.md](dependency-security.md) for the per-advisory evidence and its limits.
- **Dependency remediation overall is therefore NOT complete.**

## Active decisions and constraints — do not casually reopen

Authoritative record with rationale and re-evaluation triggers: [decisions-and-triggers.md](decisions-and-triggers.md).

- **Duplicate detection is PMID/DOI only.** Do not propose fuzzy or title-based matching.
- **Title-based import** auto-selects the first PubMed/Crossref match; the accepted mitigation is the static warning in the Add Papers dialog. Do not propose per-paper preview/confirmation flows.
- **CORS `*` on all three Edge Functions is intentional** under header-based bearer-token auth. Revisit only if auth becomes cookie-based.
- **No real-Gemini / AI Playwright E2E** — rejected as non-deterministic; the AI path is covered by mocked Vitest tests plus manual smoke.
- **Read-path architecture is stable** (server-side filter/sort/paginate, keyword RPCs, on-demand abstracts, cache-key split, select-all-IDs). Changing it requires new evidence.
- **Deferred with documented triggers:** Phase C DB optimization (jsonb GIN indexes, RPC rewrites), unused-index cleanup, write-path optimization, Hebrew/RTL (C15), mobile packaging (C7).

## Current risks and owner blockers

**Engineering risks:**

- **Schema drift — reconciliation complete:** production predated the first tracked migration, so a clean replay of the tracked migrations produced a schema that materially differed from production even though the migration ledger matched. **All reconciliation tasks are now complete** — RECON-JUNCTIONS-001, RECON-STATISTICAL-METHODS-001, RECON-INTEGRITY-001, RECON-LEGACY-COLUMNS-001, and the final **RECON-METADATA-PARITY-001** (PR #156) — merged, applied remotely, and verified (the reconciliation sequence itself ended at `20260719162013`); the aligned ledger now holds **73** aligned migrations, last `20260810152125_harden_remaining_function_search_paths`, Local = Remote (the PFA-C08 bounded-`search_path` hardening, applied 2026-08-10; before it, `20260809051802` structured publication-type provenance applied 2026-08-09, `20260807141402` duplicate-merge repair applied 2026-08-07, `20260802025704` C03B1 security remediation `harden_rpc_and_relational_ownership` 2026-08-02, and `20260731162729` Data API grant-parity 2026-07-31). Freshly generated **local and linked** `public`-schema Supabase types are **semantically identical**, and the committed generated types now match the linked output (TYPESCRIPT-BASELINE-001). `papers.search_vector` (proven semantically equivalent) remains an approved benign/artifact exclusion. The **SEC-4 default-grant diff was reclassified** (PFA-C03A1-L-GRANT-PARITY-001, 2026-07-31): it is **not** benign for a clean replay — the tracked migrations never granted the Data API roles their table privileges, so a fresh local reset yields `42501` and a non-functional app; a tracked least-privilege grant-parity migration now encodes the intended Data API surface and is **merged and applied to Production (2026-07-31; ledger 69 aligned; effective no-op — no longer pending)** (see [schema-reconciliation.md](schema-reconciliation.md)). Full inventory, owner decisions (C20–C26), and roadmap: [schema-reconciliation.md](schema-reconciliation.md). The audit confirmed RLS policies, security RPCs, and all commercial tables are **in parity** — enforcement is not broken.
- **CI and branch protection are now in place (CI-BASELINE-001):** the required `Validate` workflow gates pull requests to `main`, and `main` protection requires the `validate` check — closing the former top structural gap where `main` auto-deployed with no merge gate.
- E2E historically ran against the production Supabase project. That was addressed by **PFA-C03 (owner-selected 2026-07-31), which is now COMPLETE**; its contract and full evidence record is [pfa-c03-staging-and-security-test-plan.md](pfa-c03-staging-and-security-test-plan.md). Its first foundation subtask, **PFA-C03A1-L**, is **merged and accepted** (PR #173, merge `299b19a…`): a **local-first** ephemeral Supabase stack (all tracked migrations replayed + deterministic seed) with a **two-layer fail-closed** Production/remote guard, so **Production-backed Playwright is now prohibited by the merged guard** (it fails closed). **C03A2-L (a non-required local Playwright CI workflow, `E2E (local)`) is now merged and accepted** (PR #175, merge `1d091e4…`) — it reuses the merged C03A1-L lifecycle on an ephemeral local stack, stays separate from and does not alter the required `Validate` gate, requires no secret/variable/Environment, and never accesses Production; the workflow now exists on `main`. **Phase A is complete for the selected local-first path** (cloud phases not selected). No cloud staging project/secret is provisioned. **Phase B has started: C03B1 authoring found pre-existing database-security defects** in the then-current, unmodified **69-migration base** schema (unauthenticated cross-user read/write via SECURITY DEFINER RPCs; cross-owner relational + attachment associations); the **bounded remediation is now merged and deployed to Production** (PR #177, merge `f4088705a2d3b8869579b253b5d8f7b3162414cf`; migration `20260802025704` applied, ledger 70), so the **schema from migration `20260802025704` onward closes those defects** and **the C03B1 security blocker is removed**. **C03B1's implementation is now merged to `main` and accepted** (PR #179, regular exact-head two-parent merge `0f786dac7088b2afafc16cb76afae8eee5f206d6`; four comprehensive pgTAP suites + a `test:db:local` runner + an external true-concurrency AI-quota probe — merged-main `Validate` and `E2E (local)` green, Production unchanged); and **C03B2 (the hosted non-required `DB Tests / db-tests` workflow) is merged to `main` and accepted** (PR #181, merge `b57614182fdabff94bc26d128585c7754b7ae45c`; green on the merge commit itself). **Phase B and PFA-C03 are therefore complete** (D3 resolved 2026-08-02; **D4–D5 remain unresolved and block nothing**). A **separate** product defect — `merge_exact_duplicates` called `unnest()` on `jsonb` columns, breaking successful duplicate merging — predated PR #177 and was tracked as `MERGE-EXACT-DUPLICATES-JSONB-REPAIR-001` (never part of PFA-C03); it is now **repaired and live in Production** (PR #184, merge `bd741f56…`; migration `20260807141402` applied 2026-08-07).
- Supabase security advisors (2026-07-17): mutable `search_path` on five functions (incl. `search_papers`); SECURITY DEFINER RPCs executable by `anon`; Auth leaked-password protection disabled. **Update (PFA-C03B1, 2026-08-02):** the anon-executable read/write RPCs were found **exploitable**, not merely wide — their ownership guards omitted an explicit `auth.uid() IS NULL` clause, so an unauthenticated caller bypassed them (cross-user read via the search RPCs; cross-user **write** via `safe_bulk_insert_papers`). The remediation is now **merged and deployed** (PR #177, migration `20260802025704`): it adds the NULL-auth guards, a bounded `search_path` on `search_papers`, setter-RPC ownership validation, both-owner relational RLS, attachment↔paper ownership, and least-privilege EXECUTE across the whole SECURITY DEFINER surface (no PUBLIC/anon/`service_role`), so these advisor items are **closed in Production**. **Update (PFA-C08, 2026-08-10):** a fresh read-only advisor run found (a) `function_search_path_mutable` on four **non-RPC** functions — `set_updated_at` and the three `immutable_english_tsvector_*` search-vector helpers — which migration `20260810152125_harden_remaining_function_search_paths` pins to `search_path = pg_catalog`; it is now **merged and applied to Production** (PR #200 merge `7c61ba39…`, deployed 2026-08-10 under separate authorization, ledger **72 → 73**), and post-deploy verification confirms all four carry exactly that path with every other catalog property unchanged, so **these four advisor warnings are cleared in Production**; (b) 17 `authenticated_security_definer_function_executable` WARNs, which are the **intended** already-hardened application RPC surface (authenticated-only, `auth.uid()`-guarded) and are accepted, not defects; (c) four `rls_enabled_no_policy` INFOs on the **intentional** server-only deny-all tables; and (d) `auth_leaked_password_protection`, still disabled. **That last item is now an explicit owner decision, not an open gap:** leaked-password protection is **Pro Plan and above** and the organization is on **Free**, so **C30** (2026-08-10) keeps Paperlume on Free during development and **defers the control to commercialization** — see [owner-decisions.md](owner-decisions.md) §1.5. **The PFA-C08 database track is therefore live and verified, and PFA-C08 is closed for the current development scope**, with leaked-password protection retained as a **commercialization prerequisite** to be revisited before commercial/public paid launch, or earlier if the organization moves to Pro for another reason or Supabase changes the feature gate.

**Owner-action blockers (paused — detail and ordering in [owner-decisions.md](owner-decisions.md)):** these gate the paused commercial-launch work (C27). They are future-facing and are **not** the active next task.

- Paddle Sandbox setup: account, KYB, domain verification, Product + Price, API key, webhook secret, portal config, `APP_URL` (gates the Paddle integration PR — paused per C27).
- Marketing site provider + root-domain hosting; privacy/terms/AI-disclosure/support URLs (C16).
- Google Workspace business email; monitoring/error-tracking provider; staging-environment timing.

## Current recommended next action

**Public-launch and commercial-launch implementation remain paused by owner decision (C27, 2026-07-24).** Paddle integration, checkout, webhooks, paywalls, billing and public-launch work are **not on the active critical path** and must not be started without a new explicit owner decision. Owner-side Paddle Sandbox setup is **not** the next gate.

The active priority is **product feature and workflow development**. The high-priority UX/workflow-remediation sequence that preceded feature work is **complete**:

- **PROJECT-TAG-SELECTOR-UX-001 — completed (PR #162, merge `dedde4d0`).** Dashboard Project/Tag filters are searchable multi-select with long-name overflow fixed; presets payload **version 2** (backward-compatible v1 reads).
- **PROJECT-TAG-MATCH-MODE-001 — completed (PR #163, merge `b406e2f2`).** Each of the Project and Tag filters has an independent **Any/All** match mode (Any = OR-union within a category, All = AND-by-membership within a category; categories stay ANDed with each other and every other filter — **no** global cross-category OR). Default **Any**; presets payload **version 3** (backward-compatible v1/v2 reads normalized to Any/Any).
- **IMPORT-CONTINUATION-WORKFLOW-001 — completed (PR #164, merge `5a92229f`).** The Add Papers flow no longer dead-ends after an import: both identifier and file imports show the result summary **plus** Project/Tag assignment controls with an **Import More / Import Another File** loop that preserves selections for the next batch (a completed run is never retroactively re-assigned). No callback-contract, RPC, or schema change.

**PRODUCT-FEATURE-AUDIT-001 is complete** — merged via PR #165 (merge `214bc329`); its deliverable [product-feature-audit.md](product-feature-audit.md) is the point-in-time capability audit and owner decision packet (blocking-remediation gate **Outcome A**: no Critical/High blocker). **Owner selection is complete:** on 2026-07-24 the owner selected **PFA-C01** (complete the AI-analysis quota experience) as the first implementation candidate. **PFA-C08 is closed for the current development scope**: its database hardening is complete/live/verified (PR #200 / PR #201), while leaked-password protection remains explicitly deferred under **C30** — so there is no remaining unselected PFA candidate. **PFA-C03 was owner-selected on 2026-07-31 and is now complete** (see the PFA-C03 note below), **PFA-C05 was owner-selected on 2026-08-10 and is complete** (see item 5 below), **PFA-C02 and PFA-C04 were owner-selected on 2026-08-11 and are complete on acceptance of their implementations** (items 4 and 6 below), **PFA-C06 was owner-selected on 2026-08-14 and is complete on acceptance** (item 7 below), and **PFA-C07 was owner-selected on 2026-08-14 and is complete on acceptance** (item 8 below) — **no completion selects a next candidate**, so a new explicit owner decision is required before any further PFA work starts. **PFA-C09 — Responsive/accessibility pass was owner-selected on 2026-08-14** by explicit owner instruction, *not* by completing PFA-C07, and is **implemented and complete on acceptance** (item 9 below). **Every PFA candidate is now selected or closed, and no completion selects a next one** — a new explicit owner decision is required before any further PFA work starts. **MOBILE-DASHBOARD-DENSITY-001 (owner-selected 2026-08-15, after Production smartphone inspection) is a post-PFA product/UX refinement, not a PFA candidate and not a reopening of PFA-C09** — which remains COMPLETE / MERGED / LIVE. It compacts the permanent mobile Dashboard toolbar to Header + Search + Filters/More so the paper table regains the smartphone viewport, moving the secondary controls into mobile overlays while leaving the desktop workflow unchanged; it **selects no new PFA candidate**. The separately observed **Add Papers Projects/Tags smartphone dropdown defect was explicitly out of its scope** and was addressed afterwards as **ADD-PAPERS-MOBILE-SELECTORS-001 (owner-selected 2026-08-15)**, which the owner expanded — after further Production smartphone testing — to cover the same mobile searchable-selector defect in **Filters** and **Analytics** and the Filters sheet's own opening focus; it is likewise a post-PFA product/UX correction and **selects no new PFA candidate** (item 1 below).

**The OWNER-MANAGER-ACCESS-AND-GEMINI-QUOTA objective (PR #168) is complete — merged to `main` and live in Production:**

- **OWNER-MANAGER-ACCESS-AND-GEMINI-QUOTA-001 (2026-07-25, decision C28; scope-normalized 2026-07-26, decision C29).** A new **owner-selected objective, separate from the PFA candidate list**: internal `owner`/`manager` roles decoupled from the commercial plan (`internal_user_access` + read-only `get_current_user_access` RPC), an owner Paperlume **AI-quota exemption** (owner is never blocked, usage still recorded/refundable; AI indicator shows "Unlimited"), and structured provider-error handling + centralized Gemini model in `analyze-paper` (no prompt/schema/retry/timeout change; a provider limit is never turned into a Paperlume 402). **Both additive migrations are applied in Production** — `20260725090000` and the grant-hardening `20260726120000` — and the **owner bootstrap is complete and verified**; ledger aligned through `20260726120000` (68 rows). **Deployed functions:** `fetch-paper-metadata` v10, `analyze-paper` v15, and `get-gemini-provider-quota` v3. **Under C29 the manager-facing provider-quota dashboard is deferred:** Paperlume stays on the **Gemini Free Tier** with **Google Cloud billing intentionally disabled**; the deployed v3 function is **retained but intentionally unused** (no frontend renders the card or calls it — the frontend surface was removed in the 001Y scope normalization), and Gemini usage/limits are checked **manually via Google AI Studio**. Prior read-only Google-side evidence (`…-001S…001X`): Monitoring returns HTTP 403, project **billing disabled**, **no** project-level IAM deny policy, **no** parent org/folder. **PR #168 was merged** via a regular exact-head merge commit (`a1fc2cea53e33c8b34c557c7087236a939bb783c`, 2026-07-28); the three review threads are resolved, merged-main `Validate` (run `30357049945`) succeeded, and the Vercel **Production** deployment (`dpl_Bx1GyYog6KCDUjHtcHTFVWqySwoV`, READY) is live on `app.paperlume.app` (the GitHub merge applied no migration and deployed no Edge Function — Supabase state is unchanged). Runtime authorization is **UUID/role-based, never email-based**; the server (the deferred provider-quota function re-checking `get_current_user_access`) stays authoritative. **C27 unchanged** — no billing/paywall/upgrade; `usage_counters` privacy and Free/Pro quota values are unchanged. **PFA-C08 is closed for the current development scope under C30; PFA-C03 was owner-selected (2026-07-31), PFA-C05 on 2026-08-10, PFA-C02 and PFA-C04 on 2026-08-11, and PFA-C06, PFA-C07 and PFA-C09 on 2026-08-14 — PFA-C09 is complete on acceptance and no further PFA candidate is selected.**

The selected PFA candidates — PFA-C01, PFA-C02, PFA-C03, PFA-C04, PFA-C05, PFA-C06, PFA-C07, and PFA-C09 — are complete. **PFA-C08 is likewise closed for the current development scope** — its database-hardening track is complete/live/verified and its leaked-password control stays deferred under C30 — but it is tracked with the security advisors above rather than enumerated in this feature list:

1. **PFA-C01-AI-QUOTA-UX-001 — complete, deployed, and accepted (2026-07-25, PR #166, merge `fb876c8b6020de83d23ce2877effa0881ed5c4e4`).** Delivered a read-only `get_ai_quota_status` RPC (server-only `usage_counters` stays server-only — no client SELECT), a header usage indicator (used/remaining, lifetime vs monthly, UTC reset date), a specific actionable message when the server returns the AI `402`, and a bulk run that stops after the first authoritative quota response with complete `analyzed + failed + not attempted = total` accounting. The additive migration `20260724120000_add_ai_quota_status_rpc.sql` is **applied in Production** (project `lioxtgiputfniqbktcsz`), the merged-main `Validate` run succeeded, and the automatic Vercel **Production** deployment is live on `app.paperlume.app`. The initial MCP-generated migration-history label (`20260725054400`) was **repaired** to the canonical `20260724120000` (migration-history metadata only — no schema SQL re-executed). It adds **quota transparency only — no checkout, paywall, or upgrade path** (C27 unchanged).
2. **PFA-C03 (owner-selected 2026-07-31) — COMPLETE. Phase A complete for local-first; Phase B complete; C03B1 merged and accepted (PR #179, merge `0f786dac…`) and C03B2's hosted non-required `DB Tests / db-tests` workflow merged and accepted (PR #181, merge `b5761418…`). No next PFA candidate is selected by this completion.** The owner selected **PFA-C03 — safe staging environment and automated security tests** (the audit's two-phase approach: Phase A isolated test backend + non-required Playwright CI; Phase B automated DB-security tests); its contract is [pfa-c03-staging-and-security-test-plan.md](pfa-c03-staging-and-security-test-plan.md). **C03A0** (staging contract + readiness audit) is **complete**; **C03A1-L** (local-stack seed/reset + env/guard foundation) is **merged and accepted**; **C03A2-L** (non-required local Playwright CI) is **merged and accepted** (PR #175, merge `1d091e4…`; its `E2E (local)` workflow now exists on `main`); no cloud phases are selected, so **Phase A is complete for the selected local-first path**. **Phase B has started:** the pre-existing security defects found while authoring C03B1 are **remediated, merged, deployed, and independently accepted**, removing the C03B1 security blocker; **D3 is resolved (2026-08-02, pgTAP)**; **C03B1's implementation is now merged to `main` and accepted (PR #179, merge `0f786dac…`; four comprehensive pgTAP suites + a `test:db:local` runner + an external true-concurrency probe)**; **C03B2 (the hosted `db-tests` workflow) is merged to `main` and accepted (PR #181, merge `b57614182fdabff94bc26d128585c7754b7ae45c`) — check `DB Tests / db-tests`, non-required, green on the merge commit itself**; **D4–D5 remain unresolved and block nothing**; **Phase B and PFA-C03 are complete**. Decision **D2** was **resolved local-first** (2026-07-31): path **C03A1-L → C03A2-L** (cloud phases C03A1-C/C03A2-C not selected; D1 does not block the local path). The tracked **Data API grant-parity prerequisite** was fixed and deployed first (PR **PFA-C03A1-L-GRANT-PARITY-001**, merge `1f00800e…`, applied to Production; ledger **69** aligned, latest `20260731162729`; effective ACL no-op). **C03A1-L is now merged and independently accepted** (PFA-C03A1-L-LOCAL-STACK-FOUNDATION-MERGE-001): PR #173 merged via a **regular two-parent merge commit `299b19a08da0a61984bf2333d12406cbfd62aef7`** (four commits, eleven files), merged-main **`Validate` run `30697869074`** succeeded, and Vercel **Production** deployment **`dpl_9zhjYVmjLKTKqrt7PW6haZej9rLf`** is **READY** on `app.paperlume.app`; Production Supabase is **unchanged** (69 migrations, Edge Functions v10/v15/v3). The merged lifecycle (`npm run test:e2e:local`) replays all **69** tracked migrations against an **ephemeral local Supabase stack**, seeds a deterministic fixture (120 primary + 5 secondary papers, disposable highest-`insert_order` paper), proves RLS cross-user isolation with authenticated clients, enforces a **two-layer fail-closed** guard (Layer 2 runs before credential reads and fills; **32 guard tests**), suppresses the raw key-bearing `supabase start` output (zero credential-marker matches in accepted scans), hardens teardown/signal handling (authoritative teardown; SIGINT 130 / SIGTERM 143 one-time cleanup) and malformed-input redaction, runs the **six selected read-only specs green (32 tests)** plus a **deterministic second reset/reseed (auth 6 green)**, and the complete Vitest suite held **694 passing tests** at the merge acceptance point — all with **no Production or cloud Supabase access and no CI workflow created**. **Production-backed Playwright is now prohibited by the merged fail-closed guard.** **`C03A2-L` — a non-required local Playwright CI workflow (`E2E (local)` / job `e2e-local`) — is now merged and accepted** (PR #175 merged via a **regular two-parent merge commit `1d091e476461b50375083ac4c1a156e7c38a0b4b`**; two commits / three files). It reuses the merged C03A1-L lifecycle on an ephemeral local Supabase stack, runs guard verification plus the exact six read-only specs, skips fork-origin PRs before execution, uses `contents: read` only, pins every action to a full commit SHA, requires **no** secret/variable/Environment, and stays **separate from and does not alter the required `Validate` gate** (`Validate / validate` remains the only required merge gate; `E2E (local)` is non-required). On the merge commit the push-triggered `E2E (local)` run `30711943858` executed and passed (32 guard tests; 69 migrations; 120/5 seed + isolation; six specs, 32 tests green; teardown + clean-state assertions pass; zero credential-value log matches; 0 artifacts), the required `Validate` run `30711943868` passed, and Vercel **Production** deployment `dpl_4fZ4zGs2itrzKamigJF4gcPGGkVv` is READY on `app.paperlume.app`; the workflow now exists on `main` and Production Supabase is unchanged (69 migrations, Edge Functions v10/v15/v3). **Phase A is complete for the selected local-first path. Phase B is complete: the first DB-security subtask (C03B1) uncovered pre-existing database-security defects; the bounded remediation was merged and deployed to Production (PR #177, merge `f4088705a2d3b8869579b253b5d8f7b3162414cf`; migration `20260802025704` applied, ledger 70), removing the C03B1 security blocker. C03B1's implementation was then merged to `main` and accepted (PR #179, regular exact-head two-parent merge `0f786dac7088b2afafc16cb76afae8eee5f206d6`; four comprehensive pgTAP suites + a `test:db:local` runner + an external true-concurrency AI-quota probe; merged-main Validate and E2E green; Production unchanged); and C03B2 (the hosted `db-tests` workflow) was merged to `main` and accepted (PR #181, merge `b57614182fdabff94bc26d128585c7754b7ae45c`) — `DB Tests / db-tests`, non-required, green on the merge commit itself (run `31162869485`). **Phase B and PFA-C03 are complete** (D3 resolved 2026-08-02; D4–D5 unresolved and non-blocking).** **PFA-C08 is closed for the current development scope under C30; PFA-C07 and PFA-C09 were owner-selected on 2026-08-14, PFA-C09 is complete on acceptance, and no further PFA candidate is selected**; **C27–C29 unchanged.** The prior **OWNER-MANAGER-ACCESS-AND-GEMINI-QUOTA-001** (a separate, non-PFA objective) remains complete and merged.
4. **PFA-C02 (owner-selected 2026-08-11) — COMPLETE on acceptance of `PFA-C02-FULL-ACCOUNT-EXPORT-001`.** The audit's **Option A — complete data portability**, not a reduced citation-only export. Settings gained an **Account data** section whose single action downloads one versioned ZIP (`paperlume-account-export`, format version 1) holding the signed-in user's **whole** dataset regardless of the current filters: `manifest.json` plus a `data/<category>.json` file for every category — profile, papers (notes included as a `papers` column), projects, tags, `paper_projects`, `paper_tags`, filter presets, keyword/synonym/study-type/keyword-exclusion/study-type-exclusion pools, and attachment metadata — plus every attachment **binary** under `attachments/<paper-id>/<attachment-id>-<sanitized-name>`. Every category file is present even when empty (`[]`), and `manifest.json` carries the format, version, UTC `generated_at`, `user_id`, per-category row counts and paths, and the archived attachment count and byte total. **No migration, no RPC, no Edge Function, no Supabase configuration change, and no Production mutation** — it is client composition over existing SELECT-own reads plus authenticated private-Storage reads, so the bucket stays private and no Storage policy or grant changed. **Security:** every direct `user_id` table read carries an explicit `.eq("user_id", userId)` (S2) and every returned row's ownership is re-validated; the junction tables, which have no `user_id`, are reached only through the user's own paper IDs and then validated at **both** endpoints, so a misconfigured policy causes a hard failure rather than a cross-user leak; `profiles` uses an explicit safe-field whitelist so **`pubmed_api_key` is never selected** (not merely stripped later), and no Supabase `Session`/`User` object is ever serialized. Compile-time guards make an unclassified `profiles`/`papers` column or a dropped category a `npm run typecheck` failure. **Completeness:** every collection is paginated past the PostgREST 1,000-row cap — including *within* each junction ID batch — and the export is **complete-or-fail**: any unreadable category, any failed attachment download, or any metadata/binary count mismatch aborts the run with a safe high-level message and **no** archive download. Attachment binaries are fetched through a fixed concurrency window (3) and released into the streaming ZIP as they arrive, so all of an account's source binaries are never resident at once. The ZIP library (`fflate`, MIT, no transitive dependencies) is **dynamically imported** so it lands in a lazy chunk rather than the initial bundle. Existing CSV/RIS/BibTeX citation export is unchanged. It is **portability only — no upgrade, checkout, or paywall path** (C27 unchanged), and it is **not** account deletion — that is the separate **PFA-C04**, now implemented too (item 6 below); export remains the path to keep a copy *before* deleting.
5. **PFA-C05 (owner-selected 2026-08-10) — COMPLETE on acceptance of `PFA-C05-STORAGE-USAGE-INDICATOR-001`.** Settings gained a read-only **Storage** section showing used / quota / remaining plus a progress bar, sourced from two SELECT-own client reads — `user_storage_usage.used_bytes` and `user_entitlements.storage_quota_bytes` — each explicitly scoped with `.eq("user_id", userId)` per S2. **No migration, no RPC, no Edge Function, no Supabase configuration change, and no Production mutation**: the existing read model already supported the feature. A missing `user_storage_usage` row is the normal "no attachments yet" state and renders **0 B used**; a missing entitlement renders "Storage usage unavailable." rather than an invented Free baseline. The query runs only while Settings is open and refetches on reopen. It is **transparency only — no upgrade, checkout, paywall, or plan change** (C27 unchanged), and it does **not** alter the raw over-quota upload error, which remains outside this scope.
6. **PFA-C04 (owner-selected 2026-08-11) — COMPLETE on acceptance of `PFA-C04-SELF-SERVICE-ACCOUNT-DELETION-001`.** Settings gained a visually and structurally separate **Danger zone** whose action opens a dedicated destructive dialog; the final button stays disabled until the user types **`DELETE MY ACCOUNT`** exactly, and the phrase is re-validated **server-side** so a tampered client cannot skip it. The client calls `supabase.functions.invoke("delete-account", { body: { confirmation } })` and **sends no user id whatsoever**. The Edge Function handles the CORS preflight first, accepts only `POST`, requires an `Authorization: Bearer` header, validates that exact token with an authoritative `auth.getUser(token)` network call, and derives the deletion target **only** from the authenticated user — a `user_id` in the body or query string is structurally incapable of redirecting it, and there is no owner/manager exemption and no email-based rule. It then enumerates the caller's `{userId}/` namespace **from Storage itself** (recursive, paginated, bounded, so an orphaned binary with no `paper_attachments` row is still found), deletes it in batches of at most 1,000 paths — the documented Storage limit — and only then hard-deletes the Auth user with `auth.admin.deleteUser(userId, false)`. **Storage must precede Auth**: Supabase refuses to delete a user who still owns Storage objects, and a Storage failure aborts with the Auth user intact rather than reporting success. Because Storage and Auth are separate services, cross-service atomicity is impossible; the operation is instead **safe to retry** (an already-empty namespace performs no `remove()` call and proceeds straight to Auth deletion), and a failure after Storage cleanup is reported honestly as a retryable failure, never as success. On success the browser clears its own Supabase session, drops every cached query, and hard-navigates to `/auth`, so no deleted-account data survives in memory. **No migration, no deletion RPC, no schema/RLS/grant/Storage-policy change and no new npm dependency**: owned rows already cascade from `auth.users`, which `supabase/tests/database/008_account_deletion_cascade.test.sql` (79 assertions) now pins — including the invariant that no `public` FK to `auth.users` can block a hard delete. `subscriptions.user_id` / `subscription_events.user_id` stay `ON DELETE SET NULL` (**deliberate, unchanged** provider/audit-history retention; those rows survive unlinked and must be re-evaluated against legal/privacy requirements before commercial launch). It adds **no billing, paywall or upgrade path — C27 is unchanged** — and PFA-C02 remains the separate export-before-delete route. **The `delete-account` Edge Function is not deployed to Production**; deploying it is a separately authorized mutation that must precede the merge (see [deployment.md §7a](deployment.md)).
7. **PFA-C06 (owner-selected 2026-08-14) — COMPLETE on acceptance of `PFA-C06-BETTER-ONBOARDING-001`.** The audit's thin first-run state (`No papers yet` / `Add papers using PMIDs, DOIs, or titles`) is replaced by an actionable empty-library surface: a **Build your research library** heading, all three import methods the Add dialog genuinely exposes (identifiers, reference file, manual entry) with the accepted formats named, an **Add your first papers** CTA, and a three-step **Add → Organize → Refine** guide pointing at the sidebar's Projects/Tags and the Keyword/Study Type pools. The CTA **opens the existing `AddPaperDialog`** via the Dashboard state that already backs the permanent header button — it adds **no second import path** and no new route, and the header action stays. A **filtered zero-result view is now a distinct state**: while the user owns papers, an empty list says *No papers match your current filters* and offers Dashboard's existing `handleClearFilters` (which also clears the loaded-preset pointer) instead of falsely claiming the library is empty; a defensive `totalCount > 0` / no-filters case renders a neutral message. The three states are keyed off the **unfiltered** `totalCount`, not the visible page length. Onboarding is **derived entirely from live library state — there is no persistent onboarding flag** (no localStorage, profile column, tour, or dismissal), so it disappears on first import and returns if the library is emptied; the existing loading gate still prevents it from flashing before papers resolve. Accepted file formats are typed off the importer's own constant, so adding a parser without naming it for new users fails `typecheck`. **No migration, RPC, Edge Function, Supabase configuration, RLS/grant change, new dependency, or Production mutation**, and **no billing/upgrade path — C27 unchanged**.
8. **PFA-C07 (owner-selected 2026-08-14) — COMPLETE on acceptance of `PFA-C07-DEAD-COMPONENT-CLEANUP-AND-VISIBLE-BRANDING-001`.** The audit's finding **PFA-F002 — orphaned components** was re-proved against current `main` before acting: a repository-wide importer search found **no import site, no barrel re-export (`src/` has no index barrels at all), and no test/story/config consumer** for either `src/components/layout/Header.tsx` or `src/components/NavLink.tsx`, so both were **deleted**. The only `Header` matches elsewhere were Radix's unrelated `AccordionPrimitive.Header`, and the only `NavLink` matches were react-router-dom's own export, which the deleted wrapper *imported* rather than provided. The same owner instruction also carried a **visible-branding correction**: the product now presents exactly one name, **`PaperLume`**, on every reachable surface — the authenticated left sidebar (was `Paper Whisperer`), the Auth sign-in/sign-up card (was `Paper Whisperer`, and it is the pre-login surface, so leaving it stale would have shown two names across the login boundary), and the browser tab plus `og:title` in `index.html` (both were `Paper Index`). This was **deliberately not a global find-and-replace**: internal/technical identifiers (`paper-whisperer-62`, the `paper-whisperer` package name), Supabase/Vercel/GitHub resource names, historical documentation, and test-harness file comments were **left intact**, and no favicon or logo design was touched (`index.html` does carry two duplicate favicon `<link>` declarations — noted, deliberately not changed here). A new `e2e/branding.spec.ts` pins all four behaviors — Auth heading, dashboard sidebar label, `document.title`, and the **absence** of the legacy labels on both surfaces. **Bounded on purpose:** no other component was removed despite sparse static search, and **no dependency, `package.json`, lockfile, migration, RPC, RLS/grant, Edge Function, Supabase configuration, or Production change**. **PFA-C09 is owner-selected as the next task but was explicitly not started here** — no responsive, breakpoint, sidebar-width, drawer, touch-target, or ARIA work is included; C09 observations were recorded as observations only. **C27 unchanged.**
9. **PFA-C09 (owner-selected 2026-08-14) — COMPLETE on acceptance of `PFA-C09-RESPONSIVE-ACCESSIBILITY-PASS-001`.** The audit's **PFA-F008 — desktop-first layout, narrow-screen behavior unverified** was re-measured in a real browser rather than assumed. **Shell:** the always-rendered `w-64` rail is now `hidden md:flex` and, below `md`, the same navigation content renders inside a `Sheet` drawer opened by a labelled **Open navigation menu** header trigger — Radix owns the focus trap, Escape, background inertness and focus return. The dormant `src/components/ui/sidebar.tsx` was evaluated and **deliberately not adopted** (its `SidebarProvider` imposes a `min-h-svh` layout that conflicts with the dashboard's `h-screen`/`overflow-hidden` table containment, and its mobile Sheet hides the close button and supplies no accessible title); it remains unused and was **not** deleted. One `NAV_ITEMS` list feeds both presentations, and the seven taxonomy modals moved **out** of the rail so opening one from the drawer cannot unmount it. **Table:** horizontal overflow is intentional and contained — at every checked viewport the document and body have **no** horizontal overflow while the paper-list scroll container absorbs the wider table and the rightmost Actions column stays reachable; virtualization, selection, sorting, column visibility, resizing and lazy loading are unchanged. **Keyboard:** sorting moved from an `onClick` on the `<th>` to a real button with `aria-sort` on the header, and the pointer-only resize handle became a focusable `separator` with arrow/Shift-arrow/Home/End resizing clamped to the same bounds `useColumnWidths` enforces — resize keys can no longer trigger a sort, and Tab still escapes. **Names/focus:** the six identical `Settings` gear buttons became `Manage projects`/`Manage tags`/…; paper-row icon controls, the quick cloud-link flow, and the search/year/study-type/notes filters gained real programmatic names; and hover-only exclude controls now stay visible on focus and on touch-sized viewports. A genuine pre-existing defect was fixed centrally: because **every** dialog here is controlled and none uses `DialogTrigger`, Radix's `triggerRef`-based close-focus restore was a no-op and focus fell to `<body>` — `DialogContent` and `SheetContent` now record the opener on `onOpenAutoFocus` and restore it on `onCloseAutoFocus`. **Boundaries:** **no dependency, `package.json`, lockfile, migration, RPC, RLS/grant, Edge Function, Supabase configuration, or Production change**; **C7 still defers native-mobile/PWA packaging** — this is responsive *web* usability only; and this is **not** a formal whole-product WCAG conformance audit and must not be described as one. **C27 unchanged.**

The infrastructure baseline supports this work: schema reconciliation is complete, **73** aligned ledger rows (last `20260810152125_harden_remaining_function_search_paths`, Local = Remote), `npm run typecheck` is at **0 diagnostics** (TYPESCRIPT-BASELINE-001), and the required GitHub Actions `Validate` workflow with `main` branch protection is live (CI-BASELINE-001) — **this CI and branch-protection baseline remains mandatory for every remediation and feature PR**. The paused commercial direction remains valid future-facing work: C17/C18 (Paddle as the future MoR provider) and the built entitlement/quota/subscription/storage infrastructure are preserved, not cancelled — see C27 in [decisions-and-triggers.md](decisions-and-triggers.md) and [owner-decisions.md](owner-decisions.md).

## Authoritative documents

| Document | Authority |
|---|---|
| [README.md](../README.md) | Concise public/developer entry point |
| [architecture-read-path.md](architecture-read-path.md) | Read-path architecture detail |
| [decisions-and-triggers.md](decisions-and-triggers.md) | Durable decisions (C-numbers, S1/S2) + re-evaluation triggers |
| [owner-decisions.md](owner-decisions.md) | Owner gates, blockers, implementation unlock order |
| [deployment.md](deployment.md) | Deployment runbook, env vars, domains, smoke checklists |
| [commercial-architecture.md](commercial-architecture.md) | Entitlement/billing architecture |
| [quotas-and-pricing.md](quotas-and-pricing.md) | Plan structure, MVP baselines, instrumentation |
| [store-launch-checklist.md](store-launch-checklist.md) | Launch/store readiness (mobile deferred) |
| [schema-reconciliation.md](schema-reconciliation.md) | Schema drift inventory, canonical decisions C20–C26, reconciliation roadmap |
| [product-feature-audit.md](product-feature-audit.md) | Point-in-time evidence-based capability audit and owner decision packet (not an evergreen authority) |
| [pfa-c03-staging-and-security-test-plan.md](pfa-c03-staging-and-security-test-plan.md) | PFA-C03 implementation/decision contract and evidence record — **Phase A complete, Phase B complete, PFA-C03 complete**: C03A1-L (PR #173) + C03A2-L (PR #175) merged and accepted; pre-existing DB-security defects remediated/merged/deployed (PR #177); C03B1 suites + `test:db:local` runner merged and accepted (PR #179, merge `0f786dac…`); C03B2 hosted non-required `DB Tests / db-tests` workflow merged and accepted (PR #181, merge `b5761418…`). D4–D5 unresolved and non-blocking |
| [dependency-security.md](dependency-security.md) | npm dependency-security current state, completed remediation clusters, remediation policy and triggers |
| [migration-history.md](migration-history.md) | Historical chronology (not current state) |
| [documentation-policy.md](documentation-policy.md) | Documentation rules, incl. this file's line budget |

## Recent material changes

Keep at most 5 items; remove the oldest when adding.

1. ADD-PAPERS-MOBILE-SELECTORS-001 — **searchable multi-selects are usable on a phone: opening one no longer summons the software keyboard, and its list is a scrollable sheet instead of a dropdown pinned off the bottom of the screen.** Owner-selected **2026-08-15** from real Production iPhone testing. It began as an Add Papers Projects/Tags report and the owner **expanded it after further Production testing**, because the same defect class was present in Filters and Analytics: every one of these controls is a desktop anchored `Popover` nested inside a modal Dialog/Sheet, so Radix autofocused its search field the moment it opened, the panel was anchored to a trigger already near the bottom of an open overlay, and the remaining options could not be reached once the keyboard covered them. Eight behaviours were in scope — **Add Papers** Projects and Tags, **Filters** Projects, Tags and Keywords, **Analytics** Target Keywords and Target Authors, plus the **Filters sheet's own opening focus**, which landed on `Published from year` (a `type="number"` input) and raised the keyboard merely because the user asked to see the filters. All eight are fixed. Filters now focuses its **title** through the public `onOpenAutoFocus` lifecycle, with Radix still owning the trap; and below **768px** the seven selectors render one shared `MobileMultiSelectSheet` — heading focused on open, search visible but **never** autofocused, a plain native `overflow-y-auto` option list (not `ScrollArea`, not cmdk `CommandList`) with `overscroll-contain`, one full-width ARIA checkbox per row, and an explicit Done. **Opening a selector is not a request to type; tapping Search is** — and search still filters, which is proven rather than asserted. Height follows `window.visualViewport` via a small `useVisualViewport` hook rather than `100vh`/`dvh`, because a phone keyboard shrinks the *visual* viewport while leaving the layout viewport at full height, so the sheet also lifts off the bottom edge by whatever the keyboard covers. Nested overlays keep the parent **mounted, painted and inert** with the child explicitly layered above it, and closing the child returns focus to the **exact** trigger — captured by ref, not `document.activeElement`, because Safari does not reliably focus a tapped button. **Desktop is deliberately untouched:** the popovers, their type-immediately autofocus and the compact 199px Add Papers panel all remain, and `src/components/ui/popover.tsx` and the shared `SheetContent` autofocus policy were **not** changed globally. All existing semantics are preserved — shared Add Papers assignment state across Import IDs/Import File/Manual and the whole import callback contract, Filters Any/All (still only shown from two selections), presets, active-filter counting, and `useAnalyticsTargets` as the single authoritative analytics selection state including its 767/768 continuity. A registered `e2e/mobile-selectors.spec.ts` (24 tests) pins it, and **23 of the 24 fail against baseline `5ba2fd4`** — the exception is the desktop-preservation guard, which correctly passes on both trees. **Chromium constrained-viewport testing is a visual-viewport proxy, not a real iOS software keyboard**; the owner's Production iPhone is the real-device evidence and re-confirmation after rollout remains valuable. **No dependency, `package.json`, lockfile, migration, RPC, RLS/grant, Edge Function, Supabase configuration, or Production change.** **PFA-C09 remains COMPLETE / MERGED / LIVE**, **MOBILE-DASHBOARD-DENSITY-001 remains COMPLETE / MERGED / LIVE**, and **no new PFA candidate is selected.**
2. MOBILE-DASHBOARD-DENSITY-001 — **the permanent smartphone toolbar shrank from roughly three quarters of the viewport to a fifth, and the paper table is once again the primary mobile surface.** This is a **post-PFA product/UX refinement discovered through real Production smartphone use, not a reopening of PFA-C09** — which remains COMPLETE / MERGED / LIVE. PFA-C09 stopped the toolbar clipping *sideways* by letting fifteen controls wrap, which traded a horizontal-overflow bug for a vertical-density one: measured on the seeded library at 390x844, the permanent control region ended at **y=623 (73.8% of the viewport)**, the paper list began at **y=635 (75.2%)** and received only **197px (23.3%)** — a sliver, exactly as the owner's Production screenshot showed. "No horizontal overflow" was therefore proven insufficient as acceptance evidence, so the fix is guarded by **measured geometry**: controls now end at **y=161 (19.1%)**, the list begins at **y=173 (20.5%)** and receives **659px (78.1%)**, with the table header and the first data row both inside the first viewport. The remedy is **progressive disclosure, not deletion** — nothing was removed and no touch target was shrunk. Mobile keeps three compact levels (**navigation + Papers/count + Add**, then permanent **Search**, then **Filters / More** plus a compact AI status); year, study type, notes, projects, tags, keywords and presets moved into a **Filters** `Sheet`, and Columns, Find Duplicates, Export and Analytics into a **Library actions** `Sheet`. Search deliberately stays outside that sheet and is never duplicated (exactly one `#paper-search` exists at every viewport). The Filters badge counts **filter categories, not values** — From+To is one Year, five projects are one Project — and excludes the permanently visible search; its accessible name spells the state out rather than leaving a bare numeral. Filters write **straight through to the existing filter state** (no draft/Apply copy), and the shared clear handler is labelled truthfully as **Clear all filters** because it also clears search. **Analytics no longer expands inline on mobile** — it opens as an overlay, verified to leave the table's geometry byte-identical (listTop 173 / height 659 before, during and after) — while desktop keeps its inline Collapsible; both shells render one shared `AnalyticsContent` from one `isAnalyticsOpen` state. Two real defects were found by measurement and fixed: the entity-filter popovers were **clipped at keyboard-constrained heights** (measured at 390x420: 145px cut off below when anchored down, 12px above when flipped up), now capped to `--radix-popover-content-available-height` so Projects/Tags/Keywords stay fully visible; and the Deduplication dialog was **unmounted the instant it closed**, so Radix never fired `onCloseAutoFocus`, the PFA-C09 focus-restore helper never ran and focus fell to `<body>` — it is now mounted lazily but kept mounted, and More → Find Duplicates → close returns focus to the visible **More** trigger. **Desktop is untouched:** at 1280x720 the control region, list top and list height are **identical to the pre-change baseline** (269 / 293 / 403), and the `md` (768px) contract holds exactly — 767px is mobile, 768px is desktop, with no duplicated controls in either tree. A registered read-only `e2e/mobile-dashboard-density.spec.ts` pins all of it, and its density assertions **fail against baseline `13da427`**. **The separately reported Add Papers Projects/Tags smartphone dropdown bug is explicitly NOT fixed here** — `AddPaperDialog` has its own selector implementation, shares no primitive with these filters, and was not modified; it remains a separate owner-observed issue. The two pre-existing non-blocking accessibility follow-ups (`AlertDialogContent` focus restoration, Auth validation-error `role="alert"`) also remain separate. **No dependency, `package.json`, lockfile, migration, RPC, RLS/grant, Edge Function, Supabase configuration, or Production change.** **C7 still defers native-mobile/PWA packaging** — this is responsive *web* density work — and **no new PFA candidate is selected.**
3. PFA-C09-RESPONSIVE-ACCESSIBILITY-PASS-001 — **the app shell is responsive below `md`, table overflow is contained instead of clipped, and the table, sidebar and paper rows are operable by keyboard with real accessible names.** **PFA-F008** was re-measured in a browser at 390/768/1024/1280/1440 rather than assumed: the document and body now show **no** horizontal overflow at any of them, the paper-list scroll container absorbs the wider table, and its rightmost Actions column is reachable at every width. The unconditional `w-64` rail became `hidden md:flex`; below `md` the *same* navigation content renders in a `Sheet` drawer behind a labelled **Open navigation menu** trigger, with Radix providing the focus trap, Escape, background inertness and focus return. The dormant `src/components/ui/sidebar.tsx` was evaluated and **deliberately not adopted** — its `SidebarProvider` imposes a `min-h-svh` shell that fights the dashboard's `h-screen`/`overflow-hidden` table containment, and its mobile Sheet hides the close button and has no accessible title — so it stays unused and undeleted. One `NAV_ITEMS` list drives both presentations and the taxonomy modals moved out of the rail, so opening one from the drawer cannot unmount it. Sorting moved off an `onClick` on the `<th>` onto a real button with `aria-sort` on the header, and the mouse-only resize handle became a focusable `separator` with arrow/Shift-arrow/Home/End steps clamped to the bounds `useColumnWidths` already enforced — resize keys can no longer sort, and Tab still escapes. Six identical `Settings` gear buttons became `Manage projects`/`Manage tags`/…, paper-row icon controls and the search/year/study-type/notes filters gained programmatic names that survive typing, and hover-only exclude controls stay visible on focus and on touch-sized viewports. One pre-existing defect was fixed centrally: since every dialog here is controlled and none uses `DialogTrigger`, Radix's `triggerRef` close-focus restore was a no-op and focus fell to `<body>` — `DialogContent`/`SheetContent` now capture the opener on `onOpenAutoFocus` and restore it on `onCloseAutoFocus`. A registered read-only `e2e/responsive-accessibility.spec.ts` pins all of it; most of its cases fail against the pre-fix tree. **No dependency, `package.json`, lockfile, migration, RPC, RLS/grant, Edge Function, Supabase configuration, or Production change.** **C7 still defers native-mobile/PWA packaging** — this is responsive *web* usability — and this is **not** a formal whole-product WCAG conformance audit; C27 unchanged.
4. PFA-C07-DEAD-COMPONENT-CLEANUP-AND-VISIBLE-BRANDING-001 — **two verified-orphaned components are gone, and the product presents exactly one visible name: `PaperLume`.** The audit's **PFA-F002** finding was re-proved on current `main` rather than trusted: a repository-wide search found **no importer, no barrel re-export (`src/` has no index barrels), and no test/config consumer** for `src/components/layout/Header.tsx` or `src/components/NavLink.tsx`, so both were **deleted** — the residual `Header` hits were Radix's unrelated `AccordionPrimitive.Header`, and the residual `NavLink` hits were react-router-dom's own export, which the deleted compatibility wrapper *consumed* rather than supplied. A clean `typecheck` and `build` after deletion confirm no live TypeScript importer existed. The same owner instruction included a **visible-branding correction** applied to every reachable surface: the authenticated sidebar and the Auth sign-in/sign-up card both moved from `Paper Whisperer` to **`PaperLume`** (Auth matters because it is the *pre-login* surface — leaving it stale would have shown one name before login and another after), and `index.html`'s `<title>` and `og:title` moved from `Paper Index` to **`PaperLume`**. Deleting `Header.tsx` incidentally removed a third stale `Paper Whisperer` label. This was **explicitly not a global rename**: internal identifiers (`paper-whisperer-62`, the `paper-whisperer` package name), Supabase/Vercel/GitHub resources, historical audit and documentation wording, and E2E harness file comments were **preserved deliberately**; no favicon or logo design was changed (the two duplicate favicon `<link>` declarations in `index.html` are noted but intentionally untouched). `e2e/branding.spec.ts` pins the Auth heading, the dashboard sidebar label, `document.title`, and the **absence** of both legacy labels. **No dependency, `package.json`, lockfile, migration, RPC, RLS/grant, Edge Function, Supabase configuration, or Production change**, and **no PFA-C09 work was included in that change** — responsive/accessibility was handled separately, afterwards; C27 unchanged.
5. PFA-C06-BETTER-ONBOARDING-001 — **the empty library is now an actionable first-run surface, and a filtered zero-result view is no longer mistaken for one.** `No papers yet` is replaced by **Build your research library**: the three import methods the Add dialog actually exposes (identifiers, reference file with its accepted formats named, manual entry), an **Add your first papers** CTA that opens the **existing** `AddPaperDialog` through the same Dashboard state as the permanent header button, and a three-step **Add → Organize → Refine** guide pointing at the sidebar's Projects/Tags and Keyword/Study Type pools. State is keyed off the **unfiltered** `totalCount`, so a user who owns papers but has filtered them all out instead sees *No papers match your current filters* with Dashboard's existing `handleClearFilters` (loaded-preset pointer included), and a defensive non-filtered case renders a neutral message rather than claiming the library is empty. Onboarding is **derived from live library state with no persistent flag** — no localStorage, profile column, tour, coachmarks, or dismissal — so it disappears on first import and returns if the library is emptied; the existing loading gate still prevents it from flashing. Accepted formats are typed off the importer's own constant, so adding a parser without naming it fails `typecheck`. **No migration, RPC, Edge Function, Supabase configuration, RLS/grant change, new dependency, or Production mutation; C27 unchanged.**
